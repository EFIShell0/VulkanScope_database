# VulkanScope Database 1.3.8 filter / regional / browser cleanup audit

Database 1.3.8 is a presentation-only successor to immutable 1.3.7. Browser-logo rendering and bundled browser marks are removed. Filter/custom-select labels wrap, single-page page jump controls are inert, and regional presentation uses Automatic / Country / Manual state gating. Country mode uses country locale conventions and a representative IANA zone only for display. No D1 migration or report-identity change is introduced.

Current cache identity: app/browser-compat/release-bootstrap `v1308`, stylesheet `site.v1308.css`, cache key `1308`; the 1.3.7 JavaScript triplet and `site.v0390.css` remain the immediate cache bridge.
