# VulkanScope Database 0.39.22 / VulkanScope 0.80.8 loading, floor and canonical device-type audit

- Immutable predecessor: VulkanScope Database 0.39.21, SHA-256 `76dc0c2099dd56132700e225f338feb5efea3ecd0d15c3b701434b096cae46f2`.
- Current producer/query baseline is VulkanScope 0.80.8 / Vulkan 1.4.361. Submission schema remains 2, technicalReport remains 3 and normalizer remains 16.
- New POST submissions require VulkanScope 0.80.1 or newer. 0.80.0 and every 0.41.x/older producer are rejected before generic envelope validation with an explicit producer-floor error.
- Existing stored historical reports remain readable. No D1 migration, stored payload rewrite, canonical report hash rewrite or destructive cleanup is introduced.
- Historical physical-device presentation normalizes friendly `Integrated GPU`, `Discrete GPU`, `Virtual GPU`, `CPU` and `Other` labels to their canonical `VK_PHYSICAL_DEVICE_TYPE_*` names on read/presentation only. Stored payload bytes and report hashes remain untouched.
- The first paint includes a visible Material-consistent loading status with polite live-region semantics. Report-index discovery and compact-report loading update bounded progress text and a progress bar; fatal failure hides the loading surface before rendering the existing error state.
- Existing bounded index pagination and four-worker compact-report loading concurrency are retained. Loading feedback must not imply that a report is loaded before its request completes.
- Global vertical page scroll controls expose up/down actions only when the document is scrollable and the corresponding direction remains available. Scrolling respects `prefers-reduced-motion`.
- `tools/verify_0808_floor_loading_scroll.py`, `tools/test_0808_floor_loading_scroll_state_machine.py`, and `tools/test_0808_floor_loading_scroll_negative_mutations.py` are mandatory.
- Failing-before-fix against immutable 0.39.21, targeted negative mutations, source-overlay regression, strict-package regression, Worker contract, route/Compare/Surface contracts, Pages staging and source/package audit are mandatory evidence classes.
- Live Cloudflare deployment and remote D1 smoke testing are separate evidence classes and must not be claimed unless actually executed.
