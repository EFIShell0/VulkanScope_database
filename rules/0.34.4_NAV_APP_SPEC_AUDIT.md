# VulkanScope Database 0.34.4 audit

- Main-section transition restored with Web Animations API using opacity/translate3d only.
- Exit duration 90 ms; enter duration 150 ms; reduced-motion bypasses animation.
- Transition token prevents stale rapid-click commits.
- Vulkan API version ordering in filter, Devices Max API and Versions aggregation uses numeric-aware Intl.Collator.
- VulkanScope 0.32.6 schema-v3 fields remain compatible, including queue videoDecode/videoEncode/opticalFlow/dataGraph, exact U64 masks, presentation queues and Surface diagnostics.
- Current Vulkan Hardware Database public source tree category audit found no missing top-level category relative to VulkanScope Database normalized views.
- Canonical/raw separation and Unknown vs Unsupported semantics remain unchanged.

- Added Display/HDR aggregate and report-detail views because VulkanScope 0.32.6 schema-v3 already supplies resolution, refresh rate, wide-gamut, HDR types, luminance and display modes.
- Preserved nested instance/device layer extension lists in aggregate/report detail and Compare.
- Global baseline wording was changed to producer/query baseline so Database metadata does not falsely imply that a project-pinned header/query baseline is the currently published Khronos Registry revision.
