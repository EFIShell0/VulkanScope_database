# VulkanScope Database project rules
- The public database contains VulkanScope technical report data only; personal identifiers, account data, authentication data, private file paths and request IP addresses are not report fields.
- Supported, unsupported, unavailable and unknown are distinct states. A missing query must never be labeled unsupported without direct runtime evidence.
- A successfully queried scalar/property is available even when its value is zero or false unless that field is itself a support boolean.
- Runtime feature booleans and explicit SUPPORTED / NOT SUPPORTED report tokens are support evidence and must preserve supported/unsupported semantics.
- Runtime-enumerated extensions are supported. An absent extension is not labeled unsupported unless the report contains explicit evidence that the extension enumeration is complete and that inference is intentionally implemented.
- Loader/instance API version, physical-device API version and driver version remain separate.
- Vulkan names, extension tokens, format names, color spaces and profile names remain canonical and are not renamed for presentation.
- Vendor presentation may add a human-readable vendor/GPU-family label, but the raw vendor ID remains visible and is the filter key.
- Every technical category present in a submitted VulkanScope report must remain accessible in the database UI, either in a dedicated aggregate view, report detail tab or raw report view. Parsed summaries must never replace or discard the original report text.
- Aggregate coverage statistics are computed only from reports actually loaded by the database and must not imply global Vulkan ecosystem coverage.
- The frontend has no third-party JavaScript, analytics, remote fonts or advertising dependencies.
- The production frontend and API use HTTPS. The Content Security Policy only permits the configured VulkanScope Worker API in addition to same-origin resources.
- Submitted payloads are size-limited and schema-validated. Existing reports are normalized on read so parser fixes apply without rewriting stored payloads.
- Frontend report fetching is concurrency-bounded to prevent avoidable memory/network spikes.
- Browser-visible static assets that materially change must use versioned filenames or an equivalent cache-busting strategy so GitHub Pages/browser caches cannot silently retain an older UI release.
- The vendor filter must show a human-readable GPU family derived from authoritative vendor identity when that family is unambiguous, while retaining the canonical raw vendor ID as the filter key. A separate GPU-model filter must expose the actual reported device name.
- Report detail navigation must remain visible even when a normalized category is empty; an empty category is shown as not reported rather than silently removing the tab.
- The frontend must be able to normalize the stored report text as a compatibility fallback when the deployed Worker normalizer is older than the frontend parser, without changing supported/unsupported/unavailable/unknown semantics.
- Report submission time displayed by the UI must come from the server-side D1 `submitted_at` value, not a client-provided clock, and must preserve an exact machine-readable timestamp while the presentation may be localized.
- Physical-device API version and loader/instance API version must be displayed as separate report metadata fields.
- Reports pagination must never render more than 50 report rows per page; sorting and filtering must be applied before pagination so page boundaries are deterministic.
- Version-like report metadata should use numeric-aware ordering where possible; ordering must never reinterpret missing or unknown technical values as supported/unsupported state.
- Aggregate state filters must be exposed on every view where the normalized data provides a meaningful supported, unsupported, available, unavailable or unknown distinction; a filter must not invent a state that the underlying report cannot justify.
- Coverage visualization is state-semantic: supported is green, unsupported is red, available is blue, unavailable is amber, and unknown is neutral gray. Unavailable or unknown percentages must never reuse the supported green treatment.
- Memory aggregate coverage is based on exact reported VkMemoryPropertyFlags combinations. A combination may be marked unavailable for a report only when that report enumerated memory types and did not contain the combination; missing memory-type data is unknown.
- Aggregate views that contain boolean queue-family capabilities must display both true and false states; false queue capabilities must not disappear from presentation.
- Surface/WSI aggregate presentation keeps capabilities, formats/color spaces, present modes, transform modes, composite-alpha modes, usage flags and presentation queues individually accessible when the report provides those fields.
- Navigation icons are local inline SVG presentation only; they must not introduce third-party assets, scripts, fonts or network dependencies, and text labels remain present for accessibility.
- VK_KHR_portability_subset absence is Unknown/not listed unless a report supplies direct unsupported evidence; the portability view must never infer unsupported solely from absence.
- Explicit state counts and percentages use the same semantic palette everywhere, including aggregate tables and report detail views: supported green, unsupported red, available blue, unavailable amber, unknown gray.

- VkMemoryType.propertyFlags must be decoded to canonical Vulkan memory-property flag names in the UI while retaining the raw numeric/hex mask. Current known bits include DEVICE_LOCAL, HOST_VISIBLE, HOST_COHERENT, HOST_CACHED, LAZILY_ALLOCATED, PROTECTED, DEVICE_COHERENT_AMD, DEVICE_UNCACHED_AMD and RDMA_CAPABLE_NV; unknown future bits remain visible as UNKNOWN_BITS_0x....
- VkMemoryHeap.flags must likewise expose canonical heap flag names while retaining raw masks; unknown bits are never discarded.
- Schema-v3 technicalReport structured memory/queue/format/surface data from VulkanScope is preferred over reparsing human-readable TXT when present; TXT parsing remains a compatibility fallback.
- Desktop navigation must not silently clip the final tab. Compact button geometry is used on wide layouts and the menu layout activates before the navigation can overflow; portrait/mobile layouts must remain usable without horizontal page overflow.

- Main navigation tabs are never clipped or hidden because of viewport width. The brand and navigation retain a deliberate gap; the tab strip is horizontally scrollable with mouse/trackpad/touch, and portrait/mobile uses a two-row header with a full-width scrollable tab strip.
- Canonical Vulkan presentation applies beyond memory: queue flags, video codec operation flags, format feature flags, surface transforms, composite-alpha flags and image-usage flags are decoded to current canonical VK_* token names while the raw mask remains visible. Unknown future bits remain explicit UNKNOWN_BITS_0x....
- Vulkan 64-bit masks must not be rounded by JavaScript Number conversion. For schema-v3 submissions whose structured JSON carries numeric 64-bit format masks, exact reportText decimal masks take precedence until the application emits those values as lossless strings; this exception overrides the general structured-data preference only for unsafe-width integer masks.

- For core property fields whose Vulkan type is unambiguous, canonical decoding also covers subgroup shader-stage flags, subgroup feature flags, sample-count flags and depth/stencil resolve-mode flags. Arbitrary numeric extension properties are not guessed into enums: their raw value remains authoritative unless the field type is explicitly mapped.

- Database release 0.33.4 is compatible with VulkanScope 0.32.4. Both the compile-header baseline and independently validated runtime query catalog are Vulkan 1.4.360; the UI/Worker must not retain the obsolete 1.4.357 validated-query label.
- Schema-v3 structured technicalReport features, detailedProperties, limits, profiles, memory, queues, formats, surface and registry coverage are authoritative when present. TXT remains a compatibility fallback and raw report text remains accessible.
- Exact-width unsigned decimal strings (for example *U64 fields) supplied by VulkanScope take precedence over lossy JavaScript numeric values; producer-supplied canonical names may be displayed while raw exact masks remain visible.
- The browser document title follows navigation state. Main views use '<Section> - VulkanScope Database' (Reports may use the bare product title); report-detail views include the GPU/report context and active detail section.
- Frontend and Worker code must not assume IPv4 address syntax. Request IP addresses are neither parsed into report data nor persisted. Cloudflare edge IPv4/IPv6 handling must remain transport-only and technical report identity must not depend on client IP.
- Database release 0.33.5 preserves VulkanScope 0.32.4/0.32.5 structured-report compatibility; UI-only application 0.32.5 changes do not alter report schema semantics.
- Primary navigation scrolling uses dedicated left/right controls plus direct touch, trackpad and wheel scrolling. Native horizontal scrollbar chrome is hidden; navigation labels must never be covered by the controls or edge fades. Arrow disabled state and edge fade visibility follow the actual scroll position.
- Filter selectors are progressively enhanced into local custom listboxes so option presentation follows the VulkanScope Database design language. The original native select remains the authoritative form state; mouse, touch and keyboard operation (Enter/Space, arrows, Home/End, Escape) must remain functional.
- The hero may link to the official VulkanScope repository, but external links use noopener/noreferrer and introduce no third-party script, font or analytics dependency.
- Report comparison must expose normalized Devices, Properties/Features, Extensions, Formats, Queue families, Memory, Surface and Profiles when the reports provide them; it must not reduce comparison to the generic capability map alone. Missing data remains Unknown/not reported.

- Technical tables with horizontal overflow must not expose browser-native scrollbar chrome as the primary control. The UI must provide design-consistent horizontal controls while retaining direct touch/trackpad scrolling and keyboard accessibility; controls must reflect actual overflow state and never cover data.
- Canonical Vulkan tokens must remain complete and readable. Long tokens may wrap but must not be silently truncated, abbreviated or ellipsized when doing so would hide technical information.
- Custom listbox opening and closing transitions must be symmetrical; DOM refresh must not recreate animated controls in their final state and bypass the opening animation.
- Frontend fatal/API-load errors and static error documents use the VulkanScope Database design language and always provide a Reports-home action plus the official VulkanScope GitHub link. API endpoints themselves remain machine-readable JSON.
- Known Worker API paths called with unsupported HTTP methods return 405 and an accurate Allow header rather than 404.

- Report-detail tab and disclosure transitions are presentation-only and must not delay, mutate, or reinterpret normalized Vulkan data. Rapid interaction must not permit stale animation completion to restore an older tab.
- Disclosure controls that replace native details use semantic buttons, `aria-expanded`, keyboard focus and reduced-motion handling; touch and mouse must share the same state path.
- Queue-family presentation support is shown only from explicit submitted Surface presentation-queue evidence. Missing queue presentation evidence is Unknown, not Unsupported.
- Surface query diagnostics are displayed only when supplied by the report and remain raw/authoritative; UI never fabricates query results.

- Database release 0.33.8 may reuse the local GPU vendor artwork bundled with the VulkanScope Android application. Vendor artwork remains a presentation aid only: vendor IDs and reported GPU names remain authoritative, unknown vendors use the bundled unknown mark, and a logo must never be used to infer unsupported technical data.
- Tables that expose a report/device GPU name include a compact **Logo** column immediately adjacent to the GPU/device-name column when a report object is available. Logo cells are fixed-width, lazy-decoded local assets and must not cause horizontal text truncation or third-party network requests.
- Only explicit Turnip / third-party driver presentation text uses the red accent. The underlying driver mode/name/version remains unchanged and copyable; System Vulkan driver text and unrelated status values must not inherit this treatment.
- Public report POSTs accept `application/json` only, require the VulkanScope application identity fields expected by schema 2, and reject collection states other than complete/available. These checks supplement, not replace, payload-size, forbidden-field and schema validation.

- Database release 0.34.0 renders reported GPU model names with consistent strong emphasis in all report-backed list/table contexts; presentation styling must not alter the raw GPU name.
- Primary navigation view changes use short compositor-friendly opacity/transform transitions only. They must not animate table dimensions, delay Vulkan normalization, or allow stale transition completion to restore an older view; prefers-reduced-motion disables the transition.
- On portrait/mobile layouts with the two-row header, report-detail sticky tabs must clear the full header height and never be hidden beneath the top bar.

- Database release 0.34.0 custom listboxes render System Vulkan driver choices with strong emphasis on the reported GPU model only; system driver mode/version text stays normal weight. Turnip / third-party choices keep their explicit red-accent presentation. This is presentation-only and must not alter native select state or raw report values.

- Database release 0.34.2 requires explicit custom-listbox contrast: in System Vulkan driver choices, the reported GPU model is strong white text while the driver suffix remains muted normal-weight text in normal, hover, focus and selected states; inherited row color must not erase this distinction.

- Database release 0.34.2 presents the Compare “Differences only” option as a custom VulkanScope-styled checkbox while retaining the native checkbox as the authoritative state. The control must preserve mouse, touch, label-click and keyboard operation, expose a visible focus state, use a minimum ~44 px touch target, and honor prefers-reduced-motion.

- Database release 0.34.3 presents physical-device API, loader/instance API and equivalent Vulkan API version values with the shared `api-version-chip` presentation in every applicable aggregate/report/compare view. The underlying version string remains authoritative and must not be renamed, rounded, promoted, or inferred for styling.

- Database 0.34.5 primary-section navigation uses short compositor-only opacity/translate transitions driven by Web Animations API. Rapid navigation is token-gated so stale transitions cannot commit an older view; `prefers-reduced-motion` disables decorative motion.
- Vulkan API-version lists and maxima use numeric-aware version ordering. Lexicographic string ordering must not be used for Vulkan versions because it can misorder values such as 1.4.9 and 1.4.10.
- Database 0.34.5 is regression-audited against VulkanScope 0.32.6 schema-v3 structured reports. Queue video decode/encode/optical-flow/data-graph fields and Surface completeness/query diagnostics remain representable without inventing unsupported state.

- Android Display/HDR data present in schema-v3 `technicalReport.display` is a first-class technical category and must be accessible in both aggregate and per-report Database UI; raw report access alone is not sufficient.
- Instance/device layer extension lists carried by schema-v3 reports must remain visible and must not be discarded when normalizing layers.
- A Database-global baseline label describes the VulkanScope producer/query catalog, not an assertion about the Khronos Registry's current published release. Per-report raw registry/header metadata remains authoritative for that report.

- Database 0.34.5 Display/HDR aggregate identifies the Android device/model, not the GPU. GPU names, GPU-vendor labels and GPU logo assets must not appear in the Display/HDR aggregate view or its hidden filter state.
- Empty reported HDR type lists are presented as Unavailable in Display/HDR, not Unknown or Unsupported. Logos are presentation-only and may be shown only for HDR types explicitly reported by the submission; logo presence never implies support.
- Recognized HDR type artwork may use local assets for Dolby Vision, Dolby Vision 2, HDR10, HDR10+, HDR10+ Advanced and HDR Vivid. Unknown/unmapped HDR type strings remain escaped raw text and are never renamed or dropped.

- Database 0.34.6 uses a dedicated HDR10 asset for HDR10. HDR10 must never be synthesized by cropping, masking or relabeling the HDR10+ artwork. The supplied black HDR10 artwork is rendered on an opaque white background for dark-theme legibility.
- HDR brand artwork is presentation-only. A logo asset never creates support evidence; it may render only when the submitted report explicitly reports the corresponding HDR type.
- When the Worker receives an HTTP Origin header and ALLOWED_ORIGIN is configured to a concrete origin, mismatched browser origins are rejected. Requests without Origin remain valid so the native VulkanScope Android client and command-line diagnostics are not broken.
- Database 0.34.6 is schema-compatible with VulkanScope 0.32.7; the app's Turnip SAF picker fix does not change structured report semantics.

- Database 0.34.7 Display/HDR must not expose or apply vendor, GPU-model, or Vulkan API-version filters. Display/HDR is Android device/display metadata; only filters with direct semantic relevance to that view may affect its rows. Hidden stale filter state must never suppress Display/HDR results.


- Database 0.34.8 top-level filter custom listboxes use a compact responsive presentation and local semantic SVG iconography. The hidden native select remains authoritative; keyboard, mouse and touch behavior and supported/unsupported/available/unavailable/unknown semantics must not change for styling.
- Database 0.34.8 Display/HDR may expose submission-time ordering (Newest first / Oldest first) because it orders report rows without inferring display capability. Vendor, GPU-model and Vulkan API-version filters remain forbidden in Display/HDR, and ordering must use the server-provided `submittedAt` value with deterministic report-ID tie-breaking.


## Release 0.34.9 Compare / Portability density
- Compare and Portability may use denser presentation than general aggregate pages, but no compared capability, raw value, canonical Vulkan token, status state or portability evidence may be omitted or collapsed away solely to save space.
- Compare selector/listbox native select state remains authoritative and Differences-only remains keyboard, mouse and touch operable.
- Portability continues to treat absence of VK_KHR_portability_subset as Unknown/not listed unless direct runtime evidence justifies another state.
- Density changes are presentation-only and must be scoped to Compare/Portability so unrelated views do not regress.

## Release 0.35.0 full database audit
- Generic Vulkan scalar/property values are availability data: zero/false values remain Available unless the field is explicitly a feature/support boolean. Feature booleans retain Supported/Unsupported semantics.
- Compare reads the canonical VulkanScope schema-v2 field names `gpu.deviceType` and `vulkan.loaderInstanceApiVersion`; legacy aliases must not replace current producer fields.
- TXT compatibility normalization parses the current queue `videoCodecOperations` field and registry-coverage section instead of silently folding or dropping them.
- Worker POST body processing is byte-bounded while streaming; requests over 2 MiB are rejected before an unbounded body string is materialized.
- Worker schema checks validate required object shapes, bounded index metadata strings, application/package identity, and technicalReport schema 3 when present.
- Android Display `preferredWideGamut` is presented as the preferred wide-gamut color space reported by Android, never as measured physical-panel gamut coverage. Display/HDR remains separate from Vulkan Surface color-space support.
- The published Khronos Registry version and the VulkanScope producer/query baseline are distinct. As of this audit the public Khronos latest specification is 1.4.359 (2026-08-07); the database keeps VulkanScope's independently pinned producer/query baseline 1.4.360 labeled as such and does not call it Khronos latest.
- Frontend report-detail fetch failures must be surfaced as a visible loaded-set warning/metric; silently dropping failed reports while presenting the loaded set as complete is forbidden.
- Submission deduplication hashes a stable key-sorted JSON representation so equivalent payload objects cannot bypass deduplication merely by reordering object keys; nesting is bounded before canonicalization.


## Release 0.35.1 dominant coverage emphasis
- In coverage distributions, progress-bar fills always retain the semantic state palette for every state.
- Percentage text uses a semantic state color only when that state is the unique largest percentage within the same compared distribution.
- Non-dominant percentage text is neutral white. If the highest percentages are tied, all tied percentage labels remain neutral because no state dominates.
- State-count badges/pills are not changed by dominance styling and retain their existing semantic colors.
- Dominance styling is presentation-only; counts, denominators, percentages, filters and supported/unsupported/available/unavailable/unknown semantics are unchanged.


## Release 0.35.2 full-audit hardening
- The report index is cursor-paginated by the server-authored `(submitted_at, id)` ordering. The frontend must follow all returned index pages; an API page-size limit must never silently make older stored reports disappear from the public UI.
- TXT compatibility normalization preserves current Surface `Color-space extension` and `Format query` diagnostic lines. Structured schema-v3 remains preferred, but fallback parsing must not silently discard these diagnostics.
- Generic Surface capability/property values are availability data unless the field itself is an explicit support state. A false/zero scalar must not be reinterpreted as Unsupported. Query-diagnostic booleans are diagnostic values, not feature-support booleans.
- Compare uses plain canonical memory flag text as data. HTML presentation fragments must never be stored as Compare values or rendered as escaped technical values.
- An explicitly reported empty `display.hdrTypes` list is Unavailable in Compare as it is in Display/HDR; it is not Unknown and never implies Unsupported.
- Submission privacy validation operates on parsed JSON field names, recursively rejects sensitive personal/account/authentication identifiers, and does not reject harmless technical report text merely because it mentions a sensitive-looking phrase. GPU `deviceId` remains the Vulkan device identifier field and is not treated as a personal device identifier.
- Malformed stored JSON must fail as an explicit server error without leaking stack details or corrupting neighboring report responses.
- Worker normalizer version 9 corresponds to the 0.35.2 TXT compatibility behavior.


## Release 0.35.3 dominant coverage background containment
- Coverage progress fills always retain their semantic state color.
- A non-dominant coverage state must not paint its semantic background outside the progress-bar fill; its surrounding coverage container remains transparent/neutral and its percentage label remains neutral white.
- Only the unique dominant percentage in the same distribution may extend semantic emphasis to the percentage text and the surrounding coverage container.
- If the top percentage is tied, no tied state is dominant and all tied coverage containers/percentage labels remain neutral outside their progress fills.
- State-count badges/pills are independent of coverage dominance and must remain unchanged.
- This is presentation-only; counts, percentages, denominators, filters and capability-state semantics must not change.

## Release 0.35.4 Cloudflare account isolation
- Production Wrangler configuration is pinned to VulkanScope Cloudflare account `ccf3de9d3f2a4394af2fb7be7fd5bbf4`; it must not silently deploy to a different Cloudflare account.
- Production D1 binding remains `DB` and is pinned to database `vulkanscope-database` with UUID `8fa65ef5-701d-4110-993d-87381f9763ab`.
- The project-local Wrangler auth profile is named `vulkanscope`. Authentication state itself remains local and must never be committed to the repository.
- npm production deploy, migration, migration-list and D1 diagnostic commands fail closed when the active Wrangler account cannot be verified as the pinned VulkanScope account.
- Local dependencies, Wrangler state, environment/secret files, logs, caches and generated build output must remain excluded from Git through the repository `.gitignore`.
- Account-isolation hardening must not change report schema semantics, stored report contents, public frontend capability-state semantics or D1 report data.


## Release 0.35.5 Windows account-verifier reliability
- The fail-closed account guard must work on Windows without treating a valid activated `vulkanscope` profile as unverifiable solely because `.cmd` execution through Node child-process APIs fails.
- Project-local pinned Wrangler is the preferred verifier executable; JSON `whoami` is preferred and exact account-ID text fallback is allowed only as a compatibility path.
- Debug logging must not contaminate machine-readable identity verification.
- Any missing, unreadable or mismatched account identity still blocks production deploy, migrations, migration listing and D1 diagnostics.


## Release 0.35.6 Vulkan brand-surface parity
- The database shell follows the same chromatic dark-surface quality model as OpenGLESScope Database, but all product-brand accents and interactive chrome are derived from Vulkan official red `#A41E22`; OpenGL ES magenta must not be copied into VulkanScope.
- Background ambient glow, hero surfaces, active/hovered navigation and detail tabs, search focus, filter controls, scroll controls, pagination and action surfaces use restrained Vulkan-red-derived tones rather than neutral gray-only chrome.
- Product-brand theming must never overwrite technical state semantics: supported stays green, unsupported stays red, available stays blue, unavailable stays amber and unknown stays neutral gray.
- Release 0.35.3 dominant-coverage containment remains authoritative; brand theming must not alter coverage denominators, dominance, state colors or percentage meaning.
- Browser-visible changed CSS/JS uses versioned filenames so GitHub Pages and browser caches cannot retain the previous neutral theme.


## Release 0.35.7 producer/database full audit
- VulkanScope 0.33.7 schema-v2 + schema-v3 submissions are the current producer contract. New submissions with schema-v3 data are cross-checked so primary GPU name/vendor/device ID, device API, driver mode/version, loader/instance API and registry/header/report-baseline metadata cannot disagree with the top-level index metadata.
- Current VulkanScope report text must identify the VulkanScope application/version/versionCode/package and contain the core registry, instance, profile, device, feature, limit, format and Surface sections. Report text remains authoritative raw evidence and is never discarded.
- The current published Khronos specification and VulkanScope staging/query baseline are separate facts. As of this audit, the published Registry specification is Vulkan 1.4.359 dated 2026-08-07; VulkanScope's independently pinned producer/query staging baseline remains 1.4.360 and must not be labeled as the published latest specification.
- Frontend API reads are bounded to 4 MiB and 20 seconds. Cursor pagination rejects repeated cursors and report-detail fetching remains concurrency-bounded to four workers.
- Aggregate property, limit and feature coverage uses every loaded report in the denominator. If a capability exists in the loaded universe but is absent from a particular loaded report, that report contributes Unknown, never Unsupported and never disappears from the denominator.
- Format aggregate coverage likewise includes Unknown for a loaded report that does not contain a format row in the selected format set.
- Display/HDR prefers the producer's explicit `hdrCapabilityStatus`. A genuinely missing HDR field is Unknown; an explicitly reported empty `hdrTypes` list is Unavailable. Unsupported is never inferred from an empty HDR list.
- Display/HDR state filtering may expose Supported/Unsupported for explicit wide-gamut support evidence and Available/Unavailable/Unknown for display/HDR availability evidence, but vendor, GPU-model and Vulkan API filters remain inapplicable to the Display/HDR view.
- API responses retain CORS/nosniff/no-referrer/permissions protections and additionally send CORP/COOP plus a restrictive API CSP. Native Android submissions without an Origin header remain supported.
- Worker normalizer version 10 corresponds to the 0.35.7 validation and TXT/structured compatibility behavior.
- No D1 migration is required for 0.35.7; existing payloads remain normalized on read.

## Release 0.35.8 exact Properties / Limits semantics
- For schema-v3 reports, the selected device's `technicalReport.devices[].detailedProperties` is the authoritative Properties dataset and `technicalReport.devices[].limits` is the authoritative Limits dataset.
- DEVICE metadata, Surface metadata, formats, profiles, features, extensions and other compatibility-normalizer rows must never inflate the Properties count.
- A detailed-property section must not become a Limit merely because its section label contains `Limits` or `Sparse Properties`; only the producer's structured `limits` array is authoritative for schema-v3.
- TXT compatibility fallback keeps bracketed non-feature query-result rows as Properties and rows under the literal `LIMITS` heading as Limits.
- Report-detail Properties and Limits tab counts must equal the lengths of their corresponding authoritative per-report arrays when schema-v3 data is present.
- Aggregate Properties and Limits views consume those same separated arrays. Loaded reports missing a property/limit contribute Unknown to that capability's denominator; they are never silently dropped or labeled Unsupported.
- Raw report text and the broader compatibility capability collection remain available for raw/Compare compatibility, but neither is allowed to redefine Properties/Limit counts when structured schema-v3 arrays exist.
- Worker normalizer version 11 corresponds to the 0.35.8 separated Properties/Limits semantics.
- No D1 migration is required for 0.35.8; existing payloads are corrected on read without rewriting stored reports.



## Release 0.35.9 Display & HDR / ABI / home-metric parity
- The primary and report-detail navigation label is **Display & HDR**. Internal normalized field names remain unchanged; presentation naming must not alter Vulkan evidence semantics.
- Reports show the installed application ABI as the primary Platform / ABI value and list every producer-reported `supportedDeviceAbis` value beneath it. Missing ABI evidence is shown as Unknown and is never inferred from GPU identity.
- Android luminance values (`minLuminance`, `averageLuminance`, `maxLuminance`) are displayed with `cd/m²` when the reported value is numeric. Existing `cd/m2`, `cd/m^2`, or `cd/m²` text is normalized for presentation without changing stored report evidence.
- The home metrics expose the current VulkanScope producer/query baseline and the compatible producer contract as separate cards, matching the information hierarchy used by OpenGLESScope Database.
- VulkanScope 0.33.10 remains the current producer reference for Database 0.35.9. Compatibility presentation begins at VulkanScope 0.32.4 for the schema-2 / technical-report-3 contract already preserved by the Database compatibility path.
- No logo, ABI string, HDR value, or presentation label may be used to infer Vulkan feature support.
- Release assets that change JavaScript behavior use cache-busted `app.v0359.js`; the unchanged stylesheet remains `site.v0357.css`.
- No D1 migration or stored-payload rewrite is required for 0.35.9.


## Release 0.36.0 luminance-unit typography parity

- Display & HDR luminance evidence must preserve the producer-reported numeric/text value and must not infer or alter capability state.
- When a luminance value is numeric or already carries a recognized `cd/m2`, `cd/m^2`, or `cd/m²` suffix, presentation normalizes the visible unit to `cd/m²`.
- The `cd/m²` unit must use the same visual hierarchy as OpenGLESScope Database: dedicated `.luminance-unit`, `white-space: nowrap`, `color: var(--muted)`, and `font-size: .92em`; the numeric value remains normal primary table/detail text.
- Aggregate Display & HDR and report-detail Display & HDR must use the same luminance typography.
- Release assets that change JavaScript and CSS use cache-busted `app.v0360.js` and `site.v0360.css`.
- No D1 migration or stored-payload rewrite is required for 0.36.0.

## Release 0.36.2 VulkanScope 0.34.2 complete-report parity
- Database version is 0.36.2.
- Current producer/query baseline is VulkanScope 0.34.2 with Vulkan 1.4.360.
- Current published Khronos Vulkan specification metadata is Vulkan 1.4.360 dated 2026-08-14.
- Schema 2 / technical report 3 remains the accepted complete-report contract; VulkanScope 0.34.2 must not be rejected because of its application version.
- Structured technicalReport `devices[].detailedProperties` is authoritative for generic extension/property values, including Host Image Copy source/destination layout arrays.
- The frontend must expose structured detailed properties in report Properties, relevant aggregate/compare paths and Raw report without extension-specific field whitelists that could silently drop new validated fields.
- Features, limits, memory, queues, formats, Surface/WSI, Display & HDR, device/instance extensions, layers, profiles, registry provenance and raw report text remain independently accessible.
- Database normalization must preserve Unknown, Unsupported, Unavailable and Not applicable distinctions and must not infer missing capability evidence from GPU/device/vendor identity.

## Release 0.36.2 Host Image Copy promoted-core comparison parity
- Compare normalization treats the Vulkan 1.4 Host Image Copy property family as the promoted equivalent of VkPhysicalDeviceHostImageCopyPropertiesEXT without rewriting stored raw report provenance.
- Core 1.4 copySrcLayoutCount, pCopySrcLayouts or legacy copySrcLayouts, copyDstLayoutCount, pCopyDstLayouts or legacy copyDstLayouts, optimalTilingLayoutUUID and identicalMemoryTypeRequirements compare against the corresponding extension-structure fields under one canonical compare identity.
- Existing 0.34.1 reports using copySrcLayouts/copyDstLayouts and 0.34.2+ reports using canonical pCopySrcLayouts/pCopyDstLayouts must compare against older VK_EXT_host_image_copy reports as reported values, not as Not reported.
- The compare alias is presentation/normalization only; raw TXT, stored payloads, detail provenance and source sections remain unchanged.
- Unavailable legacy pointer placeholders remain unavailable evidence and must not be converted into supported or available values.


## Release 0.36.3 technical-differences compare filter
- Compare keeps `Differences only` and adds a second, independently toggleable `Technical differences only` control using the existing compare-toggle visual language.
- `Technical differences only` is enabled by default and removes report-generation metadata differences that do not describe Vulkan, Android platform, device, driver, Surface, memory, queue, format, extension, feature/property, Display/HDR or profile capability state.
- VulkanScope application version/versionCode and server-authored submission timestamp are metadata and are excluded only from the technical Compare view; their stored/raw report values remain unchanged and remain visible when the technical filter is disabled.
- Driver version/mode, Vulkan API versions, Android/ABI/device identity, memory budget, Surface extent and all capability/query evidence remain technical and must never be hidden by this filter.
- Filtering is presentation-only. It must not mutate normalized reports, D1 payloads, support/availability semantics, promoted aliases, raw report text or submission hashes.
- Compare summary field, difference and section counts must reflect the active technical filter so the displayed metrics describe the same visible universe as the tables.


## Release 0.36.5 VulkanScope 0.35.1 complete-report contract
- Database version is 0.36.5 and remains independent from VulkanScope application versioning.
- Current producer/query baseline is VulkanScope 0.35.1 / versionCode 352 with Vulkan 1.4.360.
- Compatible producer versions must use canonical VulkanScope 0.x semantic version form and remain at or above the declared VulkanScope 0.32.4 schema-2 / technical-report-3 floor.
- VulkanScope 0.35.1 must carry versionCode 352. A mismatched current producer identity is rejected fail-closed.
- Android security-patch evidence must use canonical `YYYY-MM-DD` form.
- Top-level application ABI and supported-device ABI evidence must exactly agree with technicalReport ABI evidence; contradictory duplicate evidence is invalid and must not be silently normalized.
- Submission schema 2 and technicalReport schema 3 remain unchanged; detailed properties, limits, formats, memory, queues, Surface/WSI, Display/HDR, extensions, features, profiles, registry provenance and raw report evidence remain accessible.
- Published Vulkan specification metadata remains Vulkan 1.4.360 dated 2026-08-14.
- Production Worker compatibility date is 2026-08-24.
- Browser-visible JavaScript metadata changes use cache-busted `app.v0364.js`; unchanged CSS remains `site.v0362.css`.
- No D1 migration, stored-payload rewrite, report-hash rewrite or capability inference is permitted for this release.
## Release 0.36.5 Cloudflare compatibility-date deploy correctness
- Database version is 0.36.5.
- `worker/wrangler.jsonc` compatibility_date must never be later than the date accepted by the Cloudflare Workers API at deployment time.
- Local timezone rollover must not be used to advance compatibility_date before Cloudflare accepts that date.
- When the local calendar is ahead of Cloudflare/API UTC acceptance, use the latest non-future accepted compatibility date and update it later only after deployment validation.
- Release verification must fail if compatibility_date is the known rejected future date for the audited deployment window.

## Release 0.37.0 VulkanScope 0.41.0 trends and permalink requirements
- Database version is 0.37.0 and remains independent from VulkanScope application versioning.
- Current producer/query baseline is VulkanScope 0.41.0 / versionCode 410 with Vulkan 1.4.360. Compatible producer floor remains VulkanScope 0.32.4+ with schema 2 / technicalReport 3.
- VulkanScope 0.41.0 must carry versionCode 410. Existing 0.35.1 / 352 identity validation remains enforced for that historical release.
- Submission schema 2, technicalReport schema 3, stable canonical hashing and existing D1 stored payloads remain unchanged. No D1 migration or stored-report rewrite is required.
- Trends are computed from the bounded set of reports successfully loaded by the frontend. Percentages are labeled loaded-submission share and never market/device/vendor share.
- Extension trend counts mean an exact extension token was enumerated in a loaded report. Absence from a report must never be labeled Unsupported.
- Trend UI bounds high-cardinality presentation: at most 25 rows per GPU/vendor/driver/API table and the top 50 exact enumerated extension tokens. This is a derived summary and never truncates canonical report data.
- Report permalinks accept only lowercase 64-hex report ids that already exist in the loaded report map. Detail-tab names are allow-listed.
- Compare permalinks accept exactly two valid loaded report ids. URL parameters never become HTML without existing escaping/safe value handling.
- Browser sharing uses the Web Share API when available and clipboard fallback otherwise. No third-party share, analytics or QR service is loaded.
- Frontend compare remains the existing normalized all-category comparison and URL sharing must not alter compare semantics or report payloads.
- Production API remains HTTPS-only with the existing same-origin allow-list, bounded 2 MiB request body, prepared/bound D1 statements and privacy-key rejection.
- Cloudflare observability is enabled with bounded sampling; no report payload or sensitive identifier is intentionally emitted to custom application logs.
- Worker compatibility date remains 2026-08-23 because it is the last deployment-verified non-future date in this project history; it may advance only after Cloudflare accepts a newer date.
- Browser-visible JavaScript is cache-busted as app.v0370.js. Existing site.v0362.css remains unchanged.
- Wrangler is pinned to 4.125.0 for the 0.37.0 release.

## Release 0.37.1 queue, Vulkan Video and query-state semantics
- Database version is 0.37.1 and remains independent from VulkanScope application versioning.
- Current producer/query baseline is VulkanScope 0.41.3 / versionCode 413 with Vulkan 1.4.360. Compatible producer floor remains VulkanScope 0.32.4+ with schema 2 / technicalReport 3.
- `VkQueueFlags == 0` must not be rendered as a fabricated generic `VK_NONE`; it is displayed as zero with an explicit no-queue-capability-bits description.
- Queue capability booleans such as Graphics, Compute, Transfer, Sparse, Protected, Video Decode, Video Encode, Optical Flow and Data Graph are direct queue-flag evidence and may be rendered Supported/Unsupported when their runtime boolean evidence is present.
- `VkQueueFamilyVideoPropertiesKHR::videoCodecOperations` requires separate query state. Successfully queried zero is `VK_VIDEO_CODEC_OPERATION_NONE_KHR`; absent `VK_KHR_video_queue` is Not applicable; failed/missing query evidence is Unavailable or Unknown.
- Missing video-codec query evidence must never be converted into zero, `VK_VIDEO_CODEC_OPERATION_NONE_KHR`, or Unsupported.
- Generic Properties and Limits availability is query availability, not feature support. A reported boolean property value of false may have Query available state and must not be visually presented as Supported/Unsupported unless that field is explicitly a capability-support boolean.
- Structured technicalReport queue evidence is authoritative for schema-v3 queue query-state fields. Compatibility fallbacks for older reports must preserve uncertainty rather than infer support.
- Worker normalizer version 14 corresponds to the 0.37.1 queue/video/query-state semantics.
- No D1 migration, stored-payload rewrite, report-hash rewrite or schema change is required.

## Release 0.38.0 statistics / hash routing / VulkanScope 0.41.4 requirements
- Database version is 0.38.0 and remains independent from VulkanScope application versioning.
- Current producer/query baseline is VulkanScope 0.41.4 / versionCode 414 with Vulkan 1.4.360. Compatible producer floor remains VulkanScope 0.32.4+ with submission schema 2 / technicalReport 3. Existing fail-closed identity rules for historical current-producer releases remain preserved.
- Submission schema 2, technicalReport schema 3, canonical report hashing and the existing D1 stored payloads remain unchanged. No D1 migration, stored-payload rewrite or report-hash rewrite is required.
- `report.schema.json` must describe the same complete-report contract enforced by the Worker: schema-2 submissions require a `technicalReport` object with technicalReport schemaVersion 3. A payload that the published JSON Schema accepts must not be rejected merely because the schema omitted this already-required complete-report member.
- Worker normalizer version 15 corresponds to the 0.38.0 VulkanScope 0.41.4 queue/video validation and presentation contract.
- VulkanScope 0.41.4 queue-family Vulkan Video query states are fail-closed: `available` may carry a numeric mask including genuine zero; `unavailable`, `not_applicable` and `unknown` must carry null numeric operation fields and must never be normalized into zero or Unsupported.
- VulkanScope 0.41.4 device-extension enumeration, extended feature/property query and Vulkan 1.4 query status/reason evidence must be preserved as first-class report diagnostics. Current-producer status tokens are fail-closed to `available`, `incomplete`, `unavailable`, `not_applicable` or `unknown`; reasons remain bounded text. These diagnostics must be visible in report detail and comparable without being reclassified as feature support.
- Every primary frontend view must have a canonical hash route. `trends` is presented canonically as `#statistics`; internal implementation names must not leak into new public URLs.
- Report-detail canonical routes use `#reports/<lowercase-64-hex-id>/<allow-listed-section>`. Report ids must already exist in the loaded report map. User-controlled route segments never become unescaped HTML.
- Two-report comparison canonical routes use exactly `#compare/<loaded-id-1>/<loaded-id-2>`. Browser back/forward and manual valid hash navigation must restore the corresponding view.
- Legacy `?view=`, `?report=&tab=` and `?compare=` links may be accepted only as compatibility inputs and must canonicalize to the hash form; newly generated links must use the canonical hash form.
- Statistics are computed only from successfully loaded reports matching the active frontend filters. Percentages must be labeled loaded-submission share and must never be described as global Vulkan, hardware, GPU, vendor, driver or market share.
- Donut/pie distribution charts are permitted only for mutually exclusive dimensions. Overlapping extension membership must remain a frequency/ranking table and must not be shown as slices of one whole.
- High-cardinality donut presentation is bounded by retaining leading categories and combining the remaining loaded-report counts into an explicit `Other` slice. This is presentation-only and must not truncate canonical reports or alter aggregate totals.
- Distribution graphics must use local first-party SVG/CSS only. No third-party JavaScript chart library, remote font, analytics, ad or chart-generation service is permitted.
- Static HTTP error pages must reference an asset that exists in the same release package; release verification must fail on broken local stylesheet/script references.
- Browser-visible JS/CSS/config changes use cache-busted `app.v0380.js`, `site.v0380.css` and `config.js?v=0380`.
- Worker compatibility date remains 2026-08-23 because it is the last project deployment-verified date; advance it only after a real Cloudflare deploy/test accepts and validates a newer date.
- Wrangler remains pinned to 4.125.0 for 0.38.0 unless a newer version is separately verified before release.


## Release 0.39.0 filter architecture and interactive statistics requirements
- Database version is 0.39.0 and remains independent from VulkanScope application versioning. Current producer/query baseline remains VulkanScope 0.41.4 / versionCode 414 with Vulkan 1.4.360; submission schema 2, technicalReport schema 3 and Worker normalizer 15 remain unchanged.
- Filtering is presentation-only. It must not mutate stored payloads, canonical report hashing, D1 rows, normalized evidence, support/availability semantics or raw reports.
- Cohort filters are view-scoped. A hidden filter must not continue suppressing rows in a view where that filter is not applicable.
- Global report-cohort filters may include exact GPU vendor, GPU model, device Vulkan API, loader/instance API where relevant, driver mode, driver version, exact enumerated device-extension cohort, physical-device type, Android version/device model, application ABI/version and bounded submission-age ranges.
- The exact device-extension cohort filter means that token was enumerated in the report. Absence remains Unknown/not listed and must never be rewritten as Unsupported.
- Display & HDR must never apply GPU vendor, GPU model, device/loader Vulkan API, driver, extension, Vulkan device type, ABI, application version or generic Vulkan capability-state filters. Display & HDR may filter only directly reported display/device evidence such as Android version/device model, submission age/order, HDR availability/type, wide-gamut state, preferred wide-gamut color space, resolution, refresh rate and display mode. Global search on this view is likewise restricted to Android device/display evidence and must not match hidden GPU/driver/Vulkan fields.
- Properties and Limits expose query-state filtering only: Query available, Query unavailable and Unknown/not reported. They must not expose generic Supported/Unsupported filters or columns because boolean false/zero property values are valid queried values, not feature-support conclusions.
- Queue generic Supported/Unsupported/Unknown filtering is enabled only after a concrete queue capability is selected. The unscoped All-capabilities view must not use an “any field matches” state filter.
- Surface generic state filtering is enabled only for a concrete Surface subgroup. Each subgroup uses its own meaningful state vocabulary and may expose an exact subgroup value/token filter; the unscoped All-surface view must not apply one mixed generic state filter.
- Properties and Limits may filter by value behavior: varying, uniform where reported, or missing from at least one loaded report. Missing evidence remains Unknown.
- Features, Formats, Extensions and Profiles may filter by coverage computed over every filtered loaded report. Coverage filters must use the same denominator and Unknown accounting as the visible aggregate rows.
- Format feature-bit filtering must use canonical VkFormatFeatureFlags2 tokens without converting 64-bit masks through unsafe JavaScript Number precision.
- Format subgroup filtering must preserve an explicitly reported zero feature mask as direct evidence: zero means no bits in that queried mask, not Missing/Unknown. When a required format-feature bit is selected, Supported/Unsupported counts describe that exact bit; absent or unavailable masks remain Unknown and must not be converted to zero.
- Memory flag filtering must use exact canonical memory-property or memory-heap flag evidence. Memory-type combination absence is Unavailable only when that report successfully enumerated memory types; reports without memory data remain Unknown.
- Queue filters may include exact queue-family index, canonical VkQueueFlagBits, presentation support state, Vulkan Video query state, exact successfully queried Vulkan Video codec-operation bits and minimum queue count. Missing/unavailable/not-applicable video query evidence must never match a codec-operation token filter.
- Extension and instance-extension namespace filters distinguish KHR, EXT and vendor/other namespaces without inferring support from prefixes.
- Compare may filter visible normalized differences by exact section and text without modifying either report or the underlying comparison universe used outside the filter.
- A visible Clear filters action clears both applicable cohort filters and view-local filters and restores default sorting/grouping for the active view. Non-default subgroup selections themselves count as active view-local filtering and must make Clear filters available.
- Statistics donut charts are limited to mutually exclusive per-report dimensions and use only the currently filtered loaded submissions. Extension membership remains an overlapping frequency ranking, never a donut/pie whole.
- Statistics donuts are locally rendered SVG/CSS, keyboard accessible, expose exact counts and filtered-submission percentages, support exact slice-to-filter interaction, and group high-cardinality trailing categories into an explicit Other slice without changing totals.
- Interactive donut slices and equivalent legend controls expose active filter state accessibly (for example `aria-pressed`) and selecting the already-active exact slice clears that cohort filter without changing report data.
- Statistics must clearly state that all percentages are filtered/loaded-submission shares, not Vulkan ecosystem, market, hardware, GPU, vendor or driver share.
- Extension statistics may be filtered independently by device/instance/both scope, namespace, minimum filtered-submission enumeration coverage and exact text search; absence from a report remains Unknown/not listed.
- Browser-visible JS/CSS/config changes use cache-busted `app.v0390.js`, `site.v0390.css` and `config.js?v=0390`.
- Worker compatibility date remains the last deployment-verified project date, 2026-08-23. No compatibility-date advance, Wrangler upgrade, schema migration, D1 migration or normalizer bump is implied by this frontend/filter release.

## Release 0.39.1 VulkanScope 0.41.5 compatibility hardening requirements
- Database version is 0.39.1 and remains independent from VulkanScope application versioning. Current producer/query baseline is VulkanScope 0.41.5 / versionCode 415 with Vulkan 1.4.360.
- Compatible producer floor remains VulkanScope 0.32.4+ with submission schema 2 / technicalReport 3. Normalizer remains 15 because this release changes validation range/metadata, not normalized output semantics.
- The strict query-diagnostic and queue/Vulkan Video contract introduced for VulkanScope 0.41.4 applies to every compatible producer version at 0.41.4 or newer. It must never be guarded by equality to one exact current version string.
- For 0.41.4+ producers, deviceExtensionStatus, extendedQueryStatus and vulkan14Status remain fail-closed to available/incomplete/unavailable/not_applicable/unknown with bounded reasons.
- For 0.41.4+ producers, queue videoCodecQueryStatus remains fail-closed to available/unavailable/not_applicable/unknown. Available may contain a genuine numeric zero mask; every non-available state requires null numeric mask fields.
- Current producer identity requires VulkanScope 0.41.5 to use versionCode 415. Historical 0.41.4/414, 0.41.3/413 and other schema-compatible supported producers remain accepted under their existing identity rules.
- No D1 migration, report-id/hash rewrite, stored-payload rewrite, schema migration, automatic upload, analytics or capability inference is introduced.
- The complete 0.39.0 view-scoped filters, interactive statistics, canonical hash routes, accessibility and responsive-design contracts remain unchanged.
- Browser-visible producer metadata changes use cache-busted `app.v0391.js` and `config.js?v=0391`; unchanged CSS may remain `site.v0390.css`.
- Worker compatibility date remains the last deployment-verified project date until a real deployment validates a newer date.



## Release 0.39.2 CI checkout and Pages artifact hygiene requirements

- A normal Git source checkout may contain repository-owned top-level `.git` metadata; source verification must not misclassify that checkout metadata as a shipped release artifact.
- The deployable GitHub Pages artifact must be staged from an explicit public allow-list and must never contain `.git`, `.github`, `worker`, `tools`, `rules`, build caches, local dependencies, native binaries, or other repository-only material.
- Source-tree auditing and staged Pages-artifact auditing are separate release gates. The staged artifact audit remains fail-closed for forbidden VCS/build/development material.
- GitHub Pages deployment must upload the staged `_site` tree, not the repository root.
- Every local stylesheet/script/image reference in staged HTML must resolve inside that staged artifact.
- Database schema, D1 data, report IDs, normalizer semantics, producer validation, filter/statistics semantics, and VulkanScope 0.41.5 compatibility are unchanged by this CI/deployment-hygiene patch.


## Release 0.39.3 GitHub Actions / source-audit hardening requirements
- Database version is 0.39.3; data/schema/normalizer/producer semantics remain unchanged from 0.39.2.
- Source auditing prunes repository-owned top-level `.git` before traversal and must never emit artifact errors for normal checkout metadata. Nested `.git` remains forbidden.
- CI invokes the audit with explicit `--source-tree .` and logs `--version` before the audit so stale scripts are diagnosable.
- Pages deployment remains allow-list staged to `_site`, and artifact audit remains fail-closed for VCS/development/build content.
- GitHub Actions use current verified major releases for August 2026: checkout v7, setup-python v7, configure-pages v6, upload-pages-artifact v5 and deploy-pages v5.
- `persist-credentials: false` is used for checkout; Pages/id-token write permission is restricted to the deploy job.
- `.nojekyll` is intentionally included through upload-pages-artifact v5 `include-hidden-files: true`; no other source dotfiles are staged.
- Source audit requires `pages.yml` to be the only workflow YAML under `.github/workflows`; stale deployment workflows are forbidden.
- 0.39.3 distribution archive entries start at repository root so an in-place extraction replaces current `.github` and `tools` files rather than creating a nested version directory.

## Release 0.39.4 tracked-source audit / repository repair requirements
- Database version is 0.39.4; data/schema/normalizer/producer semantics remain unchanged from 0.39.3.
- In a Git checkout, source packaging hygiene is derived from Git-tracked paths (`git ls-files`) rather than recursive traversal of repository-owned `.git` internals.
- Root `.git` metadata is never release content. Nested `.git` entries, `.git` symlinks and tracked VCS metadata remain invalid.
- Every normal audit invocation identifies itself as VulkanScope Database audit tool 0.39.4 in CI logs.
- `.github/workflows/pages.yml` must exactly match `tools/pages.workflow.yml`; additional workflow files are stale and invalid.
- `tools/repair_repository.py --apply` must be able to replace the hidden workflow directory and remove stale `assets/app.v*.js` versions left by in-place ZIP extraction while preserving `.git`.
- Exactly one current versioned frontend app JavaScript asset is allowed in the source tree.
- GitHub Pages staging copies an explicit public-asset allow-list and generated JSON only; it never copies the entire source assets directory blindly.
- Pages artifact validation rejects stale/unexpected assets even when their extension would otherwise be allowed.
- Existing VulkanScope 0.41.5 / Vulkan 1.4.360, schema 2 / technicalReport 3, normalizer 15 and D1/report identity semantics remain unchanged.

## Release 0.39.5 cross-producer comparison / VulkanScope 0.41.7 requirements
- Database release identity is 0.39.5 and the current producer/query baseline is VulkanScope 0.41.7 / versionCode 417 with Vulkan 1.4.360; schema 2, technicalReport 3, normalizer 15, compatibility floor 0.32.4+ and D1 storage remain unchanged.
- Compare must expose cross-producer comparisons when VulkanScope version or versionCode differs. One-sided fields must not be presented as direct driver-regression evidence because collector/query coverage and evaluator behavior may differ between producers.
- Compare must provide a presentation-only `Common evidence only` filter that restricts visible rows to keys present in both reports without rewriting, deleting or reclassifying stored evidence.
- Canonical profile comparison uses the normalized `profiles` collection when available. Legacy `VULKAN PROFILES` capability rows are fallback input only and must not create a second duplicate compare section.
- Profile identity is the canonical profile name; revision belongs to the compared value. Different profile revisions must be called out because results evaluated against different profile definitions are not direct driver-regression evidence.
- `Unknown`, `Unavailable`, `Unsupported`, `Supported` and query-availability semantics remain distinct. Compare filtering must never convert one-sided or missing evidence into unsupported capability.
- Browser-visible JavaScript changes require a new cache-busted `app.v0395.js`; Pages staging remains explicit allow-list only.
- VulkanScope 0.41.7 reports remain schema-compatible and 0.41.4+ strict query/queue fail-closed validation continues to apply to all future compatible producers.


## Release 0.39.6 Image Format Properties2 tuple-state / VulkanScope 0.41.8 requirements
- Database release identity is 0.39.6 and the current producer/query baseline is VulkanScope 0.41.8 / versionCode 418 with Vulkan 1.4.360; schema 2, technicalReport 3, normalizer 15, compatibility floor 0.32.4+ and D1 storage remain unchanged.
- Image Format Properties2 tuple values reported as `Unsupported: VK_ERROR_FORMAT_NOT_SUPPORTED` are direct Unsupported capability evidence in Compare while remaining a successfully completed query for aggregate Properties query-coverage accounting.
- Image Format Properties2 tuple values beginning `Unavailable: VkResult=` remain Unavailable. They must never be converted to Unsupported.
- `Unknown / Not reported` is reserved for a tuple not present in that submission; a concrete negative tuple result must not collapse into missing evidence.
- Historical successful Image Format Properties2 rows compare under the same canonical tuple key against 0.41.8 negative rows, so AVAILABLE -> UNSUPPORTED/UNAVAILABLE is visible without rewriting stored payloads.
- Worker validation for 0.41.8+ rejects fabricated Image Format Properties2 `Unsupported:` text that is not the exact `VK_ERROR_FORMAT_NOT_SUPPORTED` result form and rejects malformed tuple-level `Unavailable:` result strings.
- Browser-visible JavaScript changes use cache-busted `app.v0396.js`. Pages staging remains explicit allow-list only.
- No D1 migration, stored-report rewrite, report-hash rewrite or capability inference is permitted.


## Release 0.39.7 Image Format Properties2 query-outcome separation / VulkanScope 0.41.9 requirements
- Database release identity is 0.39.7 and current producer/query baseline is VulkanScope 0.41.9 / versionCode 419 with Vulkan 1.4.360.
- VulkanScope 0.41.9 successful Image Format Properties2 property payloads remain normal detailed-property evidence. Its non-success tuple outcomes are consumed from the dedicated bounded `technicalReport.devices[].imageFormatQueryResults` dataset and must not be inserted into Properties/Limit property counts.
- Dedicated tuple results preserve canonical tuple identity, semantic state and exact numeric VkResult. `unsupported` is valid only for `VK_ERROR_FORMAT_NOT_SUPPORTED` (-11); other non-zero results are `unavailable`; duplicate tuple names, success code 0 or fabricated statuses are rejected for 0.41.9+ producers.
- Compare merges historical successful Image Format Properties2 rows and the separate 0.41.9 non-success tuple dataset under the same canonical tuple identity, so AVAILABLE ↔ UNSUPPORTED/UNAVAILABLE remains directly comparable without rewriting stored reports.
- Report detail Formats exposes non-success tuple outcomes separately and explicitly states that they are excluded from Properties & Limits totals.
- Historical 0.41.8 embedded negative tuple rows remain readable and valid under their historical producer contract.
- Filtering, report hashing, D1 storage, normalizer version 15, schema 2 / technicalReport 3, compatibility floor 0.32.4+, privacy/security and Vulkan 1.4.360 semantics remain unchanged.

## Release 0.39.8 Image Format Properties2 complete tuple-state / VulkanScope 0.41.10 requirements
- Database release identity is 0.39.8 and the current producer/query baseline is VulkanScope 0.41.10 / versionCode 420 with Vulkan 1.4.360; schema 2, technicalReport 3, normalizer 15, compatibility floor 0.32.4+ and D1 storage remain unchanged.
- VulkanScope 0.41.10 `technicalReport.devices[].imageFormatQueryResults` is a bounded complete state ledger for every scheduled Image Format Properties2 format/tiling tuple and its base, OPAQUE_FD and ANDROID_HARDWARE_BUFFER variants. The ledger remains separate from Properties & Limits totals.
- `available` requires `VkResult=0` and a matching successful full `detailedProperties` Image Format Properties2 payload. `unsupported` is valid only for `VK_ERROR_FORMAT_NOT_SUPPORTED` (-11). Another non-zero `VkResult` is `unavailable`.
- An external-handle variant may be `not_applicable` only when its prerequisite device extension was not enumerated. Its `VkResult` is null and its exact prerequisite reason is retained. Base image-format queries are never Not applicable.
- For 0.41.10+ producers, every scheduled format group has exactly six canonical ledger slots: LINEAR/OPTIMAL × base/OPAQUE_FD/ANDROID_HARDWARE_BUFFER. Missing, duplicated, malformed or contradictory tuple identities fail closed.
- Worker validation cross-checks the complete ledger against the fixed query recipe and aggregate attempted/success/format-not-supported/other-error diagnostics, so a scheduled AHB or OPAQUE_FD tuple cannot silently collapse to Unknown / Not reported.
- Compare ignores `available` ledger rows when a successful detailed-property payload exists, preserving the full successful value. Non-available ledger states overlay the same canonical tuple identity as historical successful/negative evidence.
- Formats detail may summarize all tuple states but must not add the ledger to Properties & Limits counts. Not applicable remains distinct from Unsupported, Unavailable and Unknown.
- Historical VulkanScope 0.41.9 separated non-success datasets and 0.41.8 embedded negative tuple rows remain accepted under their historical producer contracts.
- No D1 migration, stored-report rewrite, report-hash rewrite, capability inference, automatic upload or normalizer/schema bump is permitted.

## Release 0.39.9 / VulkanScope 0.41.11 reporting-state requirements

- The normalizer preserves `Available`, `Unsupported`, `Unavailable`, `Not applicable`, and `Unknown / not reported` as distinct evidence states.
- Generic zero Vulkan masks are rendered as numeric `0`; the Database must not invent an unqualified `VK_NONE`.
- Format support is a whole-format statement derived from the submitted format record. Linear/optimal/buffer feature masks are separately queried values and must not inherit the whole-format Supported/Unsupported badge.
- A zero format feature mask is valid available query evidence and remains `0`; it does not by itself prove that the entire format is unsupported.
- Image Format Properties2 keeps exact tuple semantics: Available only for `VkResult=0` with matching full property payload, Unsupported only for `VK_ERROR_FORMAT_NOT_SUPPORTED` (-11), other non-zero results Unavailable, and absent external-memory prerequisites Not applicable.
- One-sided historical evidence remains Unknown / Not reported. Current explicit Unknown profile rows are retained rather than treated as Unsupported.
- Source release ZIPs exclude root `README.md`, root `release.md`, packaged Fastlane/store metadata, dependency caches, generated Pages staging, and transient build artifacts.

## Release 0.39.10 / VulkanScope 0.41.12 canonical format-token submission requirements

- Database release identity is 0.39.10 and current producer/query baseline is VulkanScope 0.41.12 / versionCode 422 with Vulkan 1.4.360; schema 2, technicalReport 3, normalizer 16, compatibility floor 0.32.4+ and D1 storage remain unchanged.
- Worker Image Format Properties2 tuple validation must accept exact canonical Vulkan `VK_FORMAT_*` tokens emitted by the producer, including ASTC 2D/3D format names whose registry spelling contains lowercase `x` dimension separators such as `VK_FORMAT_ASTC_10x8_SRGB_BLOCK` and `VK_FORMAT_ASTC_4x4x3_UNORM_BLOCK_EXT`.
- Canonical Vulkan format names must not be rewritten, uppercased, normalized to a non-registry spelling, omitted, or filtered merely to satisfy transport validation. Exact producer evidence remains lossless through Database submission.
- Tuple validation remains fail-closed for malformed names: only `VK_FORMAT_` tokens made from uppercase Vulkan token characters plus registry-style lowercase `x` numeric dimension separators are accepted; arbitrary punctuation or free-form names remain rejected.
- Existing complete-ledger semantics remain mandatory: Available requires `VkResult=0` and matching full property evidence, Unsupported requires `VK_ERROR_FORMAT_NOT_SUPPORTED` (-11), other non-zero results are Unavailable, and absent external-memory prerequisites are Not applicable.
- No D1 migration, stored-report rewrite, report-hash rewrite, schema bump, normalizer bump, capability inference, or client-side evidence mutation is permitted for this fix.


## Release 0.39.11 / VulkanScope 0.41.12 query-group unavailable submission requirements

- Database release identity is 0.39.11. The current producer/query baseline is VulkanScope 0.41.13 / versionCode 423 with Vulkan 1.4.360; submission schema 2, technicalReport 3, normalizer 16, compatibility floor 0.32.4+ and D1 storage remain unchanged.
- The Database complete-report contract must remain consistent with VulkanScope's global complete-collection rule: a scheduled advanced query that completed with an explicit query-group `Unavailable` or `Not applicable` result is valid complete-report evidence and must not be rejected merely because tuple-level evidence was never produced.
- For VulkanScope 0.41.12+ the Worker resolves the Image Format Properties2 group result from the exact `Vulkan Query Status` / `Image Format Properties 2 query` row. The row is mandatory and may represent `Available`, explicit `Unavailable: <reason>`, or explicit `Not applicable: <reason>`.
- When the Image Format Properties2 group is `Available`, all 0.41.10+ complete-ledger invariants remain mandatory: exact six-slot format/tiling/external-handle coverage, successful property/available-ledger cross-consistency, exact VkResult semantics, external prerequisite Not applicable rules, fixed query parameters and aggregate diagnostic cross-checks.
- When the entire Image Format Properties2 group is explicitly `Unavailable` or `Not applicable`, the producer must not fabricate tuple results, successful Image Format Properties2 property rows or aggregate query diagnostics. The Worker accepts that explicit query-level state only when those contradictory datasets are absent.
- An empty Image Format Properties2 ledger without the mandatory explicit query-group state remains invalid. Explicit group-level Unavailable/Not applicable combined with tuple/property/diagnostic evidence also remains invalid. Validation is fail-closed and does not normalize contradictory evidence.
- HTTP 400 validation rejection returns a bounded validation-class suffix so the current client can expose which contract family rejected the complete report without echoing report contents or weakening validation.
- No report data may be truncated, omitted, renamed, uppercased, inferred, reclassified or rewritten to obtain acceptance. The 2 MiB limit, fixed official endpoint, redirect prohibition, privacy-field rejection and explicit opt-in submission remain unchanged.
- No D1 migration, stored-payload rewrite, report-hash rewrite, schema bump, normalizer bump or historical-report mutation is permitted for this fix.

## Release 0.39.12 / VulkanScope 0.41.32 / Vulkan 1.4.361 compatibility requirements
- Treat VulkanScope Database 0.39.11 as the immutable predecessor; every changed or new file must be explicitly allow-listed by the regression contract.
- Pin the canonical Vulkan API Registry at 1.4.361 and verify its `VK_HEADER_VERSION` before release.
- Accept the additive schema-2 VulkanScope 0.41.24+ multi-device provenance envelope and cross-check it against technicalReport device evidence.
- Reject a current report if complete-report, physical-device enumeration, or 0.41.32 registry-coverage evidence contradicts the top-level submission.
- Preserve the 2 MiB transport limit as a byte limit; never truncate a complete report to make it fit.
- Bound request memory and reject malformed UTF-8 rather than replacement-decoding it.
- Keep D1 writes idempotent by canonical-payload SHA-256 and `INSERT OR IGNORE`; concurrency tests must preserve one logical report per canonical payload.
- Prefer compact stored-payload detail responses for the web UI to avoid duplicating large normalized report structures in the Worker response.

## Release 0.39.13 Windows UTF-8 deterministic tooling requirements
- Treat VulkanScope Database 0.39.12 as the immutable predecessor; this release is a tooling/release-metadata fix and must not alter Worker report-validation, D1 payload, hashing, migration, privacy, timeout or API semantics without a separately proven defect.
- Every Python `Path.read_text` / `Path.write_text` operation in release tooling must specify an explicit encoding. UTF-8 repository assets must never depend on the Windows active code page, locale, console language or Python default text encoding.
- The exact frontend asset that fails CP1252 decoding but succeeds UTF-8 decoding is a permanent regression fixture; the VulkanScope 0.41.32 verifier must succeed on that UTF-8 asset.
- The quality gate and Pages workflow must run the deterministic UTF-8 regression test before release.
- VulkanScope 0.41.32, Vulkan 1.4.361, schema 2 / technicalReport 3, normalizer 16, D1 chunking, 2 MiB transport and existing report IDs/data remain unchanged.

## Release 0.39.13 Windows UTF-8 deterministic tooling requirements
- Treat VulkanScope Database 0.39.12 as the immutable predecessor; this release is a tooling/release-metadata fix and must not alter Worker report-validation, D1 payload, hashing, migration, privacy, timeout or API semantics without a separately proven defect.
- Every Python `Path.read_text` / `Path.write_text` operation in release tooling must specify an explicit encoding. UTF-8 repository assets must never depend on the Windows active code page, locale, console language or Python default text encoding.
- The exact frontend asset that fails CP1252 decoding but succeeds UTF-8 decoding is a permanent regression fixture; the VulkanScope 0.41.32 verifier must succeed on that UTF-8 asset.
- The quality gate and Pages workflow must run the deterministic UTF-8 regression test before release.
- VulkanScope 0.41.32, Vulkan 1.4.361, schema 2 / technicalReport 3, normalizer 16, D1 chunking, 2 MiB transport and existing report IDs/data remain unchanged.
- Regression verification must ignore only explicitly declared local/generated workspace paths (for example `.git`, `node_modules`, `.wrangler`, `_site`, caches) rather than treating dependency installation output as release-source regressions. An optional local `worker/package-lock.json` may be excluded from release-file hashing only if a separate verifier checks the pinned Wrangler version and package integrity metadata when the lock is present.


## Release 0.39.14 existing-repository / generated-index regression-gate requirements
- Treat VulkanScope Database 0.39.13 as the immutable predecessor. Worker report-validation, D1 payload/chunk storage, report hashing, migrations, privacy, CORS, timeout and API semantics remain unchanged unless a separately proven runtime defect exists.
- Regression verification has two explicit modes: source-overlay mode protects every predecessor-owned path while tolerating unrelated pre-existing repository/history files; strict-package mode additionally rejects every non-allow-listed release-package file.
- A stale versioned frontend app asset is not tolerated. Existing repositories updated by overlay must run the canonical repository repair before the quality gate; only `assets/app.v03914.js` may remain as a versioned frontend app asset.
- `data/index.json` is generated state and must be validated semantically rather than by a fixed SHA-256. Regenerating the index must not invalidate the immutable predecessor contract solely because `generatedAt` or report inventory changed.
- Source-overlay tolerance must never weaken predecessor-file hashing: modifying an unchanged predecessor-owned file still fails. Strict release-package verification must still reject unexpected files.
- The aggregate quality gate must exercise an existing-repository fixture containing legitimate historical source files, generated index regeneration, stale app repair, strict-package rejection and immutable predecessor mutation detection.
- VulkanScope 0.41.32, Vulkan 1.4.361, schema 2 / technicalReport 3, normalizer 16, D1 chunking and 2 MiB transport remain unchanged. No D1 migration is required.


## Release 0.39.15 / VulkanScope 0.41.40 report-text identity compatibility requirements
- Treat VulkanScope Database 0.39.14 as the immutable predecessor; predecessor ZIP SHA-256 is `8405bef9cbe6fc1e2f1c3c5836f3c5c5f3cf612d1afc53313869b204af755777`.
- Current validated producer/query baseline is VulkanScope 0.41.40 / versionCode 450 with Vulkan 1.4.361. Submission schema 2, technicalReport schema 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- Report-text identity is producer-version aware. VulkanScope 0.41.24+ must be accepted only when the TXT evidence contains both exact split lines `Loader API: <loaderApiVersion>` and `Base probe instance API: <instanceApiVersion>` matching the JSON envelope. The ambiguous historical merged line is not sufficient for current producers.
- Historical producer compatibility is preserved: pre-0.41.24 submissions may continue to use `Loader / instance API: <loaderInstanceApiVersion>`. No historical report is rewritten.
- A current complete report must never be truncated, field-dropped, relabeled or normalized merely to satisfy Worker validation. HTTP 400 fixes must correct the consumer contract rather than weaken producer evidence.
- `tools/test_report_text_identity.mjs` and the Worker end-to-end contract are mandatory. Failing-before-fix must be demonstrated against immutable 0.39.14; the successor must accept current split identity, reject a current merged regression, accept historical merged identity, and reject mismatched loader/base-instance values.
- Existing UTF-8 strict decoding, privacy-key rejection, canonical-payload hashing, D1 chunking/idempotency, CORS/account isolation, report completeness and query-state invariants remain mandatory. No D1 migration is introduced.
- Release packaging must use the existing strict-package/extract/reverify workflow; build/Worker-deploy/runtime production evidence remains a separate PASS/FAIL/NOT EXECUTED class.

## Release 0.39.16 / VulkanScope 0.41.41 historical Compare identity requirements
- Treat VulkanScope Database 0.39.15 as the immutable predecessor; predecessor ZIP SHA-256 is `bdf533c45c84644f66ee29d2fccd3151b2990adb7622031c688a33904a609831`.
- Current producer/query baseline is VulkanScope 0.41.41 / versionCode 451 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- Compare must preserve historical report bytes and stored normalized evidence. Compatibility correction is presentation-only and must never rewrite D1 payloads, report hashes or raw report detail.
- For producers before 0.41.40 only, a boolean historical Features row whose exact name is `VkPhysicalDevice*Properties* · <field>` is the old representation of a successfully queried property value. Compare may canonicalize it to section `VkPhysicalDevice*Properties*`, name `<field>`, value `true`/`false`, state Available so it matches 0.41.40+ property evidence.
- Real `VkPhysicalDevice*Features*` rows, non-boolean rows, malformed names and 0.41.40+ producer evidence must not be remapped. Missing evidence remains Unknown/Not reported and is never inferred Unsupported.
- Compare summary wording is semantic. With Differences only disabled, the visible union count is `Visible fields`; `Visible differences` is valid only while the differences filter is enabled.
- `tools/test_compare_04141_compat.mjs` and `tools/test_compare_04141_negative_mutations.mjs` are mandatory. The compatibility test must fail against immutable 0.39.15 and pass against 0.39.16. Negative mutations must reject removal of the producer-version bound, property-struct identity mapping and dynamic visible-metric label. A real Feature row is the false-positive control.
- Worker report-validation, canonical hashing, D1 payload/chunk storage, request UTF-8 bounds, privacy/CORS rules and public-write policy remain unchanged except current producer/query metadata advances to 0.41.41.
- No D1 migration, stored-report rewrite or report-hash rewrite is introduced. Worker deployment and production runtime smoke testing remain separate evidence classes.


## Release 0.39.17 / VulkanScope 0.41.42 Surface evidence-state Compare requirements
- Treat VulkanScope Database 0.39.16 as the immutable predecessor; predecessor ZIP SHA-256 is `0428aff0568bf4124f9cce94c18c951a788b6c0e19a2757d5f172067d1228aed`.
- Current producer/query baseline is VulkanScope 0.41.42 / versionCode 452 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- Surface Compare and report-detail presentation must preserve explicit Surface query state. A false diagnostic/property value is an available queried value, not Unsupported capability evidence.
- `presentationSupported=false` is Unsupported only when the owning Surface query completed Available. If the Surface query is Unavailable, Incomplete, Not applicable or Unknown, presentation support inherits that evidence state instead of fabricating Unsupported.
- Per-queue presentation `supported=false` is Unsupported only when its `vkGetPhysicalDeviceSurfaceSupportKHR` query returned `VK_SUCCESS`; a failed/missing queue query inherits Unavailable/Incomplete/Not applicable/Unknown as appropriate.
- Generic `surface.capabilities` scalar/boolean values use query-availability semantics. Diagnostic `false`, `NO` and numeric zero remain exact reported values and must not be reclassified as Unsupported.
- TXT/structured normalization must retain `surface.queryStatus` and `surface.queryReason` so historical current-schema reports can be compared without rewriting stored bytes or D1 payloads.
- `tools/test_surface_compare_04142.mjs` and `tools/test_surface_compare_04142_negative_mutations.mjs` are mandatory. The compatibility test must fail against immutable 0.39.16 and pass against 0.39.17.
- No D1 migration, stored-report rewrite, report-hash rewrite, normalizer bump, Worker validation weakening or payload schema change is introduced.

## Release 0.39.18 / VulkanScope 0.41.43 producer-baseline requirements
- Treat VulkanScope Database 0.39.17 as the immutable predecessor; predecessor ZIP SHA-256 is `3b1a8c62143e090ec827d7b965e125c0695f5122adb089119b9ce6254510e0bf`.
- Current producer/query baseline is VulkanScope 0.41.43 / versionCode 453 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- This release is a producer-metadata/cache-identity update only. VulkanScope 0.41.43 driver-bound Surface rebind changes application lifecycle behavior but does not change Database payload or evidence semantics.
- Pages frontend, static index and Worker health/list metadata must describe VulkanScope 0.41.43 as the current producer/query baseline. The current cache-busted frontend asset is `assets/app.v03918.js`; stale `app.v03917.js` must not remain in the strict release package.
- Existing 0.39.17 Surface query-state Compare semantics and 0.39.16 historical pre-0.41.40 property/feature Compare canonicalization remain unchanged.
- `tools/test_producer_baseline_04143.mjs` and `tools/test_producer_baseline_04143_negative_mutations.mjs` are mandatory. The baseline verifier must fail against immutable 0.39.17 and pass on 0.39.18; negative mutations must reject stale frontend/Worker producer metadata and stale frontend asset identity with an unrelated generated-state false-positive control.
- Worker report validation, canonical hashing, D1 payload/chunk storage, request UTF-8 bounds, privacy/CORS rules, public-write policy, report IDs, stored payloads and migrations remain unchanged. No D1 migration, stored-report rewrite or report-hash rewrite is introduced.
- Cloudflare Worker deployment and production runtime smoke testing remain separate evidence classes and are `NOT EXECUTED` until actually performed.

## Release 0.39.19 / VulkanScope 0.41.44 producer-baseline requirements
- Treat VulkanScope Database 0.39.18 as immutable predecessor; predecessor ZIP SHA-256 is `97ff5e670762698be7d9a1cac76e442cf7f37a97712c7db4d0c9407e19771f68`.
- Current producer/query baseline is VulkanScope 0.41.44 / versionCode 454 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- This release is producer-metadata/cache-identity only. VulkanScope 0.41.44 changes application-side Vulkan Profiles evaluation but does not change Database submission envelope, technicalReport structure, report-text identity or query-evidence state vocabulary.
- Pages frontend, static index and Worker health/list metadata must identify VulkanScope 0.41.44 as current producer/query baseline. Current cache-busted frontend asset is `assets/app.v03919.js`; stale `app.v03918.js` must not remain in the strict release package.
- Existing 0.39.17 Surface evidence-state Compare semantics and 0.39.16 historical pre-0.41.40 property/feature Compare canonicalization remain unchanged.
- `tools/test_producer_baseline_04144.mjs` and `tools/test_producer_baseline_04144_negative_mutations.mjs` are mandatory. The baseline verifier must fail against immutable 0.39.18 and pass on 0.39.19; negative mutations must reject stale frontend/Worker producer metadata and stale frontend asset identity with an unrelated generated-state false-positive control.
- Worker report validation, canonical hashing, D1 payload/chunk storage, request UTF-8 bounds, privacy/CORS rules, public-write policy, report IDs, stored payloads and migrations remain unchanged. No D1 migration, stored-report rewrite or report-hash rewrite is introduced.
- Cloudflare Worker deployment and production runtime smoke testing remain separate evidence classes and are `NOT EXECUTED` until actually performed.

## Release 0.39.20 / VulkanScope 0.41.45 producer-baseline requirements
- Treat VulkanScope Database 0.39.19 as immutable predecessor; predecessor ZIP SHA-256 is `91e059a52264d5ab1c95ccd8cea9e84925a1a8b12987d2d0b05a0a124af59c7f`.
- Current producer/query baseline is VulkanScope 0.41.45 / versionCode 455 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- This release is producer-metadata/cache-identity only. VulkanScope 0.41.45 changes application-side Vulkan Video exact-profile capability collection but does not change the Database submission envelope, report-text identity, query-state vocabulary or stored-report/hash semantics.
- Pages frontend, static index and Worker health/list metadata must identify VulkanScope 0.41.45 as current producer/query baseline. Current cache-busted frontend asset is `assets/app.v03920.js`; stale `app.v03919.js` must not remain in the strict release package.
- No D1 migration, stored payload rewrite, report hash rewrite, normalizer increment or Compare semantic change is permitted for this release.
- `tools/test_producer_baseline_04145.mjs` and `tools/test_producer_baseline_04145_negative_mutations.mjs` are mandatory. The baseline verifier must fail against immutable 0.39.19 and pass on 0.39.20; negative mutations must reject stale frontend/Worker producer metadata and stale frontend asset identity with an unrelated generated-state false-positive control.
- Source-overlay regression protection, strict-package verification, Pages artifact auditing, Worker contract tests and D1 migration-chain replay remain mandatory. Live Cloudflare deploy/runtime smoke is a separate evidence class and remains NOT EXECUTED unless actually run.

## Release 0.39.21 / VulkanScope 0.41.46 producer-baseline requirements
- Database release identity is 0.39.21. Current producer/query baseline is VulkanScope 0.41.46 / versionCode 456 with Vulkan 1.4.361. Submission schema 2, technicalReport 3, normalizer 16, D1 schema and 2 MiB transport ceiling remain unchanged.
- This release is producer-metadata/cache-identity only. VulkanScope 0.41.46 changes application-side information architecture/presentation but does not change the Database submission envelope, report-text identity, evidence-state vocabulary, normalized Compare semantics or stored-report/hash semantics.
- Pages frontend, static index and Worker health/list metadata must identify VulkanScope 0.41.46 as current producer/query baseline. Current cache-busted frontend asset is `assets/app.v03921.js`; stale `app.v03920.js` must not remain in the strict release package.
- No D1 migration, stored payload rewrite, hash rewrite, normalizer bump or Worker validation broadening is permitted.
- `tools/test_producer_baseline_04146.mjs` and `tools/test_producer_baseline_04146_negative_mutations.mjs` are mandatory. The baseline verifier must fail against immutable 0.39.20 and pass on 0.39.21; negative mutations must reject stale frontend/Worker producer metadata and stale frontend asset identity with an unrelated generated-state false-positive control.
- The immutable-predecessor regression contract, full source quality gate, deterministic package creation, strict release-tree verification and extracted-ZIP quality gate remain mandatory. Cloudflare live deploy/runtime is NOT EXECUTED unless actually run.


## Release 0.39.22 / VulkanScope 0.80.8 loading, producer-floor, canonical-device-type and page-scroll requirements
- Database version is 0.39.22. Treat VulkanScope Database 0.39.21 as immutable predecessor; predecessor ZIP SHA-256 is `76dc0c2099dd56132700e225f338feb5efea3ecd0d15c3b701434b096cae46f2`.
- Current producer/query baseline is VulkanScope 0.80.8 / Vulkan 1.4.361. Submission schema 2, technicalReport 3 and normalizer 16 remain unchanged.
- New report submissions require VulkanScope 0.80.1 or newer. The floor is enforced server-side before generic schema validation and may not be bypassed by versionCode drift or a future minor version string.
- Historical reports below the new submission floor remain readable. This release must not delete, rewrite or rehash stored historical payloads.
- Historical friendly physical-device labels are canonicalized at read/presentation time: `Integrated GPU` -> `VK_PHYSICAL_DEVICE_TYPE_INTEGRATED_GPU`, `Discrete GPU` -> `VK_PHYSICAL_DEVICE_TYPE_DISCRETE_GPU`, `Virtual GPU` -> `VK_PHYSICAL_DEVICE_TYPE_VIRTUAL_GPU`, `CPU` -> `VK_PHYSICAL_DEVICE_TYPE_CPU`, and `Other`/`Other GPU` -> `VK_PHYSICAL_DEVICE_TYPE_OTHER`. Already-canonical or unknown future values remain unchanged.
- Database startup must present a visible loading surface immediately and update report-index/report-body progress without falsely claiming completion. Loading status uses an accessible polite live region and the existing VulkanScope dark/rose design system.
- Existing bounded pagination and four-worker compact-report loading remain the safety/performance contract unless separately demonstrated insufficient. Progress UI must not introduce unbounded queues or duplicate full-payload materialization.
- Add global vertical up/down page-scroll controls that are boundary-aware, hidden when the page is not scrollable, keyboard-accessible and reduced-motion-aware.
- `tools/verify_0808_floor_loading_scroll.py`, `tools/test_0808_floor_loading_scroll_state_machine.py`, and `tools/test_0808_floor_loading_scroll_negative_mutations.py` are mandatory with predecessor FAIL, successor PASS, targeted negative mutations and strict package re-verification.
- Live Cloudflare deployment and remote D1 validation remain separate evidence classes and may not be reported PASS unless actually performed.

## Release 0.39.23 / VulkanScope 0.80.9 producer-floor and Vulkan Encyclopedia requirements
- Database version is 0.39.23. Treat VulkanScope Database 0.39.22 as immutable predecessor; predecessor ZIP SHA-256 is `912fdd8fc8179b13e2fed1cbbc2db8e78aa6406849fb340076464fa893bccc9c` with 182 files.
- Current producer/query baseline is VulkanScope 0.80.9 / Vulkan 1.4.361. Submission schema 2, technicalReport 3 and normalizer 16 remain unchanged.
- New report submissions require VulkanScope 0.80.3 or newer. VulkanScope 0.80.2 and every lower producer are rejected server-side before generic submission validation. Historical stored reports remain readable and are not deleted, rewritten, renormalized in storage or rehashed.
- The Database exposes one top-level `Encyclopedia` destination adapted to the existing Database visual/navigation system. It is an offline/local reference surface and must not make network documentation requests.
- Database Encyclopedia source authority is the same locked Vulkan 1.4.361 `vk.xml` and curated evidence/VkResult semantics used by VulkanScope. The deterministic runtime corpus contains exactly 842 `vk*` commands, 6241 `VK_*` tokens, 2457 `Vk*` types, 474 extensions and 50 canonical VkResult entries.
- Registry/reference presence is never runtime capability evidence. The Database Encyclopedia must preserve the Vulkan Video Not applicable clarification and the Supported/Unsupported/Unavailable/Not applicable/Unknown distinctions from the application reference model.
- Encyclopedia search categories are All, VkResult, Commands, VK_*, Types and Extensions. Large registry symbol families require at least two search characters and visible results remain bounded to 24. The existing Database global page up/down controls remain authoritative for Encyclopedia page scrolling.
- `tools/generate_encyclopedia_03923.py` deterministically regenerates `assets/encyclopedia.v03923.js` from the locked inputs. Checked-in generated output must compare byte-for-byte with regeneration.
- `tools/verify_0809_floor_encyclopedia.py`, `tools/test_0809_floor_encyclopedia_state_machine.py` and `tools/test_0809_floor_encyclopedia_negative_mutations.py` are mandatory. The source verifier must fail against immutable 0.39.22 with version checking disabled and pass on 0.39.23. Negative mutations must reject a lowered producer floor, Encyclopedia-route removal, runtime/reference evidence conflation and locked-census drift while a harmless CSS whitespace change passes as the false-positive control.
- No D1 migration is introduced. Existing loading progress, canonical historical physical-device-type presentation, global page scroll controls, Compare semantics, report hashes, payload chunking, privacy/security boundaries and bounded request behavior remain mandatory.
- Live Cloudflare Pages/Worker deployment and remote D1 smoke testing are separate evidence classes and may be called PASS only if actually executed.

## Release 0.39.24 / VulkanScope 0.80.10 / Vulkan 1.4.362 requirements
- Database version is 0.39.24. Treat VulkanScope Database 0.39.23 as immutable predecessor; predecessor ZIP SHA-256 is `d53b6f971d73f6f1ebe6a9ea7ae9ad7dee387c8321c5c5bf5dfb2704b06cfeab` with 191 files.
- Current producer/query baseline is VulkanScope 0.80.10 / Vulkan 1.4.362. New-submission floor remains VulkanScope 0.80.3; 0.80.2 and older remain rejected while historical reports remain readable.
- The bundled canonical vk.xml is Vulkan 1.4.362 / header 362 with SHA-256 `cf31c965cf6e788697139601da0c7e02a75a9b6c7ac764e7641f5521ffd9da06`; Vulkan-Headers provenance is tag v1.4.362 commit `ee2ec5fd83dafce291024683b50dc89219333076`.
- Database Encyclopedia must be regenerated from the same locked 1.4.362 registry and curated evidence/VkResult model as the application: exactly 842 commands, 6248 VK_* tokens, 2461 Vk* types, 476 extensions and 50 VkResult entries.
- Registry/reference presence remains separate from runtime capability evidence. Existing Supported/Unsupported/Unavailable/Not applicable/Unknown and Vulkan Video clarification semantics remain unchanged.
- Current frontend assets are `assets/app.v03925.js` and `assets/encyclopedia.v03924.js`; stale current-version assets must not remain in a strict release package.
- Submission schema 2, technicalReport 3, normalizer 16, D1 schema, report hashes, payload chunking, historical device-type canonicalization, loading progress, global scroll controls and privacy/security limits remain unchanged. No D1 migration or stored payload rewrite is introduced.
- `tools/verify_vulkan_1_4_362_03924.py` and `tools/test_vulkan_1_4_362_03924_negative_mutations.py` are mandatory. The verifier must fail against immutable 0.39.23 with version checking disabled and pass on 0.39.24. The mutation suite must reject registry provenance drift, producer-baseline drift, Encyclopedia corpus drift and generator census drift while accepting an unrelated wording-only change as a false-positive control. Deterministic generation, immutable predecessor regression, source quality gate, strict package verification and extracted-ZIP quality gate remain mandatory.
- Live Cloudflare Pages/Worker deployment and remote D1 smoke testing remain NOT EXECUTED unless actually run.
## Release 0.39.24 GitHub Actions overlay-repair hotfix
- A long-lived Git checkout may retain predecessor `assets/app.v*.js` files when a release ZIP is overlaid because archive extraction cannot delete tracked files absent from the successor package. GitHub Actions must therefore run `python tools/repair_repository.py --apply` immediately after Python setup and before `audit_database.py --version`, `quality_gate.py`, index generation or source-tree audit.
- The repair step is workspace-local and deterministic: it restores the canonical Pages workflow and removes stale versioned frontend app assets without changing report data, D1 state, hashes, Worker validation, schema, normalizer or API semantics.
- CI must then run `repair_repository.py --check` and `audit_database.py --version`; the existing-repository fixture must reproduce the pre-repair stale-asset failure and prove the post-repair sequence passes.
- Frontend JavaScript syntax validation for `assets/app.v03925.js` and `assets/encyclopedia.v03924.js` must use one valid YAML multiline `run: |` block.
- This is a release-tooling/CI hotfix within 0.39.24. Vulkan 1.4.362 registry content, 0.80.10 producer baseline, 0.80.3 submission floor, Encyclopedia corpus, stored reports and runtime presentation are unchanged.

## Release 0.39.25 loading / Encyclopedia UI coherence requirements
- Database version is 0.39.25. Treat VulkanScope Database 0.39.24 as immutable predecessor; producer/query baseline remains VulkanScope 0.80.10 / Vulkan 1.4.362 and new-submission floor remains VulkanScope 0.80.3.
- This release is browser-presentation-only: no D1 migration, report rewrite, report-hash rewrite, schema change, normalizer bump, producer-floor change or Vulkan registry/corpus change is permitted.
- Database loading remains an immediate accessible `role=status` / polite live region and continues reporting truthful report-index and bounded report-body progress.
- Loading and Encyclopedia reuse the existing Database neutral panel/card, line, radius, typography, badge, notice, search and rose-accent design tokens rather than creating an independent visual theme.
- The Encyclopedia remains offline/local and backed by the unchanged locked Vulkan 1.4.362 corpus: 842 commands, 6248 VK_* tokens, 2461 Vk* types, 476 extensions and 50 VkResult entries.
- Registry/reference presence remains separate from runtime capability evidence; Vulkan Video Not applicable clarification and Supported/Unsupported/Unavailable/Not applicable/Unknown distinctions remain unchanged.
- Encyclopedia categories, two-character minimum for large registry symbol families and 24-result visible bound remain unchanged. Global page up/down controls remain authoritative; no nested vertical Encyclopedia scroller is introduced.
- Browser-visible application JavaScript advances to `assets/app.v03925.js`. The unchanged generated corpus remains `assets/encyclopedia.v03924.js`; main stylesheet changes are cache-busted through `site.v0390.css?v=03925`.
- `tools/verify_03925_ui_coherence.py` and `tools/test_03925_ui_coherence_negative_mutations.py` are mandatory, alongside the retained 0.39.22/0.39.23 loading/Encyclopedia and 0.39.24 Vulkan 1.4.362 gates.


## Release 0.39.26 / VulkanScope 0.80.12 submission-diagnostics requirements
- Database version is 0.39.26. Treat VulkanScope Database 0.39.25 as immutable predecessor. Current producer/query metadata advances to VulkanScope 0.80.12 / Vulkan 1.4.362; the new-submission floor remains VulkanScope 0.80.3.
- VulkanScope 0.80.12 preserves submission schema 2, technicalReport 3, report-text identity and the 0.80.10+ Vulkan 1.4.362 registry contract. A canonical 0.80.12 / versionCode 812 fixture must be accepted with HTTP 201.
- Do not weaken, normalize, truncate or rewrite complete-report evidence to obtain acceptance. Existing fail-closed validation remains authoritative.
- HTTP 400 validation feedback must distinguish at least exact envelope-shape failure, producer version/versionCode identity failure and the 0.80.10+ Vulkan 1.4.362 registry contract instead of collapsing those failures into `envelope_or_report_contract`.
- Validation-class feedback remains bounded and must not echo submitted capability values or personal/device-identifying contents.
- No D1 migration, report rewrite, report-hash rewrite, schema bump, normalizer bump, producer-floor change or Vulkan registry/corpus change is permitted.
- Browser loading/Encyclopedia presentation from 0.39.25 is retained unchanged apart from release/current-producer metadata and normal cache-busting.
- `tools/verify_03926_0812_submission_diagnostics.py` and its negative-mutation suite are mandatory alongside the complete Worker contract.

## Release 0.39.27 exact producer registry-baseline string requirements
- Database version is 0.39.27. Treat VulkanScope Database 0.39.26 as immutable predecessor; predecessor ZIP SHA-256 is `e3fe56410d806e65ad09c4cf0595ba743e925f89f9525b89ad667284f266d99b` with 206 files.
- Current producer/query metadata remains VulkanScope 0.80.12 / versionCode 812 / Vulkan 1.4.362. The new-submission floor remains VulkanScope 0.80.3; schema 2, technicalReport 3 and normalizer 16 remain unchanged.
- Worker registry validation must match the exact producer-emitted `registryBaseline` and `technicalReport.registryCoverage.baseline` strings. VulkanScope 0.80.10+ emits `Vulkan 1.4.362`; VulkanScope 0.80.3 through 0.80.9 emits `Vulkan 1.4.361`.
- The Worker must not strip, add at ingest time, normalize or accept an alternate unprefixed registry-baseline spelling merely to make a submission pass. Exact cross-consistency with producer evidence remains fail-closed.
- Header provenance remains exact: `Vulkan 1.4.362 compile headers; validated query catalog Vulkan 1.4.362` for 0.80.10+ and the corresponding `Vulkan 1.4.361 ...` form for 0.80.3 through 0.80.9. Registry report schema 4, catalog schema 6, 110 implemented physical-device structs, 104 validated query groups and the existing runtime token counts remain unchanged.
- The canonical Worker contract must use producer-realistic prefixed fixtures and must explicitly reject the former unprefixed `1.4.362` / `1.4.361` fixture spellings with the bounded registry-contract validation class.
- HTTP validation diagnostics introduced in 0.39.26 remain unchanged and bounded; submitted device/capability values are never echoed in validation errors.
- No D1 migration, stored-report rewrite, report-hash rewrite, schema bump, normalizer bump, producer-floor change, Vulkan registry/corpus change or capability inference is permitted.
- Browser loading/Encyclopedia behavior is unchanged. Release identity advances to cache-busted `assets/app.v03927.js`; the generated Encyclopedia remains `assets/encyclopedia.v03924.js` and the Vulkan 1.4.362 842/6248/2461/476/50 corpus remains unchanged.
- `tools/verify_03927_registry_baseline_prefix.py` and `tools/test_03927_registry_baseline_prefix_negative_mutations.py` are mandatory alongside the retained 0.39.26 diagnostics gate and complete Worker contract.


## Release 1.0.0 / VulkanScope 1.0.0 producer-baseline requirements
- Database version is 1.0.0. Treat VulkanScope Database 0.39.27 as immutable predecessor; predecessor ZIP SHA-256 is `bc87d37a8475091385ffd8155822c22ecdb65cd5e697307412cdab93cc8c1b63` with 211 files.
- Current producer/query baseline is VulkanScope 1.0.0 / versionCode 1000 / Vulkan 1.4.362. New-submission floor remains VulkanScope 0.80.3; historical stored reports remain readable and are not rewritten or rehashed.
- Producer version parsing uses canonical three-component semantic versions across major-version boundaries. VulkanScope 1.0.0 must receive every semantic validation gate that applies to 0.80.10+ producers and must not bypass current registry/query checks because the major version changed.
- Current producer identity is fail-closed: VulkanScope 1.0.0 requires versionCode 1000. Historical 0.80.x and 0.41.x identity rules remain intact. Unknown future schema-compatible producers are not assigned guessed versionCode formulas.
- VulkanScope 1.0.0 preserves the exact producer registry strings `Vulkan 1.4.362` and `Vulkan 1.4.362 compile headers; validated query catalog Vulkan 1.4.362`; alternate unprefixed forms remain rejected.
- Submission schema 2, technicalReport 3, normalizer 16, D1 schema, report hashes, payload chunking, 2 MiB transport ceiling, privacy/security rules, Vulkan 1.4.362 registry corpus and Encyclopedia 842/6248/2461/476/50 remain unchanged. No D1 migration or stored-report rewrite is introduced.
- Frontend release identity advances to `assets/app.v1000.js`; generated Encyclopedia remains `assets/encyclopedia.v03924.js` and unchanged stylesheet remains `assets/site.v0390.css` with release cache key 1000.
- `tools/verify_1_0_0_producer_baseline.py` and `tools/test_1_0_0_producer_baseline_negative_mutations.py` are mandatory. The verifier must fail against immutable 0.39.27 with version checking disabled and pass on 1.0.0. Negative mutations must reject loss of major-version parsing, 1.0.0/1000 identity, current producer metadata, exact registry contract and current frontend identity while accepting an unrelated wording-only change.
- Live Cloudflare Pages/Worker deployment and remote D1 smoke testing remain separate evidence classes and may be called PASS only when actually executed.

## Release 1.0.1 full security, specification, correctness and Surface evidence requirements
- Database version is 1.0.1. Treat VulkanScope Database 1.0.0 as immutable predecessor; predecessor ZIP SHA-256 is `2199e7cec2d86caa9c652f194166ccaa1c9b7391a4e5cd0895dbfd38e5129e7a` with 216 files.
- Current producer/query baseline is VulkanScope 1.0.1 / versionCode 1001 / Vulkan 1.4.362. VulkanScope 1.0.0 / 1000 remains an accepted historical compatible producer. The new-submission floor remains VulkanScope 0.80.3.
- Surface availability/presentation normalization is evidence-state aware. `presentationSupported=false` may be `unsupported` only when Surface query status is `available`. For `incomplete`, `unavailable`, `not_applicable` or `unknown`, visible and normalized state must preserve that query evidence instead of fabricating Unsupported.
- Worker report-text normalization must not depend on whether `Query status=` appears before or after `Available=..., presentation=...`; raw fields are parsed first and final item state is synchronized after the Surface section has been consumed.
- Pages Surface detail presentation uses the same query-aware Surface state helpers and must not classify these values with direct Boolean-only logic.
- Schema 2, technicalReport 3, normalizer 16, exact Vulkan 1.4.362 registry/header contract, 842/6248/2461/476/50 Encyclopedia corpus, D1 migrations, stored report hashes, payload chunking, 2 MiB transport bound and privacy/security controls remain unchanged. No migration or stored-report rewrite is permitted.
- `tools/verify_1_0_1_full_audit.py`, `tools/test_surface_evidence_1001.mjs` and `tools/test_1_0_1_full_audit_negative_mutations.py` are mandatory. The verifier must fail against immutable 1.0.0 with version checking disabled for the demonstrated Surface defect, pass on 1.0.1, reject representative regressions and accept an unrelated wording-only mutation.
- Final release evidence requires retained source/Worker/route/Compare/registry/floor/diagnostic gates, D1 migration replay, staged Pages audit, immutable predecessor regression verification in source-overlay and strict-package modes, clean-extract checks and two byte-identical deterministic ZIPs. Live deployment/remote D1 smoke remains NOT EXECUTED unless actually run.

## Release 1.0.2 / VulkanScope 1.0.2 producer-baseline requirements
- VulkanScope Database version is 1.0.2. Immutable predecessor is VulkanScope Database 1.0.1, ZIP SHA-256 `4215b8bf6b444035d203a1fd06ed5df6215ba71755b7f44f8d4bc1c7b366dcdb`, with 222 packaged files.
- Current producer/query baseline is VulkanScope 1.0.2 / versionCode 1002 with Vulkan 1.4.362. Historical VulkanScope 1.0.1 / 1001, 1.0.0 / 1000 and the existing 0.80.3+ producer floor remain accepted under their exact identity and evidence contracts.
- This Database release is a producer-metadata/cache-identity compatibility update for the application 1.0.2 UI/connectivity/Turnip presentation release. Submission schema 2, technicalReport 3, normalizer 16, canonical report hashing, Compare semantics, report-text identity, Surface evidence semantics, D1 schema/chunking and stored payload bytes do not change.
- Pages, static index, Worker health/list metadata and cache-busted frontend identify VulkanScope 1.0.2 as current. The sole versioned frontend application asset is `assets/app.v1002.js`; stale `app.v1001.js` is forbidden from the strict release package.
- Worker producer identity is fail-closed: VulkanScope 1.0.2 must carry versionCode 1002. A mismatched 1.0.2 code is rejected without weakening historical identity validation.
- No D1 migration, stored-report rewrite, report-hash rewrite or normalizer bump is permitted for this release.
- `tools/verify_1_0_2_producer_baseline.py`, `tools/test_1_0_2_producer_baseline_state_machine.py` and `tools/test_1_0_2_producer_baseline_negative_mutations.py` are mandatory. The verifier must fail immutable 1.0.1 with version checking disabled, pass 1.0.2, reject producer/cache/normalizer/migration regressions and accept an unrelated wording-only false-positive control.
- Final packaging requires immutable regression verification, full Worker contract, in-memory D1 migration-chain replay, staged Pages artifact audit, clean-extract re-verification, source/extract byte equality and two independently produced byte-identical deterministic ZIPs. Live Cloudflare deployment and production D1 smoke tests remain `NOT EXECUTED` unless actually run.

## Release 1.0.3 / VulkanScope 1.0.15 preloaded UI and compatibility requirements
- Treat VulkanScope Database 1.0.2 as the immutable predecessor; predecessor ZIP SHA-256 is `0ef8b84379cff75c1d4e4cccb0aa1a79fc8c678efc671118e0cf6d5ed56fff4c`.
- Database release identity is 1.0.3. Current producer/query baseline is VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362. New-submission floor remains VulkanScope 0.80.3. Submission schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashing, stored payload bytes and the 2 MiB submission ceiling remain unchanged.
- VulkanScope 1.0.x producer identity must fail closed: versionName `1.0.P` maps to versionCode `1000 + P`. The Worker contract must explicitly accept 1.0.15/1015 and reject a mismatched 1.0.15 identity.
- Compatibility with VulkanScope 1.0.15 must be source-evidenced rather than assumed. The packaged compatibility contract must record that `technicalReportJson` and `databaseSubmissionJson` are byte-equivalent to VulkanScope 1.0.2, while the 1.0.15 submission path only adds the application-side validated-network guard and does not alter the Database payload contract.
- The reports page must not block every visit on refetching every stored report. GitHub Pages deployment must build a same-origin static preload snapshot from the complete paginated Worker index plus compact report payloads, split it into bounded content-hashed JSON chunks, and publish the manifest/chunks under `data/preload/`.
- Browser startup must prefer the complete deploy-time preload snapshot, render only after the snapshot is internally complete, then refresh the live Worker index in the background and fetch only report IDs missing from the snapshot. A missing/invalid preload snapshot must fail over to the existing bounded live loader rather than making the Database unusable.
- Direct report/Compare permalinks must not wait for a full live-database refetch. If a requested report is newer than the deploy snapshot, the frontend may fetch only the requested report before route application and then complete the remaining live delta asynchronously.
- Static preload chunks must remain individually below the frontend 4 MiB response ceiling and must be addressed by content-derived filenames so browser/CDN caching cannot confuse new chunk bytes with old bytes. The manifest may be revalidated separately.
- The page must expose a thin fixed top reading-progress indicator whose horizontal scale follows `scrollY / maxScrollableY` continuously in both scroll directions and disappears when the document is not scrollable.
- Page up/down controls must use opacity/transform/visibility transitions rather than abrupt `hidden`/`display:none` state swaps. At the top only Down is eligible, in the middle both are eligible, and at the bottom only Up is eligible. Scroll activity makes eligible controls visible; inactivity fades them away. `prefers-reduced-motion` must disable decorative transition motion while preserving functionality.
- Main filter custom listboxes must use local inline SVG icons that are semantically related to the filter dimension (vendor, GPU, API/loader, driver, extension, device type, Android/device, ABI, application version, submission date, HDR/gamut, resolution, refresh rate and display mode). Native `<select>` controls remain authoritative and accessible.
- The materially changed frontend must use `assets/app.v1003.js` and cache key 1003; stale versioned app JavaScript must not remain in the strict release package. CSS changes may retain `site.v0390.css` only with the release cache key advanced to 1003.
- `tools/verify_1_0_3_ui_preload_compat.py` and `tools/test_1_0_3_preload_builder.py` are mandatory release gates. Existing report/Compare/Surface/Encyclopedia semantics and all historical producer compatibility gates remain regression-protected.
- No D1 migration, stored-report rewrite, report-id rewrite, normalizer bump or evidence-state semantic broadening is permitted for this release. Live Cloudflare Pages/Worker deployment and remote D1 smoke testing remain separate evidence classes and are **NOT EXECUTED** unless actually performed.

## Release 1.0.4 Windows/toolchain/release hardening requirements
- Treat VulkanScope Database 1.0.3 as the immutable predecessor; predecessor ZIP SHA-256 is `bebd99a8823070239e949ca1c1c06f8460519fcbe2e4173ebb56a800b96623a0`.
- Database release identity is 1.0.4. Current producer/query baseline remains VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362. The 0.80.3 new-submission floor, schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashes, preload semantics and stored payload bytes remain unchanged.
- Node ESM tooling that derives a filesystem path from `import.meta.url` must use `fileURLToPath(import.meta.url)`; direct `new URL(import.meta.url).pathname` conversion is forbidden because it can produce invalid doubled-drive Windows paths such as `C:\C:\...`.
- The canonical GitHub workflow must execute the path-sensitive Compare and Surface regression tests on `windows-latest` before the Linux build/deploy job is eligible to proceed. Linux-only success is not sufficient evidence for cross-platform release tooling.
- Worker development/deployment tooling is pinned to Wrangler 4.130.0. Production deploy preflight must run the fail-closed Cloudflare account check and `npm audit --audit-level=high`; high/critical dependency advisories block deploy rather than being waived with `npm audit fix --force`.
- npm install-script execution is explicit policy. Only reviewed Wrangler transitive installers required for the toolchain (`esbuild`, `sharp`, `workerd`) are approved through `package.json` `allowScripts`; blanket install-script approval is forbidden.
- A local `worker/package-lock.json` remains optional workspace state and may be ignored by release packaging, but when present `tools/verify_optional_npm_lock.py` must verify the exact Wrangler 4.130.0 resolution and integrity metadata.
- Tag pushes matching `v*` must create or refresh the GitHub Release from a GitHub-hosted runner after the mandatory build gates pass. Local installation of GitHub CLI is not a release prerequisite; the runner-scoped `github.token` is used with job-level `contents: write` only for the release job.
- Release ZIP generation must be deterministic, strict-tree verified against the immutable predecessor contract, exclude local/generated workspace state, use fixed ZIP timestamps and emit a separate SHA-256 sidecar.
- `tools/verify_1_0_4_windows_toolchain_release.py` and `tools/test_1_0_4_windows_toolchain_release_negative_mutations.py` are mandatory release gates. No D1 migration, stored-report rewrite, report-id/hash rewrite, normalizer bump or capability/evidence semantic broadening is permitted.


## Release 1.0.5 stale optional-lock overlay hardening requirements
- Treat VulkanScope Database 1.0.4 as the immutable predecessor; predecessor ZIP SHA-256 is `26cf7b99a19ab8ec54496a2719ba348f2a4703a70c8fbe3c431ee8856f7c87db`.
- Database release identity is 1.0.5. VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362 remains the current producer/query baseline. Schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashes, preload semantics, stored payload bytes and the 0.80.3 new-submission floor remain unchanged.
- `worker/package-lock.json` is optional local deployment-workspace state and remains ignored/excluded from release packaging. A tracked-but-ignored legacy lock from an older repository overlay must never be allowed to fail a clean successor release after the canonical repository repair step.
- `python tools/repair_repository.py --apply` must remove an existing ignored `worker/package-lock.json` before the release quality gate. This is repository hygiene only; it does not relax dependency verification or change the pinned Worker toolchain.
- `tools/verify_optional_npm_lock.py` must always verify the canonical `worker/package.json` Wrangler pin at exactly 4.130.0. If an optional lock exists outside the repaired release checkout, it must additionally require an exact 4.130.0 root lock pin, exact resolved Wrangler 4.130.0 package and integrity metadata. Stale 4.125.x locks fail closed with an explicit repair instruction.
- The existing-repository overlay regression must reproduce the real tracked-but-ignored stale-lock failure class, prove that the stale lock is rejected before repair, prove `repair_repository.py --apply` removes it, and prove the optional-lock verifier then succeeds without weakening the 4.130.0 pin.
- The canonical GitHub build order must remain repository repair first, optional-lock verification second, and the 1.0.5 stale-lock hardening verifier must run before the full quality gate.
- Browser-visible release identity advances to 1.0.5 / `assets/app.v1005.js` / cache key 1005 only to make the successor artifact identity unambiguous. No UI behavior, capability semantics, Worker API contract, migration, stored-report rewrite, report-id/hash rewrite or normalizer bump is permitted by this hotfix.
- `tools/verify_1_0_5_optional_lock_overlay.py` and `tools/test_1_0_5_optional_lock_overlay_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.4 -> 1.0.5 regression verification, strict-tree verification, clean-extract re-verification, source/extract byte equality and two independently generated byte-identical deterministic ZIPs.

## Release 1.0.6 CI / deterministic Encyclopedia / npm-audit hardening requirements
- Treat VulkanScope Database 1.0.5 as the immutable predecessor; predecessor ZIP SHA-256 is `a91af4ad277936683ef1eb01802fd201934217b78a9dc808d58575aa7c6cc00e` with 246 packaged files.
- Database release identity is 1.0.6. VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362 remains the current producer/query baseline. Schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashes, preload/live-delta semantics, stored payload bytes and the VulkanScope 0.80.3 new-submission floor remain unchanged.
- Historical release verifiers that intentionally validate an older producer baseline must run in retained-compatibility mode on later Database releases. In particular, the canonical workflow must invoke `tools/verify_1_0_2_producer_baseline.py --skip-version`; a later Database release must never be required to masquerade as Database 1.0.2 merely to prove the retained 1.0.2 producer contract.
- Encyclopedia generation is byte-deterministic across Linux and Windows. `tools/generate_encyclopedia_03924.py` must write the generated JavaScript as explicit UTF-8 bytes (or an equivalent explicit LF-only byte stream) so Python newline translation cannot produce Windows-only CRLF drift. The Windows CI prerequisite must execute the Encyclopedia regeneration state-machine test.
- The release keeps `worker/package-lock.json` as ignored local deployment-workspace state, but `npm audit` is never invoked without a lock. `worker/scripts/security-audit.mjs` must create/refresh an ignored lock using `npm install --package-lock-only --ignore-scripts --no-audit --no-fund`, validate the exact toolchain resolutions, and only then run `npm audit --audit-level=high`.
- Wrangler remains pinned exactly to 4.130.0. The known three-high-severity `sharp <0.35.0` transitive advisory class is fixed without `npm audit fix --force`: `worker/package.json` pins the transitive `sharp` resolution through an exact `overrides.sharp` value of 0.35.4, and the audit-lock verifier requires that exact resolved version plus integrity metadata when a lock exists. Blanket overrides are forbidden.
- `predeploy` remains fail-closed: Cloudflare account verification runs first and the lock-backed high-severity audit runs before `wrangler deploy`. Failure to create/validate the audit lock or any remaining high/critical advisory blocks deployment.
- The accidentally documented 1.0.5 command `tools/verify_1_0_5_stale_lock_repair.py` remains as a compatibility wrapper to the canonical retained 1.0.5 optional-lock verifier so operator command history cannot fail with a missing-file error.
- Browser-visible release identity advances to 1.0.6 / `assets/app.v1006.js` / cache key 1006 solely to make the repaired successor artifact unambiguous. No capability/evidence semantic broadening, Worker API schema change, D1 migration, stored-report rewrite, report-ID/hash rewrite or normalizer bump is permitted.
- `tools/verify_1_0_6_ci_audit_hardening.py` and `tools/test_1_0_6_ci_audit_hardening_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.5 -> 1.0.6 source-overlay and strict-tree verification, clean-extract re-verification, source/extract byte equality and two independently generated byte-identical deterministic ZIPs.

## Release 1.0.7 Windows checkout / npm subprocess hardening requirements
- Treat VulkanScope Database 1.0.6 as the immutable predecessor; predecessor ZIP SHA-256 is `a8f22f801d5b745179c4953450e48f5adcb2f59067149a805a84ab28f4391db9` with 253 packaged files.
- Database release identity is 1.0.7. VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362 remains the current producer/query baseline. Schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashes, preload/live-delta semantics, stored payload bytes and the VulkanScope 0.80.3 new-submission floor remain unchanged.
- Canonical Git text checkouts are LF-only on every platform. Root `.gitattributes` must enforce `* text=auto eol=lf` while explicitly excluding binary artwork from text conversion. The checked-in `assets/encyclopedia.v03924.js` fixture must contain no CR bytes, so a Windows checkout cannot differ byte-for-byte from the deterministic generator solely because of `core.autocrlf`.
- The Encyclopedia state-machine remains a byte-equality gate rather than normalizing CRLF away. It must diagnose a CRLF checkout fixture explicitly; generated output remains explicit UTF-8/LF bytes. A line-ending policy failure is fixed at checkout policy, not hidden by weakening the byte comparison.
- Worker security-audit tooling must never spawn `npm.cmd` directly. When invoked by an npm script it resolves `process.env.npm_execpath` and executes the npm CLI JavaScript entry point through the current `process.execPath`, preserving argument boundaries without Windows shell/cmd wrappers. A verified adjacent npm CLI path may be used only as a direct-node fallback.
- `worker/scripts/security-audit.mjs` reuses an existing lock when present. Only when the ignored workspace lock is absent may it bootstrap one with `npm install --package-lock-only --ignore-scripts --no-audit --no-fund`; it then validates exact Wrangler 4.130.0 and sharp 0.35.4 resolutions plus integrity metadata before `npm audit --audit-level=high`.
- Windows CI must execute the real failure class before Linux build/deploy can proceed: setup-node v7 with Node 24, `npm install` in `worker`, and `npm run security:audit` in `worker`. The Linux build job also pins Node 24 with setup-node v7 so Node-dependent quality gates do not depend on mutable runner defaults. Automatic package-manager caching remains disabled because the canonical source release intentionally does not ship the local deployment lock.
- Wrangler remains exactly 4.130.0, sharp remains exactly overridden to 0.35.4, reviewed install-script allow-listing remains limited to `esbuild`, `sharp`, and `workerd`, and `npm audit fix --force` remains forbidden. Cloudflare account verification still runs before the security audit in production predeploy.
- Browser-visible release identity advances to 1.0.7 / `assets/app.v1007.js` / cache key 1007 solely to make the corrected successor artifact unambiguous. No UI capability semantics, Worker API schema, D1 migration, stored-report rewrite, report-ID/hash rewrite or normalizer bump is permitted.
- `tools/verify_1_0_7_windows_checkout_security.py`, `tools/test_1_0_7_windows_checkout_security_negative_mutations.py`, and `tools/test_1_0_7_security_audit_runner.mjs` are mandatory release gates. Final packaging requires immutable 1.0.6 -> 1.0.7 source-overlay and strict-tree verification, clean-extract re-verification, source/extract byte equality and two independently generated byte-identical deterministic ZIPs.

## Release 1.0.8 tagged-checkout / strict-package boundary requirements
- Treat VulkanScope Database 1.0.7 as the immutable predecessor; predecessor ZIP SHA-256 is `414da835326c3414215d598c8cf12b8391bfa112a5f02eff4031a788bfdf1b67` with 260 packaged files.
- Database release identity is 1.0.8. VulkanScope 1.0.15 / versionCode 1015 / Vulkan 1.4.362 remains the current producer/query baseline. Schema 2, technicalReport schema 3, normalizer 16, D1 schema, report hashes, preload/live-delta semantics, stored payload bytes and the VulkanScope 0.80.3 new-submission floor remain unchanged.
- A long-lived or tagged Git checkout is a source-overlay, not the distributable release package. Historical tracked files that were not present in the immutable predecessor release ZIP may exist in that checkout and must not cause source-overlay regression failure merely because they are outside the package allow-list.
- `--strict-tree` is reserved for the actual constructed release tree/ZIP. The GitHub tag release job must not run strict-tree verification directly against the history-bearing tagged checkout before package construction.
- `tools/package_release.py` must first run source-overlay predecessor verification, derive the exact release path set from the predecessor manifest plus explicit successor additions/removals, exclude local deployment state such as `worker/package-lock.json`, and construct the ZIP only from that path set. Historical checkout extras such as stale `assets/site.v*.css` or unversioned historical `assets/app.js` must never leak into the ZIP.
- The deterministic packager must clean-extract its own ZIP, require an exact path-set match, compare every extracted file byte-for-byte with the selected source path, then run `tools/verify_regression_contract.py --strict-tree` and the source audit from inside that clean extract. Strict-package verification is therefore evidence about the package, not about unrelated repository history.
- The release-boundary regression must reproduce a history-bearing tagged checkout containing `assets/site.v0380.css`, `assets/site.v0357.css` and `assets/app.js`, prove source-overlay verification accepts the unrelated history, prove the deterministic ZIP excludes those files, prove the clean extract passes strict-tree, and prove an injected extra file makes strict-tree fail closed.
- Browser-visible release identity advances to 1.0.8 / `assets/app.v1008.js` / cache key 1008 only to make the corrected successor artifact unambiguous. No UI capability/evidence semantic change, Worker API/schema change, D1 migration, stored-report rewrite, report-ID/hash rewrite, normalizer bump or producer contract broadening is permitted.
- `tools/verify_1_0_8_release_checkout_packaging.py`, `tools/test_1_0_8_release_checkout_packaging_negative_mutations.py`, and `tools/test_1_0_8_release_checkout_packaging.py` are mandatory release gates. Final packaging still requires immutable 1.0.7 -> 1.0.8 source-overlay verification, deterministic ZIP construction, clean-extract strict-tree verification, clean-extract/source byte equality and two independently generated byte-identical ZIPs.

## Release 1.0.9 live-report consistency / Reports identity / low-coverage / producer-floor requirements
- Database version is 1.0.9. Treat VulkanScope Database 1.0.8 (SHA-256 `aa406aafdf52bfa8e008e093e3518f4313ffd8ff4efaac1722ce923fe6f1744c`) as the immutable predecessor.
- Current producer/query baseline is VulkanScope 1.0.19 / versionCode 1019 / Vulkan 1.4.362. New report POSTs from every VulkanScope version lower than 1.0.19 are rejected before generic submission validation. Historical stored reports remain readable and are never deleted, rewritten, renormalized in storage or rehashed because of this floor change.
- A deploy-time preload is only a temporary local source. Before the initial database UI is committed, the frontend must fetch the complete live report index and stage any missing compact payloads. The visible `state.reports` Map and `state.index` are replaced only after the entire live set has loaded successfully; a failed live delta must not partially mutate Reports or statistics.
- Reports, header metrics and all statistics/coverage views must derive from the same atomically committed report Map. The loading copy must expose the live index count once known, so a stale snapshot count is not presented as the final count.
- Live reconciliation must not insert a report row after the initial UI is already committed through a second entrance transition. Initial rendering happens once after live reconciliation (or once from the complete snapshot when live service is unavailable).
- Report submission time remains server authoritative from D1 `submitted_at`. In Reports it is presented as separate Date, Time and Time zone lines. Browser-local formatting is allowed; inventing a client submission timestamp is forbidden.
- The Reports Report ID column must not truncate the 64-character SHA-256 identifier. It exposes a dedicated copy control that copies the exact full 64-character ID; activating the control must not activate row navigation.
- Coverage percentages below 20% use a structurally distinct, patterned/angled progress fill while preserving the semantic state color. Values below 5% receive the stronger very-low shape. The visual must not alter the numerical percentage.
- Browser-visible JavaScript/CSS changes are cache-busted with key `1009`; current application JavaScript is `assets/app.v1009.js`. No D1 migration, report hash rewrite, submission-schema bump, technicalReport-schema bump, normalizer bump or Vulkan registry corpus change is introduced.

## Release 1.0.10 Reports render-array hotfix requirements
- Database version is 1.0.10. Treat VulkanScope Database 1.0.9 (SHA-256 `0d75cfe949901636635a1039ac53921834fe690be7099ad96d45ef14a4ee6517`) as the immutable predecessor.
- The shared `table(heads, rows)` helper owns HTML row serialization and therefore receives an Array of row strings. Callers must not pre-serialize a row Array with `.join('')` before passing it to `table()`.
- `renderReports()` must keep `rs.map(...)` as an Array through the `table(..., rows)` call. The Database-unavailable fallback must never be triggered by a local `rows.join is not a function` presentation bug.
- Do not make `table()` permissive to strings merely to hide this failure class; the caller type contract remains explicit and regression-tested.
- All Database 1.0.9 behavior remains mandatory: atomic live-index/payload reconciliation before first committed render, one committed report Map for Reports/statistics, server-authoritative stacked Date/Time/Time zone, full 64-character Report ID copy control, low/very-low coverage shapes, and the VulkanScope 1.0.19 new-submission floor.
- Browser-visible JavaScript advances to `assets/app.v1010.js` with cache key 1010. Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362, D1 schema, stored report bytes/hashes, payload chunking, privacy rules and transport limits remain unchanged.
- `tools/verify_1_0_10_reports_render_hotfix.py` and `tools/test_1_0_10_reports_render_hotfix_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.9 -> 1.0.10 source-overlay verification, clean-extract strict-tree verification and deterministic ZIP construction.

## Release 1.0.11 GitHub Actions command-boundary hotfix requirements
- Database version is 1.0.11. Treat VulkanScope Database 1.0.10 (SHA-256 `6d18b5378235a5ff556576ff9d2d91ff5608dcea8076bc17ef0f32d12ec08aa4`) as the immutable predecessor.
- The GitHub Pages `Reverify generated index metadata` step must execute the current verifier and its negative-mutation test as two distinct shell commands. A second command must never be indented as a continuation argument of a scalar `run:` command.
- Multi-command GitHub Actions steps use an explicit YAML block scalar (`run: |`) with each command on its own line. The canonical `tools/pages.workflow.yml` and deployed `.github/workflows/pages.yml` remain byte-identical.
- Release verification must reproduce the exact 1.0.10 CI failure class by mutating both workflow copies back to the malformed scalar form and must reject that fixture for the command-boundary reason.
- The 1.0.10 Reports row-array contract and every retained 1.0.9 behavior remain mandatory: atomic live reconciliation, one committed report Map for Reports/statistics, server-authoritative Date/Time/Time zone, full 64-character Report ID copy, low/very-low coverage shapes and the VulkanScope 1.0.19 new-submission floor.
- Browser-visible release identity advances to 1.0.11 / `assets/app.v1011.js` / cache key 1011 so the corrected release is unambiguous. This is metadata/cache identity only; report parsing, evidence semantics and UI behavior are otherwise unchanged.
- Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362, D1 schema, stored report bytes/hashes, payload chunking, privacy rules, CORS policy and transport limits remain unchanged. No migration or stored-report rewrite is introduced.
- `tools/verify_1_0_11_workflow_command_boundary.py` and `tools/test_1_0_11_workflow_command_boundary_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.10 -> 1.0.11 source-overlay verification, clean-extract strict-tree verification, clean-extract workflow re-verification and deterministic ZIP construction.


## Release 1.0.12 Reports visual/navigation UI requirements
- Database version is 1.0.12. Treat VulkanScope Database 1.0.11 (SHA-256 `cda8c117ae023f9d7abfdb532ad410f053a0a44140c4107bb7cfd78a957cf0dc`) as the immutable predecessor.
- Coverage values below 20% must preserve their exact geometric percentage width and semantic state color. Their distinct low-percentage treatment is a thick black diagonal hatch only; no wedge, triangular `clip-path`, angled endpoint, white hatch or artificial minimum width may distort the represented ratio. The below-5% class may remain for regression/state distinction but obeys the same non-deforming hatch geometry.
- Coverage values at or above 80% use a bounded glow/highlight treatment without changing the semantic state color, percentage width, count or denominator. Decorative shine respects `prefers-reduced-motion` and must not become evidence semantics.
- Reports submission Date, Time and Time zone remain derived exclusively from server-side D1 `submitted_at`. Their row detail is collapsed by default on a fresh page load and controlled by an accessible three-horizontal-line button located in the Submitted table header. Expanding it reveals all three stacked values with sufficient width/wrapping; hiding it must not change sort/filter/pagination state.
- Report-detail navigation must preserve source-page reading position. Opening a report records the exact vertical position for the source route; browser Back and the in-page Back control restore that route position after rendering. Report detail may start at the top. Manual history scroll restoration is allowed to avoid conflict with application route restoration.
- The Android-version filter must use the exact current VulkanScope application `ic_android.xml` head vector artwork (152×89 geometry and its current application fill colors) as local inline SVG presentation. The native select remains the authoritative filter state and no remote visual dependency is introduced.
- 1.0.11 workflow command-boundary repair, 1.0.10 Reports row-array contract and all 1.0.9 live consistency/report-ID/submission-time/1.0.19-floor behavior remain mandatory.
- Browser-visible release identity advances to 1.0.12 / `assets/app.v1012.js` / cache key 1012. The modified stylesheet may retain `assets/site.v0390.css` only with cache key 1012 so browsers/Pages cannot silently retain the previous CSS.
- Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362, D1 schema/migrations, stored payload bytes/hashes, report IDs, privacy/security controls and transport bounds remain unchanged. No migration or stored-report rewrite is introduced.
- `tools/verify_1_0_12_reports_visual_navigation_ui.py` and `tools/test_1_0_12_reports_visual_navigation_ui_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.11 -> 1.0.12 source-overlay verification, clean-extract strict-tree verification, retained 1.0.11 workflow re-verification and deterministic ZIP construction.

## Release 1.0.13 Reports disclosure animation / high-coverage color-spray requirements
- Database version is 1.0.13. Treat VulkanScope Database 1.0.12 (SHA-256 `5584a3bf4ae4103b23ce7100915b51abb6d7b307ada5b52599fca41cf50a4078`) as the immutable predecessor.
- Reports submission Date / Time / Time zone remain server-authored from D1 `submitted_at` and remain collapsed by default. The collapsed state must consume only the compact disclosure-control width; it must not reserve the expanded timestamp column as blank table space.
- Expanding/collapsing Submitted metadata uses a bounded width/height/opacity/transform transition driven by the existing three-horizontal-line header control. Expansion must grow the table horizontally rather than squeezing Device/Driver/Type content into overlaps. Reduced-motion users receive an immediate state change.
- The Reports table may become horizontally scrollable while Submitted metadata is expanded. Existing table-scroller controls must be recalculated after the disclosure transition so thumb size/position represents the new width.
- Coverage values at or above 80% retain exact semantic color and percentage geometry but replace the white sweep highlight with a state-colored spray/burst emitted from the fill endpoint plus a bounded same-color halo. The spray is decorative only and respects `prefers-reduced-motion`.
- Values below 20% retain the 1.0.12 thick black diagonal hatch with no wedge, minimum-width distortion or white texture. Android filter artwork and report scroll-position restoration from 1.0.12 remain mandatory.
- 1.0.11 workflow command-boundary repair, 1.0.10 Reports row-array contract, and 1.0.9 atomic live reconciliation/full Report ID/server timestamp/VulkanScope 1.0.19 POST floor remain mandatory.
- Browser-visible JavaScript advances to `assets/app.v1013.js`; stylesheet/config cache key advances to 1013. Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362, D1 schema/migrations, stored payload bytes/hashes, report IDs, privacy/security controls and transport bounds remain unchanged.
- `tools/verify_1_0_13_reports_disclosure_spray_ui.py` and `tools/test_1_0_13_reports_disclosure_spray_ui_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.12 -> 1.0.13 source-overlay verification, clean-extract strict-tree verification and deterministic ZIP construction.


## Release 1.0.14 Reports/data-presentation integrity requirements
- Database version is 1.0.14. Treat VulkanScope Database 1.0.13 (SHA-256 `dfb27ff28e6c8c5638a84b10d107f69925768f6c0ca5e1e4cb963463f1cd414c`) as the immutable predecessor.
- Reports Submitted disclosure keeps its three-line control pinned at the left edge in both states. The `Submitted` label expands only to the control's right; revealing Date / Time / Time zone grows table width and must not move the control or squeeze Vendor/Type/technical columns into overlap.
- Where a row presents multiple mutually compared percentage states, every represented state participates in both the denominator and dominance decision, including `Unknown` and `Not applicable`. Every state tied for the highest percentage may retain a solid fill; every strictly lower peer uses the black diagonal hatch. Zero is rendered numerically as `0.0%` when a nonzero denominator exists and retains a visible empty state-tinted hatched track.
- Coverage-report launchers open a centered, focusable dialog over a dark backdrop and use a right-pointing arrow. The report list must not expand inline through the generic downward-chevron value accordion. Escape/backdrop close and reduced-motion behavior remain supported.
- High coverage remains >=80%. Halo/spray is anchored at a clamped bar endpoint and must remain visible at 100%, preserve the semantic state color, not change percentage geometry, and honor `prefers-reduced-motion`.
- GPU vendor IDs are evidence, not inferred metadata. Raw browser/Database identity comes only from submitted `gpu.vendorId`, which VulkanScope collects from `VkPhysicalDeviceProperties.vendorID`; text/name/vendor-family fields may never fabricate or replace the ID. Human-readable vendor/family mappings are presentation-only and raw unknown IDs stay visible.
- Developer Info under the GitHub repository control identifies `Semih Boran` with nickname `EFI Shell`.
- HDR10+ uses the exact audited local VulkanScope application artwork with no invert/hue/filter mutation. Browser-visible asset changes are cache-busted.
- Resolution and gamut tokens use the same proportional display-value typography; preferred wide-gamut values are boxed tokens in aggregate and report detail views.
- Browser-visible JavaScript advances to `assets/app.v1014.js`; stylesheet/config cache key advances to 1014. Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362, D1 schema/migrations, stored payload bytes/hashes, report IDs, privacy/security controls and transport bounds remain unchanged.
- `tools/verify_1_0_14_database_ui_integrity.py` and `tools/test_1_0_14_database_ui_integrity_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.13 -> 1.0.14 source-overlay verification, deterministic construction and clean-extract strict-tree verification.


## Release 1.0.15 Versions/motion/loading/overlay requirements
- Reports Vendor and Type columns use the same fixed-left three-horizontal-line disclosure pattern as Submitted. Their toggle position never shifts; the label/value area expands only to the right and the table grows horizontally instead of squeezing technical text.
- The Versions view uses circular share statistics for the selected Device API, Loader/instance API, or device+loader pair distribution. Version percentage bars are forbidden in this view.
- Versions report counts and shares are repeated below the circular statistics in a separate ordinary table so precise counts remain readable and accessible without interpreting a chart.
- Search fields use one local clear-control implementation rather than inconsistent browser-native cancel glyphs. Global search, Encyclopedia search and row-filter searches share the same soft hover/press/focus motion and keyboard semantics.
- Buttons, cards and interactive controls use the shared short Material-consistent transition language. `prefers-reduced-motion` removes decorative motion without removing state changes or focus visibility.
- Every route/filter/search render emits a short loading accent on the major box families (metrics, cards, tables, key/value boxes and section panels). This is a presentation-only loading cue and never changes report data, state or support semantics.
- History-bearing repository overlays are repaired before canonical-state verification in every CI job that can be the first gate. `tools/repair_repository.py --apply` removes stale versioned frontend app assets before `--check`; the release package itself still contains exactly one current `assets/app.v1015.js`.
- VulkanScope producer/query baseline, schema 2 / technical report 3, normalizer 16, D1 schema, raw vendor-ID provenance, coverage-state semantics, HDR10+ audited artwork and 1.0.14 report/disclosure/modal/display behavior remain unchanged.


## Release 1.0.16 live synchronization / modal-details / Versions parity
- Reports Submitted, Vendor and Type disclosure controls stay physically pinned to the left edge while expanded content grows only to the right; expanded widths must fit complete date/time/time-zone, vendor/family/raw-ID and canonical physical-device-type text without clipping.
- Aggregate Distinct/value drill-down controls use the same centered modal, dark backdrop, right-arrow trigger and keyboard/focus semantics as Coverage reports; inline downward accordion disclosure is not used for these table drill-downs.
- Versions uses the same local circular/donut visual language as Statistics. Cohorts are GPU + selected Vulkan-version dimension, while exact GPU/version/report counts remain in a separate table below and no coverage bar is used.
- The frontend live-sync loop polls the bounded report index at a conservative interval and on focus/visibility/online recovery, fetches only missing report payloads, swaps the report set atomically, and re-renders all affected metrics/views when the live report universe changes.
- The live API exposes a databaseVersion release identity. A frontend seeing a newer databaseVersion must show a centered dark-backdrop update dialog with an explicit Refresh now action; it must not silently hot-swap frontend code.
- Live synchronization must preserve supported/unsupported/unavailable/unknown evidence semantics, active filters where still valid, current route/detail state and bounded fetch limits.
- Release assets that change JavaScript behavior are cache-busted as app.v1016.js / ?v=1016. No D1 migration or stored-report rewrite is required for 1.0.16.

## Release 1.0.17 coverage-glow / Versions identity / paged evidence-modal parity
- Every coverage state at or above 80% uses the same reliable semantic high-coverage glow/spray treatment in every aggregate view. Endpoint placement must use a valid percentage-valued custom property anchored to a positioned coverage bar; invalid CSS arithmetic must never silently drop the endpoint effect, including at exactly 100%.
- The high-coverage treatment remains semantic-state colored for Supported, Unsupported, Available, Unavailable, Not applicable and Unknown; subordinate hatching/dominance rules and zero-percent hatched-empty tracks remain authoritative and unchanged.
- Versions keeps the Statistics-style circular GPU/version visualization and the separate non-coverage report-count table. Each report-count row now uses the same GPU identity language as Devices: bold GPU name, vendor logo and canonical vendor/family/raw-ID text before the selected Vulkan version dimension and report/share values.
- Coverage Reports and Distinct/value centered modals use one bounded paged-list primitive. Visible modal rows are capped at 50 per page, the user may select 10 / 25 / 50 rows per page, Previous/Next navigation exposes explicit page/range text, and the list itself has a visible vertical scrollbar rather than growing the dialog without bound.
- Modal pagination is presentation-only: report/value ordering, counts, report IDs, evidence states and click-through behavior are preserved exactly; hidden rows remain available on later pages and are never discarded.
- Live synchronization, database-version refresh notification, fixed-left Submitted/Vendor/Type disclosures, Developer Info, vendor-ID provenance, HDR10+ audited artwork, Display/HDR chips, soft interactions, loading accents, reduced-motion semantics, schema 2 / technical report 3, normalizer 16 and D1 storage schema remain unchanged.
- Release assets that change frontend behavior are cache-busted as `assets/app.v1017.js` / `?v=1017`. No D1 migration or stored-report rewrite is required for 1.0.17.

## Release 1.0.18 bounded modal selector / Android brand color / Devices statistics / full audit
- Treat VulkanScope Database 1.0.17 as the immutable predecessor. Its release ZIP SHA-256 is `4279690b5c88d14747528b9a4e4d14eca695d3946743d9af66a5f51ada4264e2`; schema 2, technicalReport schema 3, normalizer 16, report IDs/hashes, D1 schema/migrations and stored report payload semantics remain unchanged.
- Paged Coverage Reports and Distinct/value dialogs retain 10 / 25 / 50 choices and the hard 50-row maximum. Their custom page-size selector must be compact and fully usable inside the bounded dialog: the option menu opens upward from the footer control, remains above the dialog footer rather than being clipped below it, keeps keyboard/focus semantics, and does not expand the dialog beyond its viewport bound.
- Android robot artwork used by Database controls must preserve the Android online brand green `#3DDC84` with neutral dark detail `#202124`; generic Vulkan red tinting or CSS fill overrides are forbidden. The artwork remains decorative/filter-identification UI and does not alter Android-version evidence.
- Devices uses the same Statistics/Versions circular visualization language. Each circular slice represents a GPU-model report cohort from the currently loaded/filtered report set; it is explicitly report distribution, not installed-base, market-share or hardware-popularity evidence.
- Devices must also provide a separate exact-count table below the circular chart. Rows show bold GPU identity, vendor logo, canonical vendor/family/raw-ID presentation, maximum Vulkan device API, driver-variant count, report count and textual share; the table is authoritative for exact values and does not use coverage bars.
- Raw GPU vendor IDs remain submitted evidence only. Human-readable vendor/family and logo mappings are presentation metadata and must never fabricate, replace or reinterpret an unknown raw vendor ID.
- The release baseline remains VulkanScope 1.0.19 / Vulkan 1.4.362 with the locked Vulkan registry/header baseline 1.4.362 / header 362 published 2026-09-04. Registry presence remains reference metadata rather than runtime-support evidence.
- `tools/audit_database.py` is a current-release gate, not a stale historical checker. It must audit the source tree and staged Pages artifact for release/cache identity, canonical workflow, local-resource integrity, one current versioned frontend asset, Python/JavaScript syntax, Worker contract tests, route/compare tests where available, D1 migration replay, security/resource ceilings, schema/normalizer locks and package/deploy allow-lists.
- Browser/Worker security remains fail-closed: strict CSP, no dynamic `eval`/`new Function`/`document.write`, bounded 2 MiB request ingestion, fatal UTF-8 decoding, sensitive-key rejection, canonical stable serialization, restricted Worker/D1 bindings and high-severity npm audit before deploy. Wrangler remains pinned at 4.130.0 and the reviewed sharp override remains 0.35.4 unless a later audited release changes them.
- Full auditing must not silently claim remote deployment evidence. Local/source/package tests prove only the checked source and deterministic release artifact; live Cloudflare Worker/Pages and remote D1 state remain separate operational evidence unless actually exercised.
- Frontend behavior changes are cache-busted as `assets/app.v1018.js` / `?v=1018`. All 1.0.17 live synchronization, database-version refresh notice, glow/hatching semantics, fixed-left Submitted/Vendor/Type disclosures, modal paging, Developer Info, HDR10+ artwork, display chips, motion/reduced-motion behavior and loading accents remain regression-protected.
- `tools/verify_1_0_18_full_audit.py`, `tools/test_1_0_18_full_audit_negative_mutations.py`, source audit and staged-artifact audit are mandatory release gates. Final packaging requires immutable 1.0.17 -> 1.0.18 regression verification, exact clean-extract byte equality, strict package-tree verification and deterministic ZIP construction.


## Release 1.0.19 dominant coverage glow / release-ready publication requirements
- Database version is 1.0.19. Treat VulkanScope Database 1.0.18 (SHA-256 `0b2bc5175a285ff15db76cd2bbfdb70d707251f19045e2d8cfa72cbf840bdbbf`) as the immutable predecessor.
- Coverage glow is a relative winner semantic, not an arbitrary absolute percentage threshold. For every grouped coverage row across Properties, Limits, Features, Formats, Memory, Queues, Surface, Extensions, Instance, Profiles, Portability and any shared grouped-coverage renderer, every non-zero state tied for the greatest count receives the glow/spray treatment. Every strictly smaller non-zero state remains subordinate/hatched. All-zero states remain zero/empty and never glow. Numeric widths, counts and denominators are unchanged.
- The frontend must never use the Worker API `databaseVersion` as the signal that a new frontend is safe to refresh into. Worker deployment may precede GitHub Pages publication and therefore is not release-ready evidence.
- Frontend update availability is signalled only by the same-origin `data/release.json` marker from the currently published Pages artifact, fetched with `cache: no-store`. The marker contains the released Database version and `releaseReady: true`. A newer marker may show the centered refresh dialog; an equal/older marker must not.
- Refresh from the update dialog uses a cache-busting same-origin navigation, so an already-published successor cannot loop on a stale HTML document. The frontend never hot-swaps JavaScript in place.
- Main-branch pushes run all verification/build gates but do not publish GitHub Pages. Tag pushes first pass build gates and create/refresh the GitHub Release; only after the release job succeeds may the Pages deployment publish the staged artifact and new release marker. A failed release therefore cannot advertise an unreleased frontend.
- Live D1 report synchronization remains independent and immediate: new/removed reports continue to reconcile atomically from the Worker without F5 and without waiting for a frontend release.
- Browser-visible JavaScript advances to `assets/app.v1019.js` / cache key 1019. Schema 2, technicalReport 3, normalizer 16, Vulkan 1.4.362/header 362, D1 schema, report hashes/payload bytes, vendor-ID provenance, HDR/display assets, modal paging, Devices/Versions statistics and all security/resource ceilings remain unchanged.
- `tools/verify_1_0_19_dominant_glow_release_ready.py` and `tools/test_1_0_19_dominant_glow_release_ready_negative_mutations.py` are mandatory release gates. Final packaging requires immutable 1.0.18 -> 1.0.19 source-overlay verification, clean-extract strict-tree verification and deterministic byte-identical ZIP construction.

## Release 1.0.20 release-state handshake / refresh-loop recovery / Pages publication repair
- Database release identity is 1.0.20. VulkanScope Database 1.0.19 (ZIP SHA-256 `0e0f6ab19e86e685f0bb57188d221db702e1d9611112703d251c3498195d3728`) is the immutable predecessor. Report payload bytes/hashes, D1 schema and migrations, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and producer minimum VulkanScope 1.0.19 remain unchanged.
- Worker metadata must never be a browser frontend-refresh signal. The legacy public `databaseVersion` field is removed from `/v1/health` and `/v1/reports` because Database 1.0.18 clients interpreted it as a safe-to-refresh frontend version and can loop when Worker deployment precedes Pages. Worker identity is instead exposed as `databaseReleaseVersion` / `workerReleaseVersion`, plus `frontendUpdateSignal: same-origin-pages-marker`; these fields are informational only and the frontend must not treat them as publication readiness.
- The source-tree `data/release.json` is deliberately non-ready (`releaseReady:false`). `releaseReady:true` may exist only in the staged GitHub Pages artifact after the GitHub Release job has succeeded. `tools/mark_release_ready.py` is the sole canonical transition for this marker and may operate only on the staged artifact, never the source tree.
- The release marker schema is 2 and binds `databaseVersion`, `appAsset` and `cacheKey`. Before offering Refresh, the browser must fetch the same-origin marker with `cache:no-store`, verify a newer semantic version, then independently no-store probe both the published `index.html` and the marker-named application asset. The HTML must reference the exact app asset/cache key and the fetched JS must identify the same Database version. A marker alone is insufficient readiness evidence.
- Refresh is explicit user action only. A refresh attempt is recorded in sessionStorage and an old frontend that is still served after the attempt suppresses the same-version prompt for a bounded retry window. Successful loading of the target/newer frontend clears the attempt and removes release/cache-bust query parameters. This prevents repeated modal/refresh loops during CDN propagation or partial publication failure.
- Production Pages deployment must originate from the default `main` branch workflow, not a tag-ref workflow. The validated main workflow creates the `v1.0.20` GitHub Release/tag from the validated commit first; only after that job succeeds may the deploy job rebuild the Pages artifact, build the preload snapshot, transition only the staged marker to release-ready, audit that exact ready artifact, upload it, and deploy it.
- If `v1.0.20` already exists on a workflow rerun, its resolved tag commit must equal the validated `GITHUB_SHA`; otherwise the release job fails closed and requires a version bump rather than retargeting an already-published version.
- Pages artifact upload/deploy pins follow GitHub's documented production workflow: `actions/configure-pages@v5`, `actions/upload-pages-artifact@v4`, and `actions/deploy-pages@v4`. The deploy job owns both artifact upload and deployment so it cannot consume an ambiguous or stale cross-job Pages artifact.
- If Release or deploy fails, the currently published Pages marker remains the previous version and no frontend update prompt may be generated. Live report synchronization from D1/Worker continues independently and does not require a frontend refresh.
- Current frontend asset is `assets/app.v1020.js` with cache key 1020. All 1.0.19 dominant-coverage glow semantics, modal pagination, Devices/Versions statistics, fixed Submitted/Vendor/Type disclosures, Android/HDR assets, vendor provenance, soft interaction/loading behavior and security/resource ceilings remain regression-protected.
- `tools/verify_1_0_20_release_state_handshake.py`, its negative mutation suite, source audit, non-ready staged-artifact audit, staged-ready transition audit, Worker contract test, regression contract and deterministic strict-package build are mandatory gates.

## Release 1.0.21 foreground live-report reconciliation / sync-head requirements
- Database release identity is 1.0.21. VulkanScope Database 1.0.20 (ZIP SHA-256 `b5dbe8ed37bc8ce7ec4ccc1a236bd7b2a7007911cdb58c79b56108e60cd75aed`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and the VulkanScope 1.0.19 submission floor remain unchanged.
- A visible, online Database page must detect newly committed D1 reports without F5. The foreground freshness target is bounded to roughly three seconds rather than the previous ten-second full-index poll. Browser timer throttling while hidden is acceptable; returning via visibility/focus/pageshow or reconnecting online must force an immediate full reconciliation.
- Frequent foreground checks use the Worker `GET /v1/sync` head endpoint rather than downloading the complete report index on every interval. The endpoint is GET-only, inherits the Worker `Cache-Control: no-store` policy, and returns only `reportCount`, `latestReportId`, `latestSubmittedAt` and a deterministic `syncToken` derived from those fields plus informational Worker release identity.
- `/v1/sync` obtains its head atomically from one D1 query using the latest `(submitted_at, id)` row and `COUNT(*) OVER()`. An empty database reports count 0 with empty latest fields. This endpoint is freshness metadata only; it never fabricates a report and never changes report hashing/deduplication semantics.
- Frontend sync-head requests and subsequent live-index/report-payload requests use `cache:no-store` plus a nonce query parameter so browser/CDN/intermediary caches cannot make a newly committed report invisible to foreground polling.
- The frontend retains the last committed sync token. If the head token is unchanged, no full index scan or payload download occurs. If the token changes, the frontend fetches the complete cursor-safe index, downloads only missing report payloads, verifies a complete one-to-one ID set, swaps `state.reports` atomically, updates `state.index`, reinitializes filters and re-renders the active view/metrics in the same foreground session.
- A failed head/index/payload refresh must not partially mutate the committed report Map or advance the committed sync token. Failure is retried by the next foreground tick; focus/visibility/pageshow/online reconciliation bypasses the token shortcut and performs a full consistency pass.
- Exact duplicate canonical submissions remain intentionally deduplicated by report ID. A duplicate whose canonical payload hash already exists does not increase reportCount and therefore does not produce a new row/statistical sample; this is not a live-sync failure.
- Frontend release-ready/refresh signaling remains independent from report-data freshness and continues to use only the same-origin Pages release handshake from 1.0.20.
- Current frontend asset is `assets/app.v1021.js` with cache key 1021. `tools/verify_1_0_21_live_report_sync.py`, its negative-mutation suite, Worker transport contract, source audit, regression contract, Pages artifact audit and deterministic strict-package build are mandatory gates.

## Release 1.0.22 atomic current-set / local preferences / network-observation / selection-safety requirements
- Database release identity is 1.0.22. VulkanScope Database 1.0.21 (ZIP SHA-256 `0f1619965c0b3bb6eaf2f81a17c4436312c2d71801c82484a75b1d0f23eb9aa1`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and the VulkanScope 1.0.19 submission floor remain unchanged.
- The deploy-time preload snapshot is staging input only. Snapshot chunks must be hydrated into a private Map and must never incrementally mutate the visible `state.reports` Map. Initial online loading first obtains the authoritative cursor-safe live index, reuses matching staged snapshot reports, fetches every missing report, verifies exact ID-set cardinality, and commits `state.reports` plus `state.index` only after the complete current set exists. If live indexing is unavailable, the complete validated preload snapshot may be committed atomically as an offline fallback.
- Loading UI describes one current Database set (`X of N reports ready`). It must not model the public state as “snapshot reports + N”, “new payload +1”, or otherwise expose implementation-delta payloads as an additive report set. Foreground live synchronization may continue downloading only missing payloads internally for efficiency, but every successful refresh still commits one complete verified Map atomically.
- Report favorites are a browser-local UI preference keyed only by validated 64-hex report IDs. Favorites must never be written to D1, embedded into report payloads, alter report hashes, or change report/statistical semantics. Report rows and report detail provide explicit Favorite controls; the Settings drawer provides the saved Favorites list.
- A Settings button appears immediately to the left of the VulkanScope brand and opens a left-side sliding drawer. The drawer contains Favorites, current internet/connection information, a live local clock/time-zone display, and independent persistent defaults for expanding Submitted date/time/time-zone, GPU Vendor, and GPU Type columns.
- UI preferences/favorites use bounded, validated same-origin browser local storage only. Corrupt or unavailable storage must fail soft to safe defaults; no preference is required for report viewing.
- `GET /v1/network-info` is a no-store, GET-only, non-persistent introspection endpoint for the current requester. It may echo the request transport IP that Cloudflare observed plus allow-listed `request.cf` transport/geography metadata. It must not query or mutate D1, attach network data to a report, become report identity, or expose arbitrary request headers/cookies.
- IPv4/IPv6 display is observational only. The frontend/Worker must never invent the unobserved second address family. When Cloudflare exposes `CF-Connecting-IPv6` due to Pseudo IPv4 behavior, that value is treated as the real IPv6 client address and any pseudo IPv4 is labelled separately. The client DNS resolver is not observable from an ordinary HTTP request and must be shown as not observable rather than guessed from Worker/runtime DNS behavior.
- Drag-selecting report text with the primary mouse/pointer must not activate the row/report navigation when the button is released. A selection/drag gesture suppresses only the ensuing report-navigation click; ordinary click navigation and explicit buttons/links continue to work.
- Current frontend asset is `assets/app.v1022.js` with cache key 1022. `tools/verify_1_0_22_live_report_sync.py`, its negative-mutation suite, Worker transport contract, source audit, predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.

## Release 1.0.23 live connectivity / Settings privacy / report interaction requirements
- Database release identity is 1.0.23. VulkanScope Database 1.0.22 (ZIP SHA-256 `2779cbc0fd42e1114727fbd95dc2fb3145a24234f2f1a01e65197f2ba36e3811`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and the VulkanScope 1.0.19 submission floor remain unchanged.
- Browser connectivity changes are first-class live state. `offline` must immediately expose a persistent top status surface; `online` must immediately start a Database reachability check plus live-report/release reconciliation. A successful recovery shows a restored status for exactly three seconds before the status surface smoothly collapses. The surface participates in document layout and must not obscure the top bar/content.
- Navigator online/offline state is not treated as proof that the Worker API is reachable. Repeated live-sync failure or an explicit failed network-info refresh while `navigator.onLine` remains true is presented as Database API unavailable, while cached/loaded report data remains viewable. Network status must never fabricate physical link state.
- Settings contains exactly three top-level categories: Internet, Favorites and Preferences. Category controls are keyboard-operable tabs and use short presentation-only transitions with reduced-motion handling.
- Internet contains local clock/time zone plus request-scoped `/v1/network-info` observations. IPv4 text is orange and IPv6 text is green. Unobserved address families, DNS resolver identity and other unavailable values remain explicitly unavailable/not observable and are never inferred.
- Favorites/preferences persistence is opt-in. Before `Remember on this device` is enabled, Favorites and Submitted/Vendor/Type default-expansion settings remain in-memory session state only; writes to their localStorage keys are forbidden. Enabling Remember may store only bounded validated Favorite report IDs and the three boolean UI defaults. Disabling Remember removes persistent Favorite/UI-pref keys. `Clear saved browser data` removes the persistent keys and resets current Favorites/defaults.
- The Preferences category must state that stored Favorites/display preferences remain only in this browser, are not sent to VulkanScope and are never stored in D1. Network information remains request-scoped and independent from the persistence toggle.
- Report-detail Favorite, Share and Copy-link actions are presented as one compact, design-consistent grouped control with local inline SVG icons. Widely separated generic page buttons are not used for these three report actions.
- Report-row hover/pressed state changes and report-detail enter/exit navigation use short smooth compositor-friendly transitions. Data/state changes remain immediate and authoritative; motion must not restore stale routes or delay report semantics. `prefers-reduced-motion` disables decorative motion.
- Current frontend asset is `assets/app.v1023.js` with cache key 1023. `tools/verify_1_0_23_connectivity_settings_motion.py`, its negative-mutation suite, Worker transport contract, source audit, 1.0.22 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.

## Release 1.0.24 regional time / country identity / Compare workspace requirements
- Database release identity is 1.0.24. VulkanScope Database 1.0.23 (ZIP SHA-256 `f3940d1e5fafc28bffb3690b40ffcd513d692e57077849e0655440a55968a66d`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and the VulkanScope 1.0.19 submission floor remain unchanged.
- The live connectivity banner keeps the 1.0.23 layout-participating behavior. Database-API-unavailable state has no redundant right-side `UNAVAILABLE` badge. Once a failed/offline/checking state has genuinely recovered, the restored-success surface remains visible for the full three-second success interval; a second successful reachability probe must not collapse it early. Reduced-motion affects decoration only, never this state/timing contract.
- Preferences remains one of exactly three Settings categories and gains validated regional presentation settings for country/region, date layout, 12/24-hour clock, time zone and seasonal offset. These settings affect presentation only: source UTC/ISO report timestamps, sorting, filtering, stored payloads, hashes and D1 values are never rewritten.
- All human-readable report/submission timestamps rendered by the frontend use the same regional formatter contract, including Reports, report detail, Compare metadata, Settings clock and request-scoped server time. Explicit date layouts are DD/MM/YYYY, MM/DD/YYYY, YYYY-MM-DD and localized short-month; automatic mode follows the selected/browser region. Clock mode supports automatic, 24-hour and 12-hour AM/PM.
- Time-zone preference supports browser-local automatic mode, UTC, GMT and the browser's supported IANA time-zone set. Automatic seasonal mode follows the selected zone rules. Standard/winter and daylight/summer modes are presentation-only offset overrides derived from that zone's current-year offsets; if the zone has no seasonal offset change the effective offsets remain equal. No report instant is mutated.
- `Remember on this device` remains explicit opt-in. The UI preference storage allow-list expands only to the three existing report-column booleans plus the five bounded regional strings (`regionalCountry`, `regionalDateFormat`, `regionalClock`, `regionalTimeZone`, `regionalSeason`). With Remember off these values remain session-only; disabling Remember or clearing browser data removes their persistent copy. Favorites/preferences are never sent to VulkanScope or D1.
- Internet country presentation is derived only from the request-scoped country code already supplied by `/v1/network-info`. A valid two-letter country code is expanded with platform `Intl.DisplayNames` and paired with its ISO regional-indicator flag glyph; there is no third-party flag/CDN request, tracking request or static stale flag service. Unknown/special non-country codes remain explicit instead of being fabricated.
- Compare keeps its evidence semantics and filters but is presented as a distinct comparison workspace: persistent A/B report identity cards, baseline/candidate visual distinction, a keyboard-operable Swap action, grouped comparison-mode controls, focused evidence-filter panel, visible-difference overview, section counts and A/B column differentiation. Cross-producer/profile-revision warnings and technical/common-evidence semantics remain authoritative.
- Current frontend asset is `assets/app.v1024.js` with cache key 1024. `tools/verify_1_0_24_regional_compare_country.py`, its negative-mutation suite, Worker transport contract, source audit, immutable 1.0.23 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.

## Release 1.0.25 live network observation / local flags / coherent controls / Surface and Encyclopedia workspace requirements
- Database release identity is 1.0.25. VulkanScope Database 1.0.24 (ZIP SHA-256 `d2cfde68221dbab5ceb461e14e4afcef1171de4747798a264d4db7ca2a575e27`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and the VulkanScope 1.0.19 submission floor remain unchanged.
- Internet-loss UI is event-first and probe-backed. Browser `offline` must expose the persistent offline surface immediately; browser `online` and Network Information API `change` events must force a reachability recheck. A first failed foreground `/v1/sync` or `/v1/network-info` request is sufficient to expose Database-API-unavailable state; the previous two-failure debounce is forbidden. The lightweight sync-head probe uses a short bounded timeout so a stale `navigator.onLine=true` state cannot hide a dead uplink for the generic 20-second request timeout.
- A genuine reconnect shows the restored success surface for a full five seconds before smooth layout collapse. The restored banner has no redundant `Online` badge/right-side status word. Successful background observations during that five-second interval must not collapse it early.
- Settings > Internet has no manual Refresh action. While the Settings drawer is open on Internet and the browser is online, request-scoped `/v1/network-info` observations refresh automatically at a bounded three-second interval, and connection events force an immediate refresh. Leaving the Internet category, closing Settings, becoming hidden/offline, or losing connectivity must not leave an uncontrolled polling timer running.
- Country identity continues to originate only from the request-scoped two-letter country code. To avoid platform-dependent regional-indicator rendering, every accepted country code is mapped to a bundled same-origin local PNG flag asset. Exactly one bounded local asset exists per supported country code; no flag CDN, remote image request, tracking service or runtime third-party dependency is permitted. The localized `Intl.DisplayNames` country name and two-letter code remain visible text and the flag has meaningful alternative text.
- All visible page scrollbars and custom-select list scrollbars use the Database dark/red design system with high-contrast hover affordance. Native `<select>` elements remain the authoritative form state but visible dropdown interaction uses the existing accessible custom-select layer, including the regional country/date/clock/time-zone/season controls; keyboard operation, focus visibility and reduced-motion behavior remain mandatory.
- Encyclopedia is reorganized as a distinct local-reference workspace: locked Vulkan registry identity, corpus counts, prominent local search, category navigation, explicit reference-vs-runtime-evidence guidance, result status and structured reference cards. Search/category semantics and the locked local corpus remain unchanged; registry presence must never become runtime capability evidence.
- Surface is reorganized as a distinct WSI/presentation evidence workspace with filtered-report/query/presentation/format/mode summary counts, grouped evidence navigation, exact-value filtering, explicit semantic guidance and separated capabilities/formats/present-modes/presentation-queue sections. Redesign is presentation-only: Surface query status, availability/support distinctions, canonical values and raw queue evidence are never inferred, merged or renamed.
- Current frontend asset is `assets/app.v1025.js` with cache key 1025. `tools/verify_1_0_25_connectivity_surface_encyclopedia.py`, its negative-mutation suite, Worker transport contract, source audit, immutable 1.0.24 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.

## Release 1.2.0 submission floor / workspace / Settings clarity requirements
- Database release identity is **1.2.0**. VulkanScope Database 1.0.25 (ZIP SHA-256 `641d7e24bd5b86731fa3d5f395137fd3c060f5559719899834ba34e128f85da5`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362 and historical GET readability remain unchanged.
- New report POSTs require **VulkanScope 1.2.5 or newer**. The floor check runs before generic report validation and is enforced by the Worker/API, not merely by presentation. VulkanScope 1.x `versionCode` identity is `1000 + minor*100 + patch`, so 1.2.5 is 1205 and 1.2.6 is 1206. Existing stored reports from older producer versions remain readable and comparable.
- Settings > Internet observations remain live, bounded and request-scoped, but successful automatic three-second refreshes are silent after the first observation. A recurring `Updating connection observation…` surface is forbidden. Genuine errors remain explicit and must not erase the last successfully observed values.
- The Regional country selector uses the Database's English UI language for country names, provides the bundled same-origin local flag for every supported two-letter country code and gives country labels enough width/wrapping to remain readable. The hidden native `<select>` remains authoritative and keyboard-accessible; no flag CDN or third-party country service is permitted.
- Compare explicitly emphasizes both compared VulkanScope producer versions in bold white, including the cross-producer warning and the persistent A/B identity surfaces. Cross-producer warnings continue to distinguish producer/query coverage changes from Vulkan-driver changes and must not hide one-sided evidence.
- Every primary view except Reports is a purpose-specific workspace. Surface, Compare and Encyclopedia retain their specialized evidence/reference semantics; Devices, Versions, Properties, Limits, Features, Formats, Memory, Queues, Display & HDR, Extensions, Instance, Profiles, Portability and Statistics gain clear workspace purpose/context and coherent filter hierarchy. Global filters are grouped by Hardware, Vulkan & driver, Platform, Producer, Submission, Evidence state and Display evidence only when applicable. View-specific subfilters use the same coherent workbench treatment. Redesign is presentation/navigation only and must not change capability state, denominators, raw values, support inference, sorting source values, report identity or canonical Vulkan evidence.
- Settings informational messages use the blue information-callout treatment with a local information icon. Warning/error messages use the amber/yellow warning-callout treatment with a local warning icon. Color alone is never the only distinction; icon and text remain present. These callouts introduce no remote assets or telemetry.
- Current frontend asset is `assets/app.v1200.js` with cache key 1200. `tools/verify_1_2_0_workspace_floor_settings.py`, its negative-mutation suite, Worker transport contract, source audit, immutable 1.0.25 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.


## Release 1.2.1 comparison tracking / temporal integrity / confirmation / report-detail requirements
- Database release identity is **1.2.1**. VulkanScope Database 1.2.0 is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362, the VulkanScope 1.2.5 new-submission floor and historical GET readability remain unchanged.
- Compare keeps the A/B identity and comparison-mode controls trackable while reading long evidence tables. Once the full workspace scrolls beneath the sticky application header it becomes a smooth compact sticky surface; returning to its natural top position restores the complete workspace. The compact state must not change selected reports, comparison toggles, evidence filters or evidence semantics. Reduced-motion is respected.
- Page scrolling keeps both directional controls visible whenever the document is scrollable. At the top the up control is visibly disabled/non-interactive; at the bottom the down control is visibly disabled/non-interactive. The primary page scrollbar is slightly wider than 1.2.0 while retaining the Database dark/red design.
- Browser `navigator.connection.effectiveType` is presentation-only and must be labelled as an **effective network performance class**, never as physical access technology. Values such as `4g` must explicitly state that they can occur on FTTH, Wi-Fi or Ethernet and do not prove LTE/5G/mobile access.
- Regional date/time preferences are presentation-only. Report sorting, submission-age filtering, comparison identity and all other time predicates consume the original server-authored timestamp through one raw epoch parser before localization; changing country/date/clock/time-zone/season preferences must never change which report passes a filter or its chronological order.
- Removing an existing Favorite and clearing saved browser data are destructive local actions and require an accessible in-product confirmation dialog. Cancelling leaves state unchanged. Adding a Favorite remains immediate. No browser-native confirm surface or remote dependency is introduced.
- Report-detail navigation is functional for every visible tab. The detail-tab synchronization helper is mandatory, active tab/ARIA/tabindex state stays coherent, and an empty source category remains navigable and is represented as not reported rather than causing a blank/unresponsive detail workspace.
- Current frontend asset is `assets/app.v1201.js` with cache key 1201. `tools/verify_1_2_1_compare_temporal_detail.py`, its negative-mutation suite, Worker transport contract, source audit, immutable 1.2.0 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.

## Release 1.2.2 GitHub Release retry hardening / inherited 1.2.1 UI integrity requirements
- Database release identity is **1.2.2**. VulkanScope Database 1.2.1 (ZIP SHA-256 `8df3051f0c495a1c245792cbccf431660de906a1ab757129c140f197a1944107`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, report IDs, submission schema 2, technicalReport schema 3, normalizer 16, Vulkan 1.4.362/header 362, the VulkanScope 1.2.5 new-submission floor and historical GET readability remain unchanged.
- The canonical release workflow remains default-branch/main driven and keeps least privilege: repository/default permission is `contents: read`; only the `release` job may request `contents: write`; Pages deployment keeps only `contents: read`, `pages: write` and `id-token: write`.
- GitHub Release publication must tolerate transient GitHub service failures without weakening tag immutability. Release discovery, creation and asset upload may retry only bounded transient failures (`HTTP 429`, `HTTP 5xx`, rate-limit or temporary-unavailable responses), for at most five attempts with increasing delay. Non-transient failures fail immediately.
- Before every mutating release attempt, any existing `v1.2.2` tag/release must resolve to the exact validated `GITHUB_SHA`. A conflicting tag/release must fail closed and require a new Database version; published tags must never be force-retargeted.
- If GitHub accepted a release mutation server-side but returned a transient 5xx to the runner, the next retry must re-discover release/tag state first. An already-created same-commit release is completed with idempotent `gh release upload --clobber`; the workflow must not blindly repeat creation or retarget a tag.
- Pages remains release-gated: `releaseReady:true` may be produced only after the GitHub Release job succeeds. A failed/exhausted release retry must prevent deploy and must not publish a refresh marker.
- All 1.2.1 UI behavior remains mandatory: sticky/compact Compare tracking, raw-timestamp sort/age-filter semantics, effective-network-class wording, destructive confirmations, page-scroll endpoint states and report-detail tab synchronization.
- Current frontend asset is `assets/app.v1202.js` with cache key 1202. `tools/verify_1_2_2_release_retry.py`, its negative-mutation suite, Worker transport contract, source audit, immutable 1.2.1 predecessor regression contract, Pages artifact audit and deterministic strict-package build are mandatory release gates.


## Release 1.2.3 browser compatibility / English presentation / endpoint motion / mobile overflow requirements
- Database release identity is **1.2.3**. VulkanScope Database 1.2.2 is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, report IDs, normalizer 16, Vulkan 1.4.362/header 362, VulkanScope 1.2.5 new-submission floor, Worker security boundaries and historical reads remain unchanged.
- The validated full-experience browser floors are Chromium 84+, Firefox 86+ and Safari 14.1+. A same-origin, pre-application compatibility script must identify these families/versions and feature-probe required Promise/fetch/Map/Set/URL/URLSearchParams/AbortController/ResizeObserver/requestAnimationFrame/matchMedia/Web Animations capabilities. Incompatible browsers receive a dedicated modern explanatory page before report UI is exposed; compatibility detection remains local and is never persisted or submitted.
- Browser locale must never localize Database UI strings. Country names, AM/PM markers, long dates and automatically formatted dates/times remain English. User-selected country/date/clock/time-zone/season preferences still control presentation semantics only; raw timestamps, filtering and sorting remain unchanged.
- Native Chromium/WebKit vertical scrollbar endpoint arrows follow explicit document `at-page-top` / `at-page-bottom` state classes rather than relying only on pseudo-class support. The impossible direction is visibly muted at the corresponding endpoint. Existing application page-scroll controls retain their disabled semantics.
- Interactive surfaces use the shared short opacity/transform/color/border/shadow motion language. `prefers-reduced-motion` removes decorative animation while preserving state changes, focus visibility and functionality.
- Mobile/portrait layouts must not create document-level horizontal overflow. Shells, hero/workspace/filter/settings/Compare content use `min-width:0`, tables keep their intentional local horizontal scrollers, detail tabs remain touch-scrollable, and compact breakpoints at 760 px and 430 px keep controls usable without clipping.
- Browser-visible assets advance to `assets/app.v1203.js` and `assets/browser-compat.v1203.js`, cache key 1203. `tools/verify_1_2_3_browser_motion_mobile.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.2 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.2.4 CI generated-index verification / bounded GPU-device report-table requirements
- Database release identity is **1.2.4**. VulkanScope Database 1.2.3 (ZIP SHA-256 `e93a67fa86a0070b3aba5ba93b1695a60b83cdefc6d557ad7dff674c6607382e`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, report IDs, normalizer 16, Vulkan 1.4.362/header 362, VulkanScope 1.2.5 new-submission floor, browser floors and historical reads remain unchanged.
- The canonical GitHub workflow must never execute a predecessor-version release verifier against current generated metadata. Windows source verification, post-`build_index.py` verification and release-source verification must execute the current 1.2.4 verifier/negative suite. This specifically prevents the 1.2.3 checkout from being rejected by the 1.2.2 verifier for missing `app.v1202.js` and stale 1.2.2 identity markers.
- The bounded GitHub Release retry/tag-immutability contract introduced in 1.2.2 remains mandatory: transient 429/5xx failures may retry at most five times with re-discovery, an existing tag must resolve to the validated `GITHUB_SHA`, and a published tag must never be retargeted.
- Any normal page table whose rows enumerate GPU/device/report identity must be bounded like Reports rather than rendering an unbounded report-derived row set. Devices, Versions, Memory heap/per-report tables, Queues, Surface format/presentation-queue tables, Display & HDR, Portability and the Statistics GPU-model table use 10/25/50 per-page controls, explicit `Showing A–B of N`, disabled Previous/Next endpoints and independent per-table page state. Aggregate capability tables that do not enumerate GPU/device/report identity remain unchanged.
- The bounded-table layer is presentation-only. Filtering, sorting, report membership, evidence state, coverage denominators and raw report data are computed from the complete filtered set before pagination; pagination may only slice the final rendered row array.
- Changing the shared per-page size resets bounded table pages and the Reports page to page 1 so stale page indexes cannot hide data after a size change. Multiple bounded tables on one workspace keep independent page cursors.
- Browser compatibility, English-only presentation, endpoint-scroll state, reduced-motion behavior and mobile-overflow containment from 1.2.3 remain regression-protected. Current browser-visible assets are `assets/app.v1204.js` and `assets/browser-compat.v1204.js` with cache key 1204.
- `tools/verify_1_2_4_ci_bounded_tables.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.3 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.2.5 root-scrollbar endpoint / complete motion requirements
- Database release identity is **1.2.5**. VulkanScope Database 1.2.4 (ZIP SHA-256 `123938b1b2517f7939b58350ab12963c6807cef7d0fbab38ba67e9c1e420a606`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, report IDs, normalizer 16, Vulkan 1.4.362/header 362, VulkanScope 1.2.5 new-submission floor, browser floors, bounded report-table pagination and historical reads remain unchanged.
- The Database-themed far-right viewport scrollbar is the authoritative browser-scrollbar replacement on every page. Page endpoint state is mirrored to both `document.documentElement` and `document.body` so the active root scrollbar receives the same state regardless of Chromium/WebKit root-scroll ownership.
- At the absolute top, the viewport scrollbar **UP** arrow is visibly gray/muted. At the absolute bottom, the viewport scrollbar **DOWN** arrow retains the requested Vulkan-red presentation. Non-scrollable documents visually suppress both directions. This presentation rule is independent from the in-page floating page-scroll buttons, whose boundary-disabled semantics from 1.2.1 remain unchanged.
- Literal escaped newline sequences must never be used as stylesheet line separators. The 1.2.3 endpoint/motion/mobile CSS must parse as real CSS rules; a regression test rejects accidental `\\n` serialization in `site.v0390.css`.
- Every primary Database tab/workspace uses the shared short smooth interaction language for hover, press, focus, selected/active state, disclosure, row highlight and open/close motion where applicable. Coverage includes Reports, Devices, Versions, Properties, Limits, Features, Formats, Memory, Queues, Surface, Display & HDR, Extensions, Instance, Profiles, Portability, Statistics, Encyclopedia and Compare plus Settings, custom selects, table scrollers, evidence dialogs and report-detail navigation.
- Decorative motion uses compositor-friendly opacity/transform/color/border/shadow transitions and must not delay or mutate Vulkan evidence/state. `prefers-reduced-motion` disables decorative animation/transitions while preserving state changes, keyboard focus and functionality. Destructive confirmation close is animated before hiding rather than disappearing abruptly.
- Browser-visible assets advance to `assets/app.v1205.js` and `assets/browser-compat.v1205.js`, cache key 1205. `tools/verify_1_2_5_scrollbar_motion.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.4 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.



## Release 1.2.6 cross-browser viewport-scrollbar / report-detail workspace requirements
- Database release identity is **1.2.6**. VulkanScope Database 1.2.5 (ZIP SHA-256 `3e0655b53840a7385979ae8f8bfb2473058dbbef8ef062d624438eabbf28f260`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, VulkanScope 1.2.5 new-submission floor, browser floors, bounded report tables and historical reads remain unchanged.
- The viewport scrollbar must not rely on non-standard `::-webkit-scrollbar-button` endpoint-state rendering. Standard `scrollbar-color`/`scrollbar-width` expose track/thumb styling but do not provide a cross-browser stateful arrow contract. After application initialization, a same-origin DOM viewport scrollbar becomes authoritative and native viewport chrome is hidden; internal scroll containers retain their native/customized scrolling semantics.
- The custom viewport rail synchronizes from the actual document scroll range on scroll/resize/content resize, provides a draggable thumb, track paging, keyboard-operable thumb and explicit up/down buttons. At absolute top the UP arrow is visibly gray and disabled. The DOWN arrow remains Vulkan red even at the absolute bottom as requested, while its disabled/ARIA state prevents motion beyond the document. Non-scrollable documents hide the rail.
- Current-browser review for this release covers the current stable engine families used by Chromium/Chrome/Edge/Brave/Opera/Vivaldi, Firefox and Safari/WebKit. The implementation uses standardized DOM/pointer/scroll primitives rather than browser-specific scrollbar-button painting; the validated compatibility floors remain Chromium 84+, Firefox 86+, Safari 14.1+.
- Report detail is a dedicated evidence workspace rather than a generic metric strip. The hero groups report identity, producer/device/driver/Vulkan context and actions; the exact 64-character report ID is visible without truncation in the identity hero and Overview. Report-ID wrapping/copy affordance must not mutate the canonical identity.
- Report Overview groups submission, application, Android device, GPU/driver and Vulkan/collection scalar metadata without inferring missing values or changing evidence semantics. Every existing technical detail tab and Raw report remain accessible.
- The report Back control is a larger design-system control and remains available for every report-detail tab. Detail tabs are horizontally scrollable/sticky without clipping on narrow layouts. New detail surfaces use the shared smooth interaction language and preserve `prefers-reduced-motion`.
- Browser-visible assets advance to `assets/app.v1206.js` and `assets/browser-compat.v1206.js`, cache key 1206. `tools/verify_1_2_6_cross_browser_detail.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.5 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.


## Release 1.2.7 compare follow-rail / adaptive hero / viewport endpoint requirements
- Database release identity is **1.2.7**. VulkanScope Database 1.2.6 (ZIP SHA-256 `3fcfeb0c819dbe87035419e5743aa1b59f6ec1b0bc0491624db5ce5cf5a005cc`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors, bounded report tables and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Compare tracking is owned by a dedicated sticky shell that follows the viewport below the top navigation while the user scrolls and returns naturally to its full top-of-page presentation. The sticky shell must not be clipped by the Compare workspace. Baseline/Candidate report selectors and their custom menus remain fully visible above the workspace and comparison body; opening a selector temporarily elevates its report card without changing comparison evidence.
- The authoritative same-origin viewport scrollbar rail remains the 1.2.6 DOM rail. Its endpoint state comes from the actual `document.scrollingElement` scroll range. At absolute top the UP arrow is visibly gray and disabled; at absolute bottom the DOWN arrow is visibly gray and disabled. Away from an endpoint the available arrow is Vulkan red. The draggable thumb remains draggable but uses the normal/default pointer cursor rather than grab/grabbing hand cursors.
- The persistent Vulkan Hardware Database hero is redesigned as a workspace-aware command deck shared by every primary Database tab. It retains the global search and loaded-dataset metrics, preserves the repository/developer surfaces, adds a concise evidence-policy surface, and reflects the active tab's existing META title/description without creating a second source of technical truth. The hero redesign is presentation-only and cannot alter filters, denominators, report state or evidence semantics.
- Reports-table row hover is cell-synchronous: entering the same report through Report ID, Favorite, date/time or any other column transitions every row cell—including the Submitted/date-time cell—through the same smooth background timing. No column may pop into its hover state independently.
- All new Compare shell, hero, selector elevation, report-row and viewport endpoint states use the shared short smooth interaction language and preserve `prefers-reduced-motion`. Mobile/narrow layouts must not clip sticky Compare selectors or hero content.
- Browser-visible assets advance to `assets/app.v1207.js` and `assets/browser-compat.v1207.js`, cache key 1207. `tools/verify_1_2_7_compare_hero_scroll.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.6 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.


## Release 1.2.8 Compare fixed-follow reliability requirements
- Database release identity is **1.2.8**. VulkanScope Database 1.2.7 (ZIP SHA-256 `dd18283170e04a6db697e7ad8816fb08376204d0814c1ffe26e0c86c72ea01fb`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors, bounded report tables and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Compare tracking must not depend solely on CSS `position: sticky`, because a future ancestor overflow/contain/layout change can create a different sticky containing block. After the Compare sentinel crosses the visible navigation boundary, the existing Compare shell is explicitly pinned to the viewport with `position: fixed`; a dedicated placeholder preserves its normal-flow height. Returning above the sentinel or leaving Compare clears all fixed geometry.
- Pinned Compare geometry is recalculated from the live Compare-page rectangle and visible navigation boundary on scroll and resize. The panel must remain horizontally aligned with the Compare workspace and reserve room for the authoritative viewport scrollbar rail.
- Baseline/Candidate selectors remain the same authoritative native-select/custom-listbox pair. Their menus must remain overflow-visible above the pinned shell, and opening one may only alter presentation z-order; it must not duplicate or mutate report identity/evidence state.
- Compact/follow presentation remains decorative and preserves the shared smooth interaction language plus `prefers-reduced-motion`. Comparison denominators, field eligibility, cross-producer warnings and Vulkan evidence semantics are unchanged.
- Browser-visible assets advance to `assets/app.v1208.js` and `assets/browser-compat.v1208.js`, cache key 1208. `tools/verify_1_2_8_compare_fixed_follow.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.7 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.2.9 browser-gate / Reports filters / Compare minimization / local-scrollbar requirements
- Database release identity is **1.2.9**. VulkanScope Database 1.2.8 (ZIP SHA-256 `85e242c0abe38516e3674baebeb15b346698718daf1a953aeab481fe4ca9ec1a`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors, bounded report tables and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- The browser-compatibility gate starts **hidden in source HTML**. Supported browsers must never flash an unsupported-browser card before the local feature/version check runs. The existing compatibility script is authoritative: only an actual failed version/feature check reveals the gate and hides the application; a passed check reveals the application and keeps the gate hidden.
- Reports uses the same `FILTER_FAMILIES` grouping and progressively enhanced native-select/custom-listbox presentation as the other applicable Database workspaces. Sort and Per-page controls follow the same grouped control hierarchy. The original native selects remain the authoritative form state and keyboard/listbox behavior remains unchanged.
- Reports filter layout is intrinsically responsive. At narrow/mobile widths the filter surface and Report-view controls collapse to one `minmax(0,1fr)` column with no horizontal page overflow, clipped option labels, or viewport-wider minimum widths.
- The fixed-follow Compare shell retains the 1.2.8 sentinel/placeholder/live-geometry ownership. While pinned it exposes one centered minimize/expand control below the shell. Minimized presentation is decorative only and renders the authoritative selected A/B reports' GPU names in baseline/candidate presentation colors with a visual center divider; it does not create a second report/evidence model or alter comparison keys, denominators, producer warnings, filters or routing.
- Returning above the Compare sentinel, leaving Compare, or entering report detail restores the full Compare workspace and clears minimized state. Narrow pinned geometry is clamped to the actual viewport/workspace width and must never enforce a fixed minimum wider than the available mobile viewport.
- Scrollable Settings content, custom-select menus (including Reports filters), coverage-report dialog bodies and bounded paged evidence lists use the same-origin in-panel scrollbar primitive. It reuses the authoritative viewport scrollbar's arrow/track/thumb visual language, actual host `scrollTop/scrollHeight/clientHeight` metrics, endpoint disabled state, track paging, draggable thumb and keyboard Home/End/Page/Arrow behavior. Native scroll remains the underlying authoritative scroll state; the rail is presentation/control chrome only.
- In-panel rails hide when their host is not scrollable or not visible and must not leak detached-node observers. Dynamic content is synchronized through bounded shared mutation/resize scheduling rather than one unbounded observer/timer per row/option.
- All 1.2.9 transitions preserve the shared short motion language. `prefers-reduced-motion` removes decorative transitions while preserving compatibility gating, filtering, selector keyboard semantics, Compare minimize/expand state and scrollbar functionality/focus visibility.
- Browser-visible JavaScript advances to `assets/app.v1209.js` and `assets/browser-compat.v1209.js`, cache key 1209. `tools/verify_1_2_9_ui_responsive_scroll.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.8 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.
- History-bearing/overlay repository repair for 1.2.9 must remove superseded versioned browser bundles as well as superseded app bundles. In particular, `assets/browser-compat.v1208.js` and any other non-current `browser-compat.v*.js` must be deleted by `tools/repair_repository.py --apply` and rejected by `--check`; extracting a successor ZIP over an older checkout must not leave predecessor browser code able to break Linux CI or enter source history.

## Release 1.2.10 non-Reports mobile filters / Compare version-emphasis requirements
- Database release identity is **1.2.10**. VulkanScope Database 1.2.9 (ZIP SHA-256 `1161306a93a8491a730e50a5c756ffdfb9fe9aad2a0f06130fe6612ab7bd0918`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors, bounded report tables and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Reports retains the responsive filter layout introduced in 1.2.9. Every other primary workspace with global filters must become intrinsically single-column at <=900 px: `#filters`, every direct `.filter-family`, `.filter-family-controls`, enhanced `.custom-select`/button and native-select fallback must stay within the viewport using `min-width:0` and 100% width/max-width rather than shrinking into unreadable side-by-side columns.
- Non-Reports view-specific `.subfilters` follow the same <=900 px single-column containment. Compare's Evidence filters are included, so Section/field-search controls cannot force horizontal page overflow. FILTER_FAMILIES/FILTER_APPLICABILITY and the native selects remain authoritative; responsive presentation must not change filtering semantics, state options, query routing or keyboard/listbox behavior.
- A cross-producer Compare warning keeps the same producer/version comparison and warning semantics, but each escaped VulkanScope version identity (semantic version plus optional versionCode) is presented in bold white with `.compare-version-token`. Styling must never alter the producer values used for comparison.
- Browser-visible JavaScript advances to `assets/app.v1210.js` and `assets/browser-compat.v1210.js`, cache key 1210; CSS uses the same local stylesheet with cache key 1210. `tools/verify_1_2_10_mobile_filters_compare_versions.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.9 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.2.11 Compare mobile identity-flow requirements
- Database release identity is **1.2.11**. VulkanScope Database 1.2.10 (ZIP SHA-256 `83daa53e1c4217079368a0c0bbcce6460be26cb4b0c49f69639731d2fca12a0d`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors, bounded report tables and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- In the full Compare workspace, report identity is content-sized. GPU name, vendor identity and exact driver mode/version must never overlap the Baseline/Candidate metadata chips when any identity item wraps. A normal-state animation cap must be large enough to contain the complete identity block; clipping technical identity to preserve an animation is forbidden.
- At <=760 px the Compare identity line is intrinsically single-column so GPU, vendor and driver rows receive full available width. Long identity text may wrap with `overflow-wrap` but must not be silently truncated, reclassified or rewritten. Metadata chips continue to wrap independently below the identity block.
- The existing fixed-follow/compact Compare state may collapse the identity line only while that compact state is active. Returning to the full workspace restores the complete identity without changing the selected reports, A/B roles, filters, evidence keys, denominators, producer warnings or routing. Reduced-motion behavior remains mandatory.
- Browser-visible JavaScript advances to `assets/app.v1211.js` and `assets/browser-compat.v1211.js`, cache key 1211; CSS remains the local versioned stylesheet with cache key 1211. `tools/verify_1_2_11_compare_mobile_identity.py`, its negative-mutation suite, source/Pages audits, immutable 1.2.10 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Compare semantic change is permitted for this release.

## Release 1.2.12 searchable-filter and overlay-layer requirements
- Immutable predecessor Database 1.2.11 (ZIP SHA-256 `a63b4f85f1064233a22251c67f3aa6bedcc169bf0a986be5028efd0c1947985f`) remains the data/semantic baseline. No D1 migration, stored-payload rewrite, canonical report-hash rewrite, normalizer increment, Vulkan registry change or producer-floor change is permitted.
- Every enhanced selector used inside the main/detail workspaces is locally searchable on desktop and mobile. Search is presentation-only: the hidden native `select` remains authoritative, its complete option collection remains intact, and matching never rewrites option values or filtering semantics.
- Searchable custom listboxes render at most 50 option buttons at once. If more than 50 options match, the current selection is kept inside a bounded 50-option window when practical and the UI states that results are bounded; typing narrows against the complete native option set. A zero-result query shows an explicit empty state.
- Search field keyboard handling must preserve Escape close, ArrowDown/Enter entry into options, option Arrow/Home/End navigation and the existing mouse/touch selection path. Search UI is local-only and introduces no external dependency or network request.
- Opening a Compare report selector while the fixed-follow header is pinned must suppress the centered Minimize/Expand dock so the dock can never overlap or intercept listbox options. Closing the selector restores the dock without changing pinned/minimized state.
- Settings overlay owns a layer above pinned Compare and the viewport/surface chrome, so the complete underlying page receives the dim/blur treatment. Opening Settings closes transient custom listboxes before focus moves into the drawer. Destructive confirmation remains above the Settings drawer.
- Browser-visible JavaScript advances to `assets/app.v1212.js` and `assets/browser-compat.v1212.js`, cache key 1212; CSS remains the local stylesheet with cache key 1212. `tools/verify_1_2_12_filter_search_overlay.py`, its negative-mutation suite, immutable 1.2.11 regression contract, source/Pages audits, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.2.13 filter-pagination / Compare-direction / full-UI audit requirements
- Database release identity is **1.2.13**. VulkanScope Database 1.2.12 (ZIP SHA-256 `bfae0e9bff7b84d73e93dfe37208df2fe3052193eb90648e7f4e63699eb0bb0c`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Searchable main/detail custom selectors paginate the complete post-search result set in deterministic pages of at most 50 options. Search/filter matching is applied before selector pagination, the native `select` remains complete and authoritative, and moving between pages never rewrites option values or filtering semantics. A new search starts at page 1; opening a selector may select the page containing its current authoritative value.
- Selector pagination exposes local Previous/Next controls plus `Page X of Y`, preserves mouse/touch/keyboard operation, and permits keyboard page traversal. Zero-result handling remains explicit. Mobile presentation must fit inside the selector width without document-level horizontal overflow; long option labels may be visually ellipsized while the native option text/value remains intact.
- The fixed-follow Compare minimize control is directional: while the full comparison header is shown, the Minimize action uses a downward chevron; while the header is minimized, the Expand action uses an upward chevron. Resetting/unpinning Compare restores the full-state downward chevron. This is presentation-only and does not alter A/B roles, selected reports, comparison filters, routing or evidence semantics.
- Full-release UI regression review covers mobile and desktop containment, listbox/pager layering, pinned Compare geometry, Settings overlay ownership, long-text wrapping/truncation boundaries, JavaScript syntax, Worker contract, route/report-text identity tests, source/Pages audits, D1 migration replay and deterministic strict-package construction. Intentional local table scrollers remain allowed; document-level horizontal overflow is not.
- Browser-visible JavaScript advances to `assets/app.v1213.js` and `assets/browser-compat.v1213.js`, cache key 1213; CSS remains the local versioned stylesheet with cache key 1213. `tools/verify_1_2_13_filter_pagination_compare_audit.py`, its negative-mutation suite and immutable 1.2.12 regression contract are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.

## Release 1.2.14 direct-page-jump / Browser-information requirements
- Database release identity is **1.2.14**. VulkanScope Database 1.2.13 (ZIP SHA-256 `92424276d6eb586d1d30035753f45d33d40e8f41e150cee5343f573f47e615cb`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Every user-visible pagination surface that exposes a page number also exposes direct page entry: searchable custom-filter selectors, the Reports table, bounded aggregate tables and paged Coverage/Distinct/value modals. The existing Previous/Next controls remain available.
- Direct page input accepts decimal digits only and permits only page numbers in the inclusive current range `1..pageCount`. Keyboard insertion, paste and input-event fallback all reject invalid or out-of-range non-empty values, so page 4 cannot be entered when only three pages exist. Enter and the explicit Go control commit; Escape restores the current page; Arrow Up/Down stay within the valid page range.
- Page-count changes from filtering/searching or page-size changes must immediately update the page field's value, maximum and digit length. Searchable selectors still filter the complete native option set before deterministic 50-option pagination and a new search resets to page 1. Native filter values remain authoritative.
- Direct-page navigation is presentation/navigation only. It must not change report ordering, filters, evidence state, support/availability semantics, report IDs/hashes, denominators or D1 payloads. Mobile and desktop layouts must contain the new controls without document-level horizontal overflow.
- Settings > Internet adds a separate **Browser information** section below **My internet information**. Browser/runtime details are derived locally from standard browser APIs and may include best-effort browser/version/engine identity, User-Agent Client Hints where exposed, platform/languages, privacy signals, logical cores/device-memory hints, touch/viewport/screen values, UI preferences and Web Platform capability availability.
- Browser information is local-only UI. It is never attached to VulkanScope reports, sent to the Worker, persisted in D1 or obtained from a third-party browser-identification/fingerprinting service. Missing/privacy-restricted values are shown as not exposed/unavailable rather than inferred; GPU renderer/canvas fingerprinting is forbidden.
- Browser-visible JavaScript advances to `assets/app.v1214.js` and `assets/browser-compat.v1214.js`, cache key 1214. `tools/verify_1_2_14_page_jump_browser_info.py`, its negative-mutation suite, immutable 1.2.13 regression contract, source/Pages audit, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.

## Release 1.2.15 Information / report-JSON-export requirements
- Database release identity is **1.2.15**. VulkanScope Database 1.2.14 (ZIP SHA-256 `eedc9dcabca095a0a02e041c2c46b79e8bfe3798f5a9b871c9f0efb6bc5b0d86`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Settings has exactly four top-level categories: Internet, Favorites, Preferences and **Information**. Information is a local read-only surface and must not trigger network requests merely by being opened. It lists every library/dependency or runtime component explicitly declared or referenced by the repository with its name, version/pinning status, license, purpose/scope and declaration source.
- Dependency metadata must distinguish direct repository pins from toolchain/transitive/runtime components. If an exact transitive version is not pinned by the repository, the UI must state that fact (for example, `Resolved by Wrangler 4.130.0`) instead of fabricating a version. The Pages frontend continues to ship zero third-party JavaScript runtime libraries, analytics libraries, advertising libraries or remote fonts.
- Report detail exposes **Download JSON** beside Favorite, Share and Copy link. The action uses the existing canonical public `GET /v1/reports/:id?compact=1` read path, serializes that response locally as UTF-8 `application/json`, provides a filesystem-safe filename and does not call POST, mutate state, rewrite report bytes, or add an export-specific Worker/D1 endpoint.
- The JSON action is conceptually consistent with the availability of per-report JSON export in Sascha Willems' Vulkan Hardware Database, but VulkanScope Database must describe its download accurately as its own stored public report payload; it must not label it a Vulkan Profile unless it is actually generated against the Khronos Vulkan Profiles schema.
- The fourth Settings category and all Information cards/license labels must fit desktop and mobile layouts without document-level horizontal overflow. On narrow phones the category navigation becomes a 2×2 grid, long version/license/source text wraps, and the four report-detail actions wrap into usable touch targets without clipping.
- Browser-visible JavaScript advances to `assets/app.v1215.js` and `assets/browser-compat.v1215.js`, cache key 1215. `tools/verify_1_2_15_information_json_export.py`, its negative-mutation suite, immutable 1.2.14 regression contract, source/Pages audit, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.

## Release 1.3.0 Vulkan® presentation / license-reading / full-audit requirements
- Database release identity is **1.3.0**. VulkanScope Database 1.2.15 (ZIP SHA-256 `3e78a70f31bf92e7ea2024227ae2df7f8cae3656dbe5cdffecef805eee352523`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- User-facing presentation of the standalone product/API name **Vulkan** uses **Vulkan®**. The compound project name **VulkanScope** is never rewritten. This is presentation-only: canonical Vulkan registry tokens, raw report text, API/storage fields, JSON payloads, report hashes, routes, code identifiers, filenames, extension/profile names and parser comparisons remain byte/semantic compatible and do not gain the trademark symbol.
- The footer shows, directly below the VulkanScope Database identity, the red Turkish notice `VulkanScope projesinin Khronos Group ile hiçbir ilgisi yoktur, resmi Khronos Group projesi değildir`. The same notice is the first content in Settings > Information. It must wrap without horizontal overflow on desktop and mobile.
- Settings > Information keeps the repository-declared component inventory and adds a clearly styled **Read license (.md)** action for licensed software/toolchain components with a local same-origin Markdown-readable license document. The local documents identify their official upstream source. Components that are managed services/platform APIs rather than bundled libraries must not be given a fabricated software license file.
- License reading must not introduce third-party JavaScript, analytics, remote fonts or automatic external network requests. Local license links may open a same-origin `.md` document; upstream source URLs may be stated inside those documents for provenance.
- The 1.3.0 full audit covers JavaScript/Python syntax, routes, Worker transport/security, D1 migration replay, immutable predecessor contract, source/Pages allow-list integrity, local-link integrity, stale versioned assets, mobile/desktop containment, footer/Information wrapping, Settings layer ownership, Compare/listbox behavior, filter pagination/page-jump contracts, JSON export, browser-information privacy boundaries, incorrect/fabricated dependency metadata and deterministic release packaging. Any discovered release-blocking regression must be corrected before packaging.
- Browser-visible JavaScript advances to `assets/app.v1300.js` and `assets/browser-compat.v1300.js`, cache key 1300. `tools/verify_1_3_0_trademark_licenses_full_audit.py`, its negative-mutation suite, immutable 1.2.15 regression contract, source/Pages audit, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.

## Release 1.3.1 startup-freshness / filter-integrity / inline-license / live-report-notification requirements
- Database release identity is **1.3.1**. VulkanScope Database 1.3.0 (ZIP SHA-256 `f49ebdc98d776de7792ceafc31c433c855bb87be1415b75191a6599c4f21ebe0`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- All first-party user-facing VulkanScope Database UI copy is English unless a future requirement explicitly requests another language. The Khronos independence notice is exactly: `VulkanScope is not affiliated with the Khronos Group and is not an official Khronos Group project.` It appears in the footer and at the start of Settings > Information. This supersedes the Turkish 1.3.0 presentation requirement only; the independence meaning is unchanged.
- Every searchable filter selector presents search plus deterministic 50-option pages and direct numeric page entry even when the current result set has only one page. Previous/Next and Go controls remain visible but disabled whenever they cannot change state. In particular, **Go is disabled when the entered page equals the current page**, when the value is empty/invalid/out of range, or when only one page exists.
- Filter menus keep search, options and pagination in distinct layout regions. Only the option region scrolls; search and pagination must not overlay or obscure options. The menu and its custom scrollbar remain contained and correctly layered on mobile and desktop, including Compare and filter-family stacking contexts. Native select values remain authoritative.
- License documents are opened in a same-origin, accessible in-site modal viewer. The viewer displays the bundled `.md` source as text, is bounded to 512 KiB, supports Escape/backdrop/close dismissal, restores focus, uses the project's custom scrollbar treatment and never opens a new tab or fetches third-party license content automatically.
- Startup frontend freshness is checked before the app is revealed. A small same-origin versioned bootstrap fetches `data/release.json` with `cache:no-store`; when a newer release-ready marker is found it independently probes the current `index.html` and marker-named app asset before cache-busted replacement navigation. Failure/timeout must reveal the local app rather than trap the user. HTML also carries defensive no-cache metadata. Worker metadata is never accepted as frontend publication readiness.
- Live report synchronization remains atomic. When reconciliation actually adds one or more previously unseen report IDs after initial load, a green accessible popup reports the number added and states that the Database updated automatically. Repeated polls with no additions do not notify; notification does not alter report ordering, filtering or evidence semantics.
- Release auditing includes stale/hard-coded Database identity checks (including footer/schema metadata), JavaScript/Python syntax, filter menu containment/layering, pagination behavior, license-modal local-only behavior, startup release bootstrap, live report notification, Worker transport/security, D1 migration replay, source/Pages allow-lists, immutable predecessor regression and deterministic packaging.
- Browser-visible JavaScript advances to `assets/app.v1301.js`, `assets/browser-compat.v1301.js` and `assets/release-bootstrap.v1301.js`, cache key 1301. `tools/verify_1_3_1_freshness_filters_license_live_sync.py`, its negative-mutation suite and immutable 1.3.0 regression contract are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.


## Release 1.3.2 cache-convergence / pointer-integrity / pagination / information-notice requirements
- Database release identity is **1.3.2**. VulkanScope Database 1.3.1 (ZIP SHA-256 `ec60e0456f46427713db697e6bd0b8e7742dfad62843e9743befdc3576da84dc`) is the immutable predecessor. D1 schema/migrations, stored payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Release freshness is convergence-based rather than one-shot. The startup bootstrap performs a bounded first-paint check, then keeps retrying at bounded intervals and on `visibilitychange`, `pageshow` and `online`. A newer release is eligible only when the no-store release marker is release-ready and independently cache-busted probes prove that the published `index.html`, marker-named app asset, current browser-compat asset and current release-bootstrap asset all identify the same release. Once proven, navigation to the cache-busted current `index.html` is automatic; the user is not required to press a Refresh button.
- The immediate predecessor frontend triplet (`app.v1301.js`, `browser-compat.v1301.js`, `release-bootstrap.v1301.js`) remains byte-identical and is intentionally retained in the Pages artifact for one release as a CDN propagation bridge. Older stale frontend assets remain forbidden. This prevents an edge that still serves 1.3.1 HTML from losing the assets referenced by that immutable HTML while 1.3.2 propagates.
- Searchable filter pagination must fit every supported filter width on desktop and mobile. Previous and Next remain accessible icon buttons with explicit ARIA labels; the page-jump control is the flexible middle column and no pager control may force horizontal overflow.
- Custom viewport/surface scrollbar rails are non-interactive in their blank gutter/corner regions. Pointer input is accepted only by intended arrow/track/thumb controls. A pointer drag/release or click inside a custom-select scrollbar or menu must not bubble into the document-level outside-click closer and close the filter accidentally.
- Settings > Internet > Browser information presentation remains unchanged from 1.3.1; this release introduces no browser-logo bundle or new third-party browser artwork dependency.
- The exact English Khronos independence text remains `VulkanScope is not affiliated with the Khronos Group and is not an official Khronos Group project.` in the footer and Settings > Information. It is presented as a modern blue informational note with an information icon, not as a red danger/error notice.
- Browser-visible JavaScript advances to `assets/app.v1302.js`, `assets/browser-compat.v1302.js` and `assets/release-bootstrap.v1302.js`, cache key 1302. `tools/verify_1_3_2_cache_pointer_browser_ui.py`, its negative-mutation suite and immutable 1.3.1 regression contract are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-state semantic change is permitted for this release.

## Release 1.3.3 Compare-direction / symmetric-motion requirements
- Database release identity is **1.3.3**. VulkanScope Database 1.3.2 (ZIP SHA-256 `77c611aa89f26c6f3de3ed09046f7f54d3c99b35e47f93df0e5ebb88798afa73`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser compatibility floors and the VulkanScope 1.2.5 submission floor remain unchanged.
- Compare minimize/expand direction has exactly one authoritative presentation source. `setCompareMinimizeControl` writes the chevron path for the current state: full header / Minimize is a downward chevron; minimized header / Expand is an upward chevron. CSS must not rotate that same SVG based on `.is-minimized`, because doing both transformations cancels the visible direction change. Reset/unpin restores the full-state downward chevron.
- Compare minimize/expand remains presentation-only: selected A/B reports, report identity, filters, evidence keys, denominators, producer warnings, routing and technical comparison semantics are unchanged. Existing upper Compare workspace collapse/expand transitions remain compositor-friendly and use the same state path as the dock control.
- Local license viewer opening and closing must be symmetrical. Visibility is staged with short opacity/transform transitions after authoritative hidden/state changes; a monotonic motion token prevents a stale close completion from hiding a viewer that has already reopened. Focus restoration and local-only license fetching remain intact.
- Searchable custom-selector page changes and bounded local modal page changes use one short compositor-only opacity/translate entrance after the authoritative page/DOM state is committed. Previous, Next, direct page jump, keyboard and pointer paths converge on the same page-state functions; animation must never change values, filtering semantics, ordering, report identity or pagination math.
- All 1.3.3 decorative motion obeys `prefers-reduced-motion: reduce`: transitions/WAAPI page motion are skipped while the final state, focus movement and accessibility semantics still update synchronously. Rapid interaction cancels/replaces obsolete page animations rather than allowing stale visual completions.
- Browser-visible assets advance to `assets/app.v1303.js`, `assets/browser-compat.v1303.js` and `assets/release-bootstrap.v1303.js`, cache key 1303. Pages retains only the immediate immutable 1.3.2 frontend triplet as the cache-convergence bridge; older 1.3.1 versioned triplets are repair targets and must not ship.
- `tools/verify_1_3_3_compare_direction_symmetric_motion.py`, its negative-mutation suite, source/Pages audits, immutable 1.3.2 regression contract, Worker transport test, D1 migration replay and deterministic strict-package build are mandatory release gates. No new D1 migration or stored-data rewrite is permitted.


## Release 1.3.4 filter-scrollbar / Compare-chevron requirements
- A custom filter scrollbar is visible only when its own scroll host has real overflow and JavaScript has marked the rail `.is-scrollable`; opening a filter must not force a decorative rail for short option lists.
- A non-scrollable custom rail stays hidden, `aria-hidden`, and non-interactive; genuine overflow remains keyboard, pointer, wheel, touch and reduced-motion compatible.
- Compare state chevrons are semantic: full/open **Minimize** points upward, minimized/closed **Expand** points downward. JavaScript owns path direction and CSS must not state-rotate the SVG.
- Database 1.3.4 is presentation-only relative to 1.3.3: no D1 migration, report-schema, report-hash, normalizer, Vulkan registry baseline, producer-floor or browser-floor change is permitted.

## Release 1.3.5 Submitted time-zone wrapping requirements
- Database release identity is **1.3.5**. VulkanScope Database 1.3.4 (ZIP SHA-256 `b64d7ac3a8b20e3a3613f4b64db0a806486bc4fdbc2c3840f86d86c7c20128b0`) is the immutable predecessor. D1 schema/migrations, stored report bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 submission floor remain unchanged.
- Reports Submitted Date, Time and Time zone remain derived from the server-authored `submitted_at` instant. This release changes only wrapping/presentation; sorting, age filtering, report identity, Compare identity and stored timestamps remain untouched.
- The Submitted Time zone value must permit wrapping even though earlier `.submitted-stack span` styling used `white-space: nowrap`. Date and Time remain intentionally single-line.
- If the selected regional seasonal override appends `Daylight / summer offset` or `Standard / winter offset`, that seasonal descriptor must appear on a dedicated second line under the IANA zone plus GMT offset. Its wording must remain exact and must not be truncated or abbreviated.
- Long IANA zone names must remain complete and readable with safe wrapping and without document-level horizontal overflow, ellipsis or clipping.
- Existing Submitted disclosure sizing/animation, table-scroll controls and reduced-motion behavior remain unchanged. The 1.3.4 demand-driven filter scrollbar and Compare chevron contracts remain mandatory.
- Browser-visible assets advance to `assets/app.v1305.js`, `assets/browser-compat.v1305.js` and `assets/release-bootstrap.v1305.js`, cache key 1305. Pages retains only the immutable 1.3.4 frontend triplet as the one-release cache-propagation bridge.
- `tools/verify_1_3_5_submitted_timezone_wrap.py`, its negative-mutation suite, immutable 1.3.4 regression contract, source/Pages audits, Worker contract, D1 migration replay and deterministic strict-package construction are mandatory release gates.
- No D1 migration, report-payload rewrite, report-hash rewrite, normalizer increment, producer-floor change or Vulkan evidence-semantic change is permitted.

## Release 1.3.6 report-detail evidence-workspace / Raw-report download requirements
- Database release identity is **1.3.6**. VulkanScope Database 1.3.5 (ZIP SHA-256 `92c86c9013f4bab4ea8376e5db07c965b035843313c893561b047957e5189e97`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, browser floors and the VulkanScope 1.2.5 submission floor remain unchanged.
- The report **Overview** layout remains the established identity/metadata overview. Registry, Properties, Limits, Features, Formats, Memory, Queues, Surface, Display & HDR, Extensions, Instance, Profiles and Raw report use a redesigned evidence-workspace presentation with a section identity surface, bounded summary statistics and clearly separated evidence panels. Presentation must never drop, merge, relabel or reinterpret canonical Vulkan values or evidence state to fit the new layout.
- Every non-Overview report tab remains navigable even when its category is empty. Empty categories render an explicit not-reported/empty evidence state rather than removing the tab or silently substituting another category.
- Report-detail state semantics remain authoritative: supported green, unsupported red, available blue, unavailable amber, not-applicable purple and unknown gray. Query availability remains separate from returned scalar/property values; zero and false remain valid reported values when the query succeeded.
- Technical tables retain the existing local horizontal-scroller controls, direct touch/trackpad scrolling and keyboard accessibility. The redesign may wrap tables inside evidence cards but must not restore browser-native horizontal scrollbar chrome as the primary control or clip canonical Vulkan tokens/raw values.
- The Raw report tab explains the relationship between `reportText` and normalized/structured evidence. The raw text remains independently accessible and selectable. A **Download raw report** action creates a local UTF-8 `text/plain` Blob directly from the stored `reportText` string without adding/removing bytes, normalizing line endings, calling POST, adding an export Worker endpoint, mutating D1, or changing report identity/hash semantics.
- Raw report scrolling uses the same local first-party DOM surface-scrollbar system as Settings, custom listboxes, dialogs and the license viewer. The native Raw-report scrollbar is hidden only after enhancement; the themed rail appears only when the Raw-report text actually overflows and retains pointer, keyboard, wheel/touch and reduced-motion behavior.
- The redesigned detail workspace is intrinsically responsive: intro surfaces, statistics, panel headings, Raw-report controls and long Vulkan/raw values remain within the report workspace on desktop/mobile. Intentional table-local horizontal scrolling remains allowed; document-level horizontal overflow is not.
- Decorative report-detail motion remains short and compositor-friendly and obeys `prefers-reduced-motion`. Motion never delays or changes normalized data, tab state, raw report text, download bytes or accessibility state.
- Browser-visible assets advance to `assets/app.v1306.js`, `assets/browser-compat.v1306.js` and `assets/release-bootstrap.v1306.js`, cache key 1306. Pages retains only the immutable 1.3.5 frontend triplet as the one-release cache-propagation bridge.
- `tools/verify_1_3_6_report_detail_workspace_raw_download.py`, its negative-mutation suite, immutable 1.3.5 regression contract, source/Pages audits, Worker contract, D1 migration replay and deterministic strict-package construction are mandatory release gates.
- No D1 migration, report-payload rewrite, report-hash rewrite, normalizer increment, producer-floor change or Vulkan evidence-semantic change is permitted.

## Release 1.3.7 browser brand-mark requirements
- Database release identity is **1.3.7**. VulkanScope Database 1.3.6 (ZIP SHA-256 `864e515dffa51d59ab9bf6b790b96a8bfea791cd782408b283df7264de8d22b8`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical 64-hex report IDs, normalizer 16, Vulkan 1.4.362/header 362, validated browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Settings > Internet > Browser information uses a current, vendor-recognizable browser mark for every browser identity that VulkanScope Database can explicitly distinguish: Google Chrome, Chromium, Microsoft Edge, Mozilla Firefox, Apple Safari, Opera Browser, Brave, Vivaldi and Samsung Browser. The former Samsung Internet display name is updated to the current Samsung Browser product name while the `SamsungBrowser/` UA token remains the parser input.
- Browser marks are bundled as first-party-served static assets and preserve their intrinsic/original brand colors. CSS must not recolor, monochromize or `currentColor`-tint them. The logo host may provide a neutral contrast surface only. No logo CDN, favicon service, analytics service, third-party fingerprinting endpoint or other runtime external request may be introduced.
- Browser identity remains best-effort and privacy-preserving. Brave may be identified only from browser-exposed Brave signals; Vivaldi/Samsung/Edge/Opera/Firefox/Chrome/Safari are identified only from exposed UA/Client-Hint/browser signals already available to the page. When identity cannot be distinguished reliably, the neutral browser icon remains; a generic Chromium-family result must not be falsely branded as Google Chrome or Chromium.
- Local browser-brand provenance/trademark notes are shipped in `licenses/browser-marks.md`. The browser marks are descriptive identification only and never imply affiliation, sponsorship or endorsement. Vendor trademark restrictions remain applicable.
- Browser-visible JavaScript advances to `assets/app.v1307.js`, `assets/browser-compat.v1307.js` and `assets/release-bootstrap.v1307.js`, cache key 1307. Pages retains only the immutable 1.3.6 frontend triplet as the one-release cache-propagation bridge; 1.3.5 frontend assets are stale and forbidden.
- `tools/verify_1_3_7_browser_brand_marks.py`, its negative-mutation suite, immutable 1.3.6 regression contract, source/Pages audit, Worker transport/security tests, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, report-evidence semantic change or Vulkan capability-state inference change is permitted for this release.


## Release 1.3.8 filter / regional-mode / browser-cleanup requirements
- Database release identity is **1.3.8**. VulkanScope Database 1.3.7 (ZIP SHA-256 `1d58903dc5a8bf472022b59248c61d13af3a2bef3b6fbc21cdcad227e4010f49`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, validated browser floors and VulkanScope 1.2.5 new-submission floor remain unchanged.
- Settings > Internet > Browser information is text-only. Browser logo/icon/mark hosts and browser-brand image assets are removed; the page must not fetch or bundle a browser-logo service/asset for this panel. Browser/runtime detection remains local and best-effort.
- Custom-select choice labels must remain readable at narrow widths: meaningful labels wrap inside their own control/menu surface instead of being clipped behind ellipsis. Disabled native selects are mirrored by disabled custom-select buttons and cannot be opened.
- When pagination has exactly one page, page-number jump inputs and Go controls are disabled, unfocusable, and non-interactive. Existing Previous/Next disabled states remain authoritative.
- Regional date/time presentation exposes exactly three modes: **Automatic**, **Country**, and **Manual**. Automatic uses browser/system language/region/time zone and locks Country/Date/Clock/Time-zone/Season controls. Country enables only Country / region and derives locale date/clock conventions plus a representative IANA time zone and ordinary zone DST rules from that country. Manual enables all granular controls; `Browser / system region` means no country override.
- Country mode is presentation-only. Countries spanning multiple IANA zones use the documented in-code representative primary zone; this must not rewrite server-authored timestamps, report payloads, report hashes, sorting epochs, age filters or comparison identity.
- Browser-visible current assets advance to `assets/app.v1308.js`, `assets/browser-compat.v1308.js`, `assets/release-bootstrap.v1308.js` and `assets/site.v1308.css`, cache key 1308. Pages retains the immutable 1.3.7 JS triplet and immutable `site.v0390.css` as the one-release cache-propagation bridge.
- `tools/verify_1_3_8_regional_filter_browser_cleanup.py`, its negative-mutation suite, immutable 1.3.7 regression contract, source/Pages audit, Worker contract, D1 migration replay and deterministic strict-package build are mandatory release gates.
- No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment, producer-floor change or Vulkan evidence semantic inference change is permitted.

## Release 1.3.9 regional-control fit / country-profile / filter-gutter requirements
- Regional mode remains exactly Automatic / Country / Manual. Automatic locks every regional override. Country enables only Country / region. Manual **keeps Country / region disabled** and enables only Date format, Clock format, Time zone and Seasonal offset; this supersedes the 1.3.8 sentence that Manual unlocks every granular control.
- Entering Manual resets the country override to Browser / system region so a previously selected country cannot silently influence Manual locale presentation.
- Country mode displays a separate readable profile for the selected country: representative locale, date-format pattern, 12/24-hour convention, representative IANA zone, current UTC offset, standard/winter offset, daylight/summer offset, whether a seasonal clock change is observed in the representative zone for the current year, and the current offset state. Browser Intl data is authoritative for presentation; multi-zone countries still use the documented representative zone only.
- Country/regional presentation never rewrites D1 `submitted_at`, report payloads, hashes, sort epochs, age filters or Compare identity.
- Settings regional selectors use full available width. Filter and selector labels remain fully readable by vertical wrapping rather than clipping or being hidden behind custom scrollbar chrome.
- Custom-select scrollbar gutter space is demand-driven. A non-overflowing option list reserves no blank scrollbar column; an overflowing list reserves the rail width only while `.has-surface-scrollbar` is active.
- Browser-visible JavaScript advances to `assets/app.v1309.js`, `assets/browser-compat.v1309.js` and `assets/release-bootstrap.v1309.js`; stylesheet advances to `assets/site.v1309.css`, cache key 1309. The 1.3.8 JavaScript triplet remains the immediate cache bridge.
- `tools/verify_1_3_9_regional_filter_layout_profile.py`, its negative-mutation suite, immutable 1.3.8 regression contract, source/Pages audit, Worker contract, D1 migration replay and deterministic strict-package build are mandatory release gates.

## Release 1.3.10 country-selector search / pagination requirements
- Database release identity is **1.3.10**. VulkanScope Database 1.3.9 (ZIP SHA-256 `6f0180d5f53559f418b46b6e43aef66163c60ef768249ea9c7c51e3124edf059`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362, validated browser floors and the VulkanScope 1.2.5 new-submission floor remain unchanged.
- Settings > Preferences > Country / region is a searchable custom selector in Country mode. Search operates on the complete native country/region option set before deterministic **50-option pagination**, and matches the rendered country/region text including its two-letter code. The native `<select>` value remains authoritative.
- The Country / region selector exposes the same page controls as other searchable selectors: Previous, Next, direct numeric page entry and Go. The controls remain rendered for one-page search results and follow the existing disabled/unfocusable one-page contract.
- Opening Country / region begins browsing at page 1 instead of jumping the menu to the page containing the currently selected country. The selected country remains shown in the closed control, but the first opened browse page begins with Browser / system and the alphabetically sorted country list. A selected value such as Türkiye must therefore never strand the user at a later page beginning around Solomon Islands with no path to earlier countries.
- Typing in the Country / region search resets the result page to page 1. Search filtering is applied before pagination, so no matching country is hidden merely because it was on another unfiltered page. Pagination and search do not mutate the selected country until the user explicitly chooses an option.
- Country/region ordering remains derived from English `Intl.DisplayNames` and the project's natural collator; codes are not used as the primary sort key. The existing representative IANA-zone profile remains presentation-only.
- Settings > Preferences > Time zone uses the same searchable/paged large-selector infrastructure so its complete native option set is also reachable without a selected-value page trap. This does not change Automatic/Country/Manual ownership or timestamp semantics.
- Browser-visible JavaScript advances to `assets/app.v1310.js`, `assets/browser-compat.v1310.js` and `assets/release-bootstrap.v1310.js`, cache key 1310. `assets/site.v1309.css` remains the current stylesheet because this correction requires no CSS change. The immutable 1.3.9 JavaScript triplet is the immediate startup/cache bridge.
- `tools/verify_1_3_10_country_selector_search_pagination.py`, its negative-mutation suite, immutable 1.3.9 regression contract, source/Pages audits, Worker contract, D1 migration replay and deterministic strict-package construction are mandatory release gates. No D1 migration, report-payload rewrite, report-hash rewrite, normalizer increment, producer-floor change or Vulkan evidence-semantic change is permitted.

## Release 1.4.0 submission floor / Settings order / address mosaic / startup-filter requirements
- Database release identity is **1.4.0**. VulkanScope Database 1.3.10 (ZIP SHA-256 `3f9932e9661ec93398c6a608234597c1c1fbf20ab2a97bf93dd87364ffdf3581`) is the immutable predecessor. D1 schema/migrations, stored report payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362 and validated browser floors remain unchanged.
- New report POSTs require **VulkanScope 1.4.0 or newer**. VulkanScope 1.4.0 itself is accepted when the submission is otherwise valid; every lower producer version is rejected by the Worker before generic report validation. Existing stored reports from lower producer versions remain readable and comparable through GET paths.
- Settings category order is exactly **Favorites, Preferences, Internet, Information** from left to right. Favorites is the initial active category; keyboard tab order follows the same DOM order.
- Request-scoped observed IPv4 and IPv6 address values are visually mosaicked by default. Only an address family actually observed by the Worker receives a reveal control; the unobserved second family remains explicitly not observed and is never fabricated.
- Clicking an observed address smoothly removes its mosaic; clicking the same address again restores it. IPv4 and IPv6 reveal state is independent. Closing Settings or leaving the Internet category remasks both families, and a newly observed address never inherits the previous value's revealed state. Reduced-motion users receive the same state transitions without decorative animation.
- Address mosaic/reveal is presentation-only. Request IPs remain non-persistent transport observations: they are not report fields, are not written to D1, do not participate in report identity/hash, and do not add a third-party request.
- The authoritative native report filters remain in the document for progressive enhancement but the filter panel is HTML-hidden during initial report/database loading. The rendered-view owner may reveal it only after the first committed render, preventing legacy/native filter controls from flashing before the current custom-filter UI is ready.
- The 1.3.10 Country / region and Time zone searchable 50-option pagination contract remains regression-protected.
- Browser-visible current assets advance to `assets/app.v1400.js`, `assets/browser-compat.v1400.js`, `assets/release-bootstrap.v1400.js` and `assets/site.v1400.css`, cache key 1400. The immutable 1.3.10 JavaScript triplet and `site.v1309.css` are retained as the immediate cache-propagation bridge.
- `tools/verify_1_4_0_submission_settings_ip_filter_boot.py`, its negative-mutation suite, immutable 1.3.10 regression contract, source/Pages audits, Worker transport test, D1 migration replay and deterministic strict-package construction are mandatory release gates. No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment or Vulkan capability-evidence inference change is introduced.

## Release 1.4.1 independent address controls / hard startup-filter hold requirements
- Database release identity is **1.4.1**. VulkanScope Database 1.4.0 (ZIP SHA-256 `c50af2d9597a86a81f6a24c6f1a9208be3bae325a7a9cf1d62894deaf94322ca`) is the immutable predecessor. The VulkanScope new-submission floor remains **1.4.0+**; D1 schema/migrations, stored report payload bytes/hashes, canonical report IDs, normalizer 16, Vulkan 1.4.362/header 362 and validated browser floors remain unchanged.
- Every rendered observed-address control owns an independent reveal slot. **Active address**, **IPv4**, **IPv6** and **Pseudo IPv4** must never share reveal state merely because two rows currently contain the same address or address family. Revealing or hiding one row changes only that row.
- Closing Settings or leaving the Internet category remasks every address slot. When a slot's observed value changes, only the corresponding slot is reset to masked before rerender. Unobserved address families remain explicit `Not observed on this request` text and receive no reveal control.
- The masked presentation uses an opaque local mosaic layer plus blurred underlying text, with a clearly separated eye-icon **Show/Hide** action. Reveal/remask transitions are smooth, keyboard focus remains visible, and `prefers-reduced-motion: reduce` disables decorative motion without changing privacy state.
- The startup filter hold is parse-time and runtime-owned. `<body>` begins with `database-loading`; while that class is present, `#filters` is forced to `display:none`. `setDatabaseLoading(...)` is the single owner of the class and removes it only after the first report-backed render has committed. The existing HTML `hidden` state remains as progressive-enhancement fallback. No legacy/native or pre-grouped custom filter layout may be visible while report/preload construction is in progress.
- Browser-visible current assets advance to `assets/app.v1401.js`, `assets/browser-compat.v1401.js`, `assets/release-bootstrap.v1401.js` and `assets/site.v1401.css`, cache key 1401. The immutable 1.4.0 JavaScript triplet and `site.v1400.css` are retained as the immediate cache-propagation bridge.
- `tools/verify_1_4_1_address_privacy_filter_startup.py`, its negative-mutation suite, source/Pages audits, Worker contract, D1 migration replay and deterministic strict-package construction are mandatory release gates. No D1 migration, stored-payload rewrite, report-hash rewrite, normalizer increment, producer-floor change or Vulkan capability-evidence inference change is permitted.
