# VulkanScope Database 0.39.16 / VulkanScope 0.41.41 historical Compare identity audit

## Evidence
Immutable predecessor: VulkanScope Database 0.39.15, SHA-256 `bdf533c45c84644f66ee29d2fccd3151b2990adb7622031c688a33904a609831`.

The supplied comparison between a pre-0.41.40 report and a 0.41.40 report shows same Vulkan property evidence rendered as one-sided fields because older producers serialized `VkPhysicalDevice*Properties*` boolean members into the Features dataset while 0.41.40 corrected them to detailed properties. The same comparison shows the unfiltered union count labeled `Visible differences` even when identical rows are visible.

Categories: REGRESSION, COMPATIBILITY, REPORTING, USABILITY.

## failing-before-fix
`node tools/test_compare_04141_compat.mjs <immutable 0.39.15>`: FAIL because the historical property-feature identity function and dynamic visible metric do not exist.

## Successor behavior
For producer versions below 0.41.40 only, exact boolean rows named `VkPhysicalDevice*Properties* · field` are canonicalized in Compare to the current property identity with Available query state. Stored payloads and report hashes remain untouched. Real Features rows, non-boolean evidence and current producer rows are explicit false-positive controls and remain unchanged.

When Differences only is disabled the summary uses `Visible fields`; when enabled it uses `Visible differences`.

## Verification
- historical Compare behavior: PASS
- frontend JavaScript syntax: PASS
- negative mutations: mandatory aggregate-gate class
- predecessor failure oracle: PASS as failing-before-fix evidence
- D1 migration: none
- production Worker/Pages deployment: NOT EXECUTED by local source verification

## Final local source gate
The final edited source tree completed `python tools/quality_gate.py` with PASS. This includes deterministic UTF-8 tooling, Vulkan registry/producer verification, report-text identity, repository repair check, immutable 0.39.15 regression contract, existing-repository overlay/strict-package fixtures, source audit, route/Compare contracts, historical Compare compatibility, Compare negative mutations, Worker syntax/contracts, complete D1 migration replay and staged Pages artifact audit.

Production Cloudflare Worker/Pages deployment remains NOT EXECUTED by the local source gate.
