# VulkanScope Database 0.39.25 loading / Encyclopedia UI coherence release

- Release identity remains 0.39.25. This release changes browser presentation only.
- Loading remains an immediate `role=status`, polite live region and continues to expose truthful index/body progress through the existing bounded loader.
- Loading presentation reuses the Database neutral panel, line, radius, text and rose accent tokens; it must not introduce a separate visual theme.
- Encyclopedia remains offline/local and uses the unchanged locked Vulkan 1.4.362 corpus and curated evidence model.
- Encyclopedia search categories, two-character large-family threshold and 24-result bound remain unchanged.
- Registry/reference presence remains separate from runtime capability evidence and the Vulkan Video Not applicable clarification remains visible.
- Encyclopedia presentation reuses Database panel/card, badge, notice, search and typography language. Styling must not create an independent red-gradient visual subsystem.
- The global page-scroll controls remain authoritative; Encyclopedia does not introduce a nested vertical scrolling viewport.
- Materially changed main-page CSS/JS is cache-busted with the `03925` query key while retaining the existing 0.39.25 asset identities.
- Submission schema 2, technicalReport 3, normalizer 16, D1 schema, report hashes, Worker validation, producer baseline/floor and stored reports are unchanged.
