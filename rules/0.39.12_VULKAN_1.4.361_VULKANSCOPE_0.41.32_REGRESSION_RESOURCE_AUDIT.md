# VulkanScope Database 0.39.12 — Vulkan 1.4.361 / VulkanScope 0.41.32 regression and resource audit

- VulkanScope 0.41.32 is the pinned current producer baseline and Vulkan 1.4.361 is the pinned canonical registry baseline.
- Schema 2 / technicalReport 3 remain the transport contract; additive 0.41.24+ summary provenance fields are accepted and cross-checked rather than silently discarded.
- VulkanScope 0.41.18+ complete submissions require authoritative `baseReportComplete=true`; 0.41.24+ additionally requires complete, non-safety-rejected physical-device enumeration.
- VulkanScope 0.41.32 registry coverage is checked against the pinned 1.4.361 producer contract, including `VkPhysicalDevicePrivateDataBaseHandleFeaturesNV`.
- The 2 MiB request ceiling remains fail-closed and non-truncating. Request decoding is streaming, byte-bounded and strict UTF-8.
- Compact report detail responses return the stored canonical payload without server-side normalized-array duplication; the legacy expanded detail route remains available.
- D1 remains payload-JSON based and requires no migration for the additive producer envelope.
- All release changes must pass predecessor regression, Worker contract, frontend route/compare, source/package hygiene, registry and producer compatibility gates.
