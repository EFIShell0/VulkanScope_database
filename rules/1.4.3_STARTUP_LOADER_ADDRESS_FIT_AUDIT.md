# VulkanScope Database 1.4.3 startup loader / address-fit audit

- Immutable predecessor: Database 1.4.2, SHA-256 `29dbf337d2305dfc1d898bd0dc8279f287af7ce06444bae7c22e613267e226f6`.
- Root cause of the recorded startup corruption: `database-loading` was simultaneously the `<body>` state class and the loading card presentation class, so card flex/padding/border rules were applied to the body during startup.
- Correction: the card is now `database-loading-panel`; `database-loading` remains state-only for filter/loading ownership. During the startup hold, `#databaseLoading` is fixed to the viewport center with viewport-bounded dimensions.
- Address correction: observed-address rows are full-width; address text uses safe wrapping and no ellipsis/nowrap truncation. The independent privacy slots and opaque mosaic behavior are unchanged.
- Responsive contract covers desktop plus 520 px and 340 px narrow layouts without document-level horizontal overflow.
- No persistence, D1, report schema/hash, normalizer, Vulkan registry/evidence, Compare, or producer-floor semantics change.
