# 0.34.9 Compare / Portability density audit

- Scoped compact styling to `data-main-view="compare"` and `data-main-view="portability"`.
- Compare report selectors remain backed by the original native `<select>` elements through the existing custom-listbox enhancement.
- Differences-only remains the existing native checkbox state source.
- Portability state inference is unchanged: explicit extension enumeration is Supported; absence remains Unknown/not listed.
- No comparison categories or table columns were removed.
- Mobile keeps responsive wrapping and existing table-scroll controls.
