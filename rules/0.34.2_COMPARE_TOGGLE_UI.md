# VulkanScope Database 0.34.2 Compare toggle UI audit

- The `#diffOnly` native checkbox remains the authoritative Compare state and continues to drive the existing `change` handler.
- Presentation uses a local custom check surface with a VulkanScope red accent, animated check stroke, hover/pressed/focus states, and a 44 px-class interaction target.
- The entire label is clickable/touchable; keyboard focus remains on the native checkbox and is surfaced through `:focus-visible`.
- `prefers-reduced-motion: reduce` disables decorative transitions.
- No Compare data semantics or filtering logic changed.
