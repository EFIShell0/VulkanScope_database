# VulkanScope Database 1.2.7 Compare / Hero / Scroll Audit

- Immutable predecessor: VulkanScope Database 1.2.6, SHA-256 `3fcfeb0c819dbe87035419e5743aa1b59f6ec1b0bc0491624db5ce5cf5a005cc`.
- Scope: Compare sticky shell and selector overflow, viewport-scrollbar lower endpoint/cursor, persistent Database hero presentation, Reports row-hover motion and release identity.
- Compare: dedicated `compare-sticky-shell` owns sticky positioning; workspace/report cards remain overflow-visible and opened custom selectors elevate above the sticky surface.
- Viewport rail: authoritative `document.scrollingElement` max range; both impossible endpoint arrows are gray/disabled; thumb uses default cursor while remaining pointer-draggable and keyboard-operable.
- Hero: active-tab META is projected into a single shared workspace card; no report/evidence semantics are duplicated or changed.
- Reports: all cells in one row share the same hover surface transition, including Submitted/date-time when pointer enters through a distant column.
- Data contract: D1 schema/migrations, stored payload/hash semantics, normalizer 16, Vulkan 1.4.362/header 362 and VulkanScope 1.2.5 submission floor unchanged.
