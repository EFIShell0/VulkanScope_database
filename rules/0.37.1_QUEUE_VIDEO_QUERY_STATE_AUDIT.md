# VulkanScope Database 0.37.1 Queue / Video / Query-State Audit

- VulkanScope 0.41.3 / 413 is the current producer baseline.
- Queue capability booleans remain direct `VkQueueFlags` evidence.
- Zero `VkQueueFlags` is not assigned a nonexistent `VK_NONE` name.
- Vulkan Video codec-operation mask is displayed only with explicit query-state evidence.
- Successfully queried zero uses `VK_VIDEO_CODEC_OPERATION_NONE_KHR`.
- Not applicable, Unavailable and Unknown remain distinct and are never collapsed into Unsupported.
- Property and limit state labels explicitly describe query availability; false property values remain values.
- Schema 2 / technicalReport 3 and D1 storage remain unchanged.
