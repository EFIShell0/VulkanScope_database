# VulkanScope Database 1.0.16 live synchronization / modal details / Versions audit

## Release identity
- Database version: 1.0.16
- Immutable predecessor: VulkanScope Database 1.0.15
- Producer/query baseline remains VulkanScope 1.0.19 / Vulkan 1.4.362.
- Submission schema 2, technical report 3, normalizer 16 and D1 storage schema remain unchanged.

## Requested repairs
- Widens the right-growing Submitted, Vendor and Type disclosures so full date/time/time-zone, human vendor/family/raw-ID and canonical device-type text fit without clipping; mobile keeps horizontal table scrolling rather than shrinking those expanded columns.
- Converts Distinct/value drill-downs from inline downward accordions to the same right-arrow, centered modal and dark backdrop used by Coverage reports.
- Rebuilds Versions visualization with the Statistics donut language. Each slice is a GPU + selected version cohort; the separate table lists GPU, version, report count and share with no coverage bars.
- Adds bounded live report synchronization every 10 seconds and when the page regains focus/visibility/connectivity. Only missing payloads are fetched; the report map is swapped atomically and all metrics/views re-render only when the report universe changes.
- Adds explicit API databaseVersion metadata and a dark-backdrop update dialog with Refresh now when a newer frontend/database release is detected. Frontend code is never silently hot-swapped.

## Preserved contracts
- 1.0.15 Vendor/Type/Submitted pinned controls, coverage dominance/hatching, zero-state tracks, Developer Info, vendor-ID provenance, coverage-report modal, high-percentage glow, exact HDR10+ artwork, display chips, loading accents and soft/reduced-motion interactions remain mandatory.
