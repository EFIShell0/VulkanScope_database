# VulkanScope Database 1.2.4 CI / bounded report-table audit

- Corrected the canonical Actions workflow so source, generated-index and release verification use the current 1.2.4 contract instead of the predecessor 1.2.2 release verifier.
- Preserved bounded transient GitHub Release retry and immutable-tag ownership checks.
- Added a shared bounded report-table renderer with independent page cursors and shared 10/25/50 page size.
- Applied it to every normal table that enumerates GPU/device/report identity outside Reports: Devices, Versions, Memory heaps/per-report types, Queues, Surface formats/presentation queues, Display & HDR, Portability and Statistics GPU models.
- Coverage/statistical inputs are computed before pagination; only final HTML row arrays are sliced.
- D1, payload, normalizer, Vulkan evidence and producer-floor semantics are unchanged.
