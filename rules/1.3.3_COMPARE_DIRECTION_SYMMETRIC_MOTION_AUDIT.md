# VulkanScope Database 1.3.3 Compare direction / symmetric motion audit

This release is a presentation-only successor to immutable Database 1.3.2 (`77c611aa89f26c6f3de3ed09046f7f54d3c99b35e47f93df0e5ebb88798afa73`). It does not change D1, report payload/hash semantics, the normalizer, Vulkan registry baseline, producer floor, or browser floors.

Mandatory checks:

- The Compare control has one direction owner: JavaScript path geometry. Full/Minimize is down; minimized/Expand is up. No `.is-minimized` CSS rotation may invert the same SVG a second time.
- Compare reset/unpin returns the full-state label, ARIA state and downward chevron. The upper Compare workspace remains on the established smooth/reduced-motion state path.
- License viewer open and close are symmetric opacity/transform/visibility transitions. A motion token prevents stale close timers from hiding a reopened viewer; focus restoration remains correct.
- Custom-select Previous/Next/direct-page jumps and bounded modal pagination animate only after authoritative page state/DOM visibility changes. Page animation is compositor-only, cancelled/replaced on rapid input, and skipped under reduced motion.
- Pages includes the 1.3.2 predecessor app/browser/bootstrap triplet plus the 1.3.3 current triplet. Repository repair removes older versioned frontend triplets.
- Current source/Pages identity is 1.3.3 / cache 1303. The 1.3.3 verifier and negative-mutation suite run at all release gates.
- Regression contract is anchored to the immutable 1.3.2 ZIP; deterministic clean-extract packaging and D1 migration replay must pass.
