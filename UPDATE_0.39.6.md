# Updating to VulkanScope Database 0.39.6

Replace repository files, run `python tools/repair_repository.py --apply`, audit with `python tools/audit_database.py --source-tree .`, run the route/compare/Worker contracts, then commit deletions with `git add -A`.
