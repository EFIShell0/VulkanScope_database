# VulkanScope Database 0.34.2

## Compare control refinement

- Replaced the browser-like `Differences only` checkbox presentation with a VulkanScope-styled custom checkbox.
- Added a clearer check mark, VulkanScope red checked state, hover/pressed/focus feedback, and a touch-friendly interaction target.
- Kept the native checkbox as the authoritative state so Compare behavior and accessibility remain intact.
- Added reduced-motion handling.

No Vulkan report semantics, canonical names, normalization rules, or Worker submission behavior changed.
